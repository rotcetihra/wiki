# TODO

## Текущая задача: Добавить раздел «Библиотеки» для C++

**Цель:** Документировать все стандартные заголовки C++ и POSIX-заголовки (аналог раздела `Библиотеки` для C).

### Структура

**Папка:** `Языки программирования/C++/Библиотеки/`

### Что сделано

- Определён полный список заголовков (см. ниже)

### Что делается

Создание категории 8 «Утилиты» — 7 заголовков.

### Что нужно сделать

#### Категория 3: Контейнеры (14 заголовков) — ЗАВЕРШЕНО ✅
- [x] `<array>` — фиксированный массив (23 файла)
- [x] `<deque>` — дек (30 файлов)
- [x] `<flat_map>` — плоская карта C++23 (21 файл)
- [x] `<flat_set>` — плоское множество C++23 (16 файлов)
- [x] `<forward_list>` — односвязный список (21 файл)
- [x] `<list>` — двусвязный список (25 файлов)
- [x] `<map>` — ассоциативный массив (21 файл)
- [x] `<set>` — множество (17 файлов)
- [x] `<span>` — диапазон C++20 (13 файлов)
- [x] `<spanstream>` — поток span C++23 (6 файлов)
- [x] `<stack>` — стек (9 файлов)
- [x] `<unordered_map>` — неупорядоченная карта (24 файла)
- [x] `<unordered_set>` — неупорядоченное множество (18 файлов)
- [x] `<vector>` — динамический массив (30 файлов)
- **Итого:** 274 файла

#### Категория 1: Языковая поддержка (9 заголовков)
- [x] `<algorithm>` — алгоритмы
- [x] `<any>` — любой тип
- [x] `<atomic>` — атомарные операции
- [x] `<barrier>` — барьер синхронизации
- [x] `<bit>` — битовые операции
- [x] `<charconv>` — преобразование символов
- [x] `<chrono>` — хронометраж
- [x] `<compare>` — трёхстороннее сравнение
- [x] `<concepts>` — концепты

#### Категория 2: Многопоточность (5 заголовков)
- [ ] `<condition_variable>` — переменная условия
- [ ] `<mutex>` — мьютекс
- [ ] `<semaphore>` — семафор
- [ ] `<shared_mutex>` — разделяемый мьютекс
- [ ] `<thread>` — потоки

#### Категория 3: Контейнеры (14 заголовков)
- [ ] `<array>` — массив
- [ ] `<deque>` — дек
- [ ] `<flat_map>` — плоская карта (C++23)
- [ ] `<flat_set>` — плоское множество (C++23)
- [ ] `<forward_list>` — односвязный список
- [ ] `<list>` — двусвязный список
- [ ] `<map>` — ассоциативный массив
- [ ] `<set>` — множество
- [ ] `<span>` — диапазон
- [ ] `<spanstream>` — поток span (C++23)
- [ ] `<stack>` — стек
- [ ] `<unordered_map>` — неупорядоченная карта
- [ ] `<unordered_set>` — неупорядоченное множество
- [ ] `<vector>` — динамический массив

#### Категория 4: Итераторы (1 заголовок)
- [x] `<iterator>` — итераторы

#### Категория 5: Строки (4 заголовка)
- [x] `<format>` — форматирование
- [x] `<regex>` — регулярные выражения
- [x] `<string>` — строки
- [x] `<string_view>` — строковый вид

#### Категория 6: Ввод-вывод (10 заголовков)
- [x] `<fstream>` — файловые потоки
- [x] `<iomanip>` — манипуляторы
- [x] `<ios>` — базовые типы потоков
- [x] `<iosfwd>` — предварительные объявления
- [x] `<iostream>` — стандартные потоки
- [x] `<istream>` — входной поток
- [x] `<ostream>` — выходной поток
- [x] `<print>` — вывод (C++23)
- [x] `<sstream>` — строковые потоки
- [x] `<streambuf>` — буфер потока

#### Категория 7: Поддержка языка (14 заголовков) — ЗАВЕРШЕНО ✅
- [x] `<exception>` — исключения (13 файлов)
- [x] `<functional>` — функциональные объекты (29 файлов)
- [x] `<initializer_list>` — список инициализации (3 файла)
- [x] `<limits>` — числовой диапазон (26 файлов)
- [x] `<memory>` — умные указатели, аллокаторы (20 файлов)
- [x] `<new>` — управление памятью (9 файлов)
- [x] `<numbers>` — мат. константы C++20 (12 файлов)
- [x] `<numeric>` — числовые операции (14 файлов)
- [x] `<optional>` — опциональное значение (12 файлов)
- [x] `<source_location>` — местоположение источника C++20 (2 файла)
- [x] `<tuple>` — кортеж (16 файлов)
- [x] `<type_traits>` — свойства типов (54 файла)
- [x] `<typeindex>` — индекс типа (8 файлов)
- [x] `<typeinfo>` — информация о типе (6 файлов)
- **Итого:** 238 файлов (14 индексов + 224 статьи)

#### Категория 8: Утилиты (7 заголовков)
- [ ] `<bit>` — битовые утилиты (дубль, если не в категории 1)
- [ ] `<cstdlib>` — abs, div, labs, etc.
- [ ] `<cmath>` — мат. функции (C++ обёртки)
- [ ] `<complex>` — комплексные числа
- [ ] `<valarray>` — векторные массивы
- [ ] `<ratio>` — рациональная арифметика
- [ ] `<utility>` — utility, swap, forward, move

#### Категория 9: Корутины и выполнение (2 заголовка)
- [ ] `<coroutine>` — корутины (C++20)
- [ ] `<execution>` — политики выполнения (C++17)

#### Категория 10: Локализация (1 заголовок)
- [ ] `<locale>` — локализация

#### Категория 11: Файловая система и ошибки (2 заголовка)
- [ ] `<filesystem>` — файловая система (C++17)
- [ ] `<system_error>` — системные ошибки

#### Категория 12: Совместимость с C (23 заголовка)
- [ ] `<cassert>` — assert
- [ ] `<ccomplex>` — комплексные (C)
- [ ] `<cctype>` — classify characters (C)
- [ ] `<cerrno>` — errno (C)
- [ ] `<cfenv>` — среда плавающей точки (C)
- [ ] `<cfloat>` — float limits (C)
- [ ] `<climits>` — int limits (C)
- [ ] `<clocale>` — локализация (C)
- [ ] `<csetjmp>` — setjmp/longjmp (C)
- [ ] `<csignal>` — сигналы (C)
- [ ] `<cstdarg>` — va_list (C)
- [ ] `<cstddef>` — size_t, nullptr_t (C)
- [ ] `<cstdint>` — int types (C)
- [ ] `<cstdio>` — ввод-вывод (C)
- [ ] `<cstdlib>` — память, числа (C)
- [ ] `<cstring>` — строки (C)
- [ ] `<ctgmath>` — тригонометрия (C)
- [ ] `<ctime>` — время (C)
- [ ] `<cuchar>` — Unicode (C)
- [ ] `<cwchar>` —宽字符 (C)
- [ ] `<cwctype>` —宽классификация (C)

#### Категория 13: POSIX/Linux заголовки (25+ заголовков)
- [ ] `<aio.h>` — асинхронный ввод-вывод
- [ ] `<arpa/inet.h>` — интернет-функции
- [ ] `<dirent.h>` — каталоги
- [ ] `<dlfcn.h>` — динамическая загрузка
- [ ] `<fcntl.h>` — управление файлами
- [ ] `<fnmatch.h>` — сопоставление имён файлов
- [ ] `<glob.h>` — шаблоны путей
- [ ] `<grp.h>` — группы
- [ ] `<iconv.h>` — преобразование кодировок
- [ ] `<langinfo.h>` — информация о языке
- [ ] `<libgen.h>` — basename/dirname
- [ ] `<limits.h>` — лимиты
- [ ] `<locale.h>` — локализация (POSIX)
- [ ] `<mqueue.h>` — очереди сообщений
- [ ] `<ndbm.h>` — база данных
- [ ] `<netdb.h>` — réseau / network database
- [ ] `<netinet/in.h>` — интернет-протоколы
- [ ] `<poll.h>` — опрос файловых дескрипторов
- [ ] `<pthread.h>` — потоки POSIX
- [ ] `<pwd.h>` — пользователи
- [ ] `<regex.h>` — регулярные выражения (POSIX)
- [ ] `<sched.h>` — планировщик
- [ ] `<search.h>` — поиск
- [ ] `<semaphore.h>` — семафоры POSIX
- [ ] `<signal.h>` — сигналы (POSIX)
- [ ] `<spawn.h>` — запуск процессов
- [ ] `<string.h>` — строки (POSIX)
- [ ] `<strings.h>` — строки (POSIX)
- [ ] `<sys/mman.h>` — отображение памяти
- [ ] `<sys/resource.h>` — ресурсы
- [ ] `<sys/select.h>` — select
- [ ] `<sys/socket.h>` — сокеты
- [ ] `<sys/stat.h>` — информация о файлах
- [ ] `<sys/time.h>` — время
- [ ] `<sys/types.h>` — типы данных
- [ ] `<sys/un.h>` — Unix-сокеты
- [ ] `<sys/wait.h>` — ожидание процессов
- [ ] `<syslog.h>` — системный журнал
- [ ] `<termios.h>` — терминал
- [ ] `<time.h>` — время (POSIX)
- [ ] `<unistd.h>` — стандартные функции POSIX
- [ ] `<utime.h>` — время модификации
- [ ] `<wordexp.h>` — расширение слов

### Источники

- https://en.cppreference.com/w/cpp/header
- Стандарт C++23 (ISO/IEC 14882:2024)
- POSIX.1-2024 (IEEE Std 1003.1-2024)

---

## Выполнено

### Скопировать все руководства C# с metanit.com

**Статус:** Завершено ✅
**Коммит:** `a4a6dee`

| Категория | Руководств | Файлов |
|-----------|-----------|--------|
| Руководства | 9 | 376 |
| Веб-разработка | 7 | 554 |
| Графические программы | 7 | 542 |
| Базы данных | 6 | 232 |
| **Итого** | **29** | **1704** |

### Добавить Руководство по Vulkan

**Статус:** Завершено ✅
**Коммит:** `f29074f`

### Добавить Руководство по Qt

**Статус:** Завершено ✅
**Коммит:** `00eb594`

### Исправить хлебные крошки и навигацию

**Статус:** Завершено ✅
**Коммит:** `5f8c24a`

### Добавить Руководство по C++

**Статус:** Завершено ✅
**Коммиты:** `144c319`..`ba396b1`

### Добавить Хеш таблицы

**Статус:** Завершено ✅
**Коммиты:** `df83367`..`e7dc4d6`

### Добавить Руководство по GTK

**Статус:** Завершено ✅
**Коммиты:** `15493c1`..`f3ba02c`

### Добавить Базы данных: Глава 1 SQLite

**Статус:** Завершено ✅
**Коммиты:** `6b485d5`..`16e6e3a`

### Замена «Организация программ» копиями metanit.com

**Статус:** Завершено ✅
**Коммиты:** `673508a`..`fbe3f0a`

### Замена Руководства по Си копиями metanit.com

**Статус:** Завершено ✅
**Коммиты:** `889bde3`..`a0622b4`
