# TODO

## Текущая задача: Обновить документацию библиотек C23 по man-страницам

### Что сделано

Обновлены все 31 заголовков (новый формат с переводом man-страниц):
- `<assert.h>` — assert(3)
- `<complex.h>` — complex.h(0p)
- `<ctype.h>` — ctype(3)
- `<errno.h>` — errno(3)
- `<fenv.h>` — fenv(3)
- `<float.h>` — float.h(0p)
- `<inttypes.h>` — inttypes.h(0p)
- `<iso646.h>` — iso646.h(0p)
- `<limits.h>` — limits.h(0p)
- `<locale.h>` — locale.h(0p)
- `<math.h>` — math.h(0p)
- `<setjmp.h>` — setjmp(3)
- `<signal.h>` — signal(7)
- `<stdalign.h>` — stdalign.h (POSIX)
- `<stdarg.h>` — stdarg(3)
- `<stdatomic.h>` — stdatomic.h (POSIX)
- `<stdbit.h>` — stdbit.h (C23)
- `<stdbool.h>` — stdbool.h(0p)
- `<stdckdint.h>` — stdckdint.h (C23)
- `<stddef.h>` — stddef.h(0p)
- `<stdint.h>` — stdint.h(0p)
- `<stdio.h>` — stdio.h(0p)
- `<stdlib.h>` — stdlib.h(0p)
- `<string.h>` — string.h(0p)
- `<strings.h>` — strings.h(0p)
- `<tgmath.h>` — tgmath.h(0p)
- `<threads.h>` — threads.h (C11)
- `<time.h>` — time.h(0p)
- `<uchar.h>` — uchar.h (POSIX)
- `<wchar.h>` — wchar.h(0p)
- `<wctype.h>` — wctype.h(0p)

### Что делается

Задача завершена.

### Что нужно сделать

- [ ] Обновить TODO.md в корне проекта
- [ ] Закоммитить изменения
- [ ] Запушить в удалённый репозиторий

## Библиотеки C23

- `<assert.h>` — проверка утверждений ✅
- `<complex.h>` — комплексная арифметика ✅
- `<ctype.h>` — классификация символов ✅
- `<errno.h>` — коды ошибок ✅
- `<fenv.h>` — окружение чисел с плавающей запятой ✅
- `<float.h>` — константы для типов с плавающей запятой ✅
- `<inttypes.h>` — макросы для целочисленных типов ✅
- `<iso646.h>` — альтернативные операторы ✅
- `<limits.h>` — константы для целочисленных типов ✅
- `<locale.h>` — локализация ✅
- `<math.h>` — математические функции ✅
- `<setjmp.h>` — нелокальные переходы ✅
- `<signal.h>` — обработка сигналов ✅
- `<stdalign.h>` — выравнивание ✅
- `<stdarg.h>` — variadic аргументы ✅
- `<stdatomic.h>` — атомарные операции ✅
- `<stdbit.h>` — битовые операции ✅
- `<stdbool.h>` — логический тип bool ✅
- `<stdckdint.h>` — проверка переполнения ✅
- `<stddef.h>` — стандартные определения ✅
- `<stdint.h>` — целочисленные типы фиксированной ширины ✅
- `<stdio.h>` — ввод/вывод ✅
- `<stdlib.h>` — общие функции ✅
- `<string.h>` — работа со строками ✅
- `<strings.h>` — дополнительные функции строк ✅
- `<tgmath.h>` — обобщённые математические макросы ✅
- `<threads.h>` — многопоточность ✅
- `<time.h>` — дата и время ✅
- `<uchar.h>` — Unicode символы ✅
- `<wchar.h>` — широкие строки ✅
- `<wctype.h>` — классификация широких символов ✅
