# TODO

## Библиотеки C23

- `<assert.h>` —断言
- `<complex.h>` — комплексная арифметика
- `<ctype.h>` — классификация символов
- `<errno.h>` — коды ошибок
- `<fenv.h>` — окружение чисел с плавающей запятой
- `<float.h>` — константы для типов с плавающей запятой
- `<inttypes.h>` — макросы для целочисленных типов
- `<iso646.h>` — альтернативные операторы
- `<limits.h>` — константы для целочисленных типов
- `<locale.h>` — локализация
- `<math.h>` — математические функции
- `<setjmp.h>` — нелокальные переходы
- `<signal.h>` — обработка сигналов
- `<stdalign.h>` — выравнивание
- `<stdarg.h>` — variadic аргументы
- `<stdatomic.h>` — атомарные операции
- `<stdbit.h>` — битовые операции
- `<stdbool.h>` — логический тип bool
- `<stdckdint.h>` — проверка переполнения
- `<stddef.h>` — стандартные определения
- `<stdint.h>` — целочисленные типы фиксированной ширины
- `<stdio.h>` — ввод/вывод
- `<stdlib.h>` — общие функции
- `<string.h>` — работа со строками
- `<strings.h>` — дополнительные функции строк
- `<tgmath.h>` — обобщённые математические макросы
- `<threads.h>` — многопоточность
- `<time.h>` — дата и время
- `<uchar.h>` — Unicode символы
- `<wchar.h>` — широкие строки
- `<wctype.h>` — классификация широких символов

## POSIX-библиотеки

- `<aio.h>` — асинхронный ввод/вывод
- `<arpa/inet.h>` — интернет-адресация
- `<assert.h>` —断言 (POSIX-расширения)
- `<complex.h>` — комплексная арифметика (POSIX-расширения)
- `<cpio.h>` — формат cpio
- `<dirent.h>` — работа с каталогами
- `<dlfcn.h>` — динамическая загрузка библиотек
- `<errno.h>` — коды ошибок (POSIX-расширения)
- `<fcntl.h>` — управление файлами
- `<fenv.h>` — окружение чисел с плавающей запятой (POSIX-расширения)
- `<fmtmsg.h>` — форматированные сообщения
- `<fnmatch.h>` — сопоставление имён файлов
- `<ftw.h>` — обход дерева файлов
- `<glob.h>` — расширение имён файлов
- `<grp.h>` — работа с группами
- `<iconv.h>` — конвертация кодировок
- `<inttypes.h>` — макросы для целочисленных типов (POSIX-расширения)
- `<langinfo.h>` — информация о языке
- `<libgen.h>` — обработка путей
- `<locale.h>` — локализация (POSIX-расширения)
- `<math.h>` — математические функции (POSIX-расширения)
- `<monetary.h>` — денежные форматы
- `<mqueue.h>` — очереди сообщений
- `<ndbm.h>` — база данных
- `<netdb.h>` — база данных сети
- `<netinet/in.h>` — интернет-протоколы
- `<netinet/tcp.h>` — TCP
- `<nl_types.h>` — категории сообщений
- `<poll.h>` — опрос файловых дескрироров
- `<pthread.h>` — потоки выполнения
- `<pwd.h>` — работа с пользователями
- `<regex.h>` — регулярные выражения
- `<sched.h>` — планирование
- `<search.h>` — поиск в таблицах
- `<semaphore.h>` — семафоры
- `<setjmp.h>` — нелокальные переходы (POSIX-расширения)
- `<signal.h>` — обработка сигналов (POSIX-расширения)
- `<spawn.h>` — запуск процессов
- `<stdint.h>` — целочисленные типы (POSIX-расширения)
- `<stdio.h>` — ввод/вывод (POSIX-расширения)
- `<stdlib.h>` — общие функции (POSIX-расширения)
- `<string.h>` — работа со строками (POSIX-расширения)
- `<strings.h>` — дополнительные функции строк (POSIX-расширения)
- `<sys/mman.h>` — отображение памяти
- `<sys/select.h>` — выбор файловых дескрипторов
- `<sys/socket.h>` — сокеты
- `<sys/stat.h>` — информация о файлах
- `<sys/time.h>` — время
- `<sys/times.h>` — время процесса
- `<sys/types.h>` — системные типы
- `<sys/uio.h>` — векторный ввод/вывод
- `<sys/wait.h>` — ожидание процессов
- `<syslog.h>` — системное журналирование>
- `<tar.h>` — формат tar
- `<termios.h>` — управление терминалом
- `<time.h>` — дата и время (POSIX-расширения)
- `<ucontext.h>` — контекст выполнения
- `<unistd.h>` — стандартные функции POSIX
- `<utime.h>` — установка времени
- `<utmpx.h>` — учёт входов
- `<wchar.h>` — широкие строки (POSIX-расширения)
- `<wctype.h>` — классификация широких символов (POSIX-расширения)
