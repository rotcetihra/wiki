# TODO

## Текущая задача: Добавить POSIX-заголовки в Библиотеки

### Что сделано

- Все 31 C23 заголовков обновлены ✅
- Добавлено 38 POSIX/Linux заголовков ✅:
  - `<unistd.h>`, `<fcntl.h>`, `<dirent.h>`, `<sys/stat.h>`, `<sys/types.h>`, `<sys/wait.h>`
  - `<pthread.h>`, `<semaphore.h>`, `<sys/socket.h>`, `<sys/mman.h>`, `<sys/select.h>`, `<spawn.h>`
  - `<poll.h>`, `<regex.h>`, `<netdb.h>`, `<netinet/in.h>`, `<arpa/inet.h>`, `<dlfcn.h>`
  - `<sys/time.h>`, `<sys/resource.h>`, `<termios.h>`, `<sys/un.h>`, `<grp.h>`, `<pwd.h>`
  - `<iconv.h>`, `<langinfo.h>`, `<mqueue.h>`, `<aio.h>`, `<search.h>`, `<utime.h>`
  - `<libgen.h>`, `<nl_types.h>`, `<monetary.h>`, `<fnmatch.h>`, `<glob.h>`, `<wordexp.h>`
  - `<ar.h>`, `<cpio.h>`, `<tar.h>`, `<endian.h>`, `<byteswap.h>`, `<err.h>`
  - `<error.h>`, `<execinfo.h>`, `<fts.h>`, `<malloc.h>`, `<paths.h>`, `<sysexits.h>`
  - `<ucontext.h>`, `<sched.h>`, `<syslog.h>`

### Что делается

Завершено.

### Что нужно сделать

Все POSIX/Linux заголовки добавлены. Задача выполнена.

## Библиотеки C23

Все 31 заголовков C23 ✅

## Выполнено

- Добавление POSIX/Linux заголовков в Библиотеки (38 заголовков) ✅
