# Сохранение документов в базу данных

[[Языки программирования/C#|C#]] / [[Языки программирования/C#/Базы данных|Базы данных]] / [[Языки программирования/C#/Базы данных/Руководство по MongoDB|Руководство по MongoDB]] / [[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB|Глава 1. C# и MongoDB]] / Сохранение документов в базу данных

[[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB/Настройка модели с помощью атрибутов|Назад]] | [[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB|Содержание]] | [[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB/Получение документов из базы данных|Вперёд]]

**Дата написания:** 05.09.2026

Для добавления данных в коллекцию у объекта IMongoCollection применяются методы: InsertOneAsync (для добавления одного документа) и InsertManyAsync() (для добавления набора документов).

### InsertOneAsync

```csharp
using MongoDB.Bson;
using MongoDB.Driver;

MongoClient client = new MongoClient("mongodb://localhost:27017");
var db = client.GetDatabase("test");
var users = db.GetCollection<BsonDocument>("users");

BsonDocument tom = new BsonDocument
{
    {"Name", "Tom"},
    {"Age", 38}
};

await users.InsertOneAsync(tom);
Console.WriteLine(tom);
```

### InsertManyAsync

```csharp
BsonDocument bob = new BsonDocument { {"Name", "Bob"}, {"Age", 42}};
BsonDocument sam = new BsonDocument { {"Name", "Sam"}, {"Age", 25}};
await users.InsertManyAsync(new List<BsonDocument>{ bob, sam});
```

### Добавление Poco-объектов

Мы можем добавлять в коллекцию не только документы BsonDocument, но и объекты стандартных классов C#:

```csharp
var users = db.GetCollection<Person>("users");
Person alice = new Person { Name="Alice", Age= 33 };
await users.InsertOneAsync(alice);
```

**Источник:** [https://metanit.com/sharp/mongodb/1.6.php](https://metanit.com/sharp/mongodb/1.6.php)
