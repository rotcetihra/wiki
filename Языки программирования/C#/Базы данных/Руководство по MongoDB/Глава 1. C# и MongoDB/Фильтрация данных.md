# Фильтрация данных

[[Языки программирования/C#|C#]] / [[Языки программирования/C#/Базы данных|Базы данных]] / [[Языки программирования/C#/Базы данных/Руководство по MongoDB|Руководство по MongoDB]] / [[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB|Глава 1. C# и MongoDB]] / Фильтрация данных

[[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB/Получение документов из базы данных|Назад]] | [[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB|Содержание]] | [[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB/FilterDefinitionBuilder и определение фильтров|Вперёд]]

**Дата написания:** 05.09.2026

При выборке данных в методы Find()/FindAsync() передается объект BsonDocument, который устанавливает параметры фильтрации.

```csharp
// определяем фильтр - находим все документы, где Name = "Tom"
var filter = new BsonDocument { { "Name", "Tom" } };
List<BsonDocument> users = await collection.Find(filter).ToListAsync();
```

### Операторы выборки

-   $eq (равно), $ne (не равно)
-   $gt (больше чем), $lt (меньше чем)
-   $gte (больше или равно), $lte (меньше или равно)
-   $in, $nin

### Логические операторы

-   $or: соединяет два условия, и документ должен соответствовать одному из этих условий
-   $and: соединяет два условия, и документ должен соответствовать обоим условиям
-   $not: документ должен НЕ соответствовать условию
-   $nor: соединяет два условия, и документ должен НЕ соответстовать обоим условиям

### Проверка на отсутствие поля. Оператор $exists

```csharp
var filter = new BsonDocument("Languages", new BsonDocument { { "$exists", true } });
```

### Соответствие регулярному выражению. Оператор $regex

```csharp
var filter = new BsonDocument("Name", new BsonDocument { {"$regex", "m$" } });
```

**Источник:** [https://metanit.com/sharp/mongodb/1.8.php](https://metanit.com/sharp/mongodb/1.8.php)
