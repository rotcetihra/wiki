# Коллекции

[[Языки программирования/C#|C#]] / [[Языки программирования/C#/Базы данных|Базы данных]] / [[Языки программирования/C#/Базы данных/Руководство по MongoDB|Руководство по MongoDB]] / [[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB|Глава 1. C# и MongoDB]] / Коллекции

[[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB/Установка драйвера и подключение к серверу и базе данных|Назад]] | [[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB|Содержание]] | [[Языки программирования/C#/Базы данных/Руководство по MongoDB/Глава 1. C# и MongoDB/Документы и элементы|Вперёд]]

**Дата написания:** 05.09.2026

Данные в базе данных хранятся в коллекциях, которые представлены объектом IMongoCollection. Для работы с коллекциями тип IMongoDatabase предоставляет ряд методов:

-   CreateCollection()/CreateCollectionAsync(): создает новую коллекцию
-   RenameCollection()/RenameCollectionAsync(): переименовывает коллекцию
-   GetCollection(): возвращает коллекцию
-   DropCollection()/DropCollectionAsync(): удаляет коллекцию
-   ListCollections()/ListCollectionsAsync(): возвращает список всех коллекций
-   ListCollectionNames()/ListCollectionNamesAsync(): возвращает список имен всех коллекций

## Создание коллекции

```csharp
using MongoDB.Driver;

MongoClient client = new MongoClient("mongodb://localhost:27017");
var db = client.GetDatabase("test");
await db.CreateCollectionAsync("people");  // создаем коллекцию "people"
```

## Получение коллекции

Для взаимодействия с коллекцией нам необходимо получить саму эту коллекцию. Для этого необходимо вызвать метод GetCollection\<TDocument\>. Этот метод типизируется типом документов, которые хранит коллекция:

```csharp
using MongoDB.Bson;
using MongoDB.Driver;

MongoClient client = new MongoClient("mongodb://localhost:27017");
var db = client.GetDatabase("test");
// получаем коллекцию users 
IMongoCollection<BsonDocument> users = db.GetCollection<BsonDocument>("users");
```

## Удаление коллекции

```csharp
await db.DropCollectionAsync("users");
```

**Источник:** [https://metanit.com/sharp/mongodb/1.2.php](https://metanit.com/sharp/mongodb/1.2.php)
