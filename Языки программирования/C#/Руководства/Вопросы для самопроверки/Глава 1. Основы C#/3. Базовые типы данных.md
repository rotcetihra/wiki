# Базовые типы данных

[[Языки программирования/C#|C#]] / [[Языки программирования/C#/Руководства|Руководства]] / [[Языки программирования/C#/Руководства/Вопросы для самопроверки|Вопросы для самопроверки]] / [[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 1. Основы C#|Глава 1. Основы C#]] / Базовые типы данных

[[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 1. Основы C#/2. Литералы|Назад]] | [[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 1. Основы C#|Содержание]] | [[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 1. Основы C#/4. Консольный ввод-вывод|Вперёд]]

**Дата написания:** 05.09.2026

**Вопрос 1**

Какие из нижеперечисленных НЕ являются встроенными типами языка C#?

- uint
- sbyte
- real
- int128
- object
- float64

**Ответ**

Не являются встроенными типами языка C# следующие:

- real
- int128
- float64

---

**Вопрос 2**

Какой тип данных языка C# будет представлять следующая переменная?

```csharp
bool enabled = true;
```

**Ответ**

Переменная `enabled` представляет тип `bool`.

---

**Вопрос 3**

Какой тип данных языка C# будет представлять следующая переменная?

```csharp
var weight = 84.45f;
```

**Ответ**

Переменная `weight` представляет тип `float`.

---

**Вопрос 4**

Сколько байт занимает значение типа uint?

**Ответ**

Значение типа uint занимает 4 байта.

---

**Вопрос 5**

Какие из следующих вариантов представляют корректное определение переменных:

1. `string person = "Tom";`
2. `var person = "Tom";`
3. `var person;`
4. `string person;`

**Ответ**

Правильные варианты:

1. `string person = "Tom";`
2. `string person;`
3. `var person = "Tom";`

---

**Вопрос 6**

Какой системный тип соответствует базовому типу данных int в языке C# и сколько байт он занимает?

1. System.Int32, 4 байта
2. System.Single, 4 байта
3. System.UInt32, 8 байт
4. System.Int16, 2 байта

**Ответ**

System.Int32, 4 байта.

---

**Вопрос 7**

Какие суффиксы используются в C# для явного указания типа данных `float` и `decimal` при присвоении значений?

1. S/s — для float, D/d — для decimal
2. X/x — для float, Y/y — для decimal
3. F/f — для float, M/m — для decimal
4. L/l — для float, U/u — для decimal

**Ответ**

F/f — для float, M/m — для decimal.

---

**Вопрос 8**

Чем отличается объявление переменной с использованием `var` от явного указания типа данных, например, `int`?

1. `var` и `int` — это синонимы для объявления целочисленных переменных.
2. При использовании `var` тип переменной определяется автоматически на основе присвоенного значения.
3. `var` используется для объявления переменных с типом `string`.
4. `var` позволяет объявлять переменные без указания типа и инициализации.

**Ответ**

При использовании `var` тип переменной определяется автоматически на основе присвоенного значения.

**Источник:** [https://metanit.com/sharp/questions/1.10.php](https://metanit.com/sharp/questions/1.10.php)