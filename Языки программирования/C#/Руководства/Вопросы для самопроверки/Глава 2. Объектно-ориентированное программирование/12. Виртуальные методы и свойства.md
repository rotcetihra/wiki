# Виртуальные методы и свойства

[[Языки программирования/C#|C#]] / [[Языки программирования/C#/Руководства|Руководства]] / [[Языки программирования/C#/Руководства/Вопросы для самопроверки|Вопросы для самопроверки]] / [[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 2. Объектно-ориентированное программирование|Глава 2. Объектно-ориентированное программирование]] / Виртуальные методы и свойства

[[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 2. Объектно-ориентированное программирование/11. Перегрузка операций преобразования типов|Назад]] | [[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 2. Объектно-ориентированное программирование|Содержание]] | [[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 2. Объектно-ориентированное программирование/13. Абстрактные классы и члены классов|Вперёд]]

**Дата написания:** 05.09.2026

**Вопрос 1**

Какое ключевое слово применяется для переопределения виртуальных методов и свойств:

- overridable
- override
- overriding
- overriden

**Ответ**

override.

---

**Вопрос 2**

Что будет выведено на консоль в результате выполнения следующей программы и почему:

```csharp
class Person
{
    public string Name { get; set; }
    public virtual void Display()
    {
        Console.WriteLine($"Person {Name}");
    }
}
class Employee : Person
{
    public string Company { get; set; }
    public override void Display()
    {
        Console.WriteLine($"Employee {Name}");
    }
}
class Program
{
    static void Main(string[] args)
    {
        Person person = new Employee { Name = "Sam", Company = "Microsoft" };
        person.Display();
        
        Console.ReadKey();
    }
}
```

**Ответ**

На консоль будет выведена строка "Employee Sam". Потому что переменная person хранит ссылку на объект Employee, и при вызове метода Display будет использоваться именно реализация данного метода в классе Employee.

---

**Вопрос 3**

Что будет выведено на консоль в результате выполнения следующей программы и почему:

```csharp
class Person
{
    public string Name { get; set; }
    public virtual void Display()
    {
        Console.WriteLine($"Person {Name}");
    }
}
class Employee : Person
{
    public string Company { get; set; }
    public override void Display()
    {
        Console.WriteLine($"Employee {Name}");
    }
}
class Manager : Employee
{
    public override void Display()
    {
        Console.WriteLine($"Manager {Name}");
    }
}
class Program
{
    static void Main(string[] args)
    {
        Person person = new Manager { Name = "Bob", Company = "Microsoft" };
        Employee employee = (Employee)person;
        employee.Display();
        
        Console.ReadKey();
    }
}
```

**Ответ**

На консоль будет выведена строка "Manager Bob". Потому что переменная person хранит ссылку на объект Manager. При преобразовании к типу Employee ссылка по прежнему представляет объект типа Manager, и при вызове метода Display будет использоваться реализация метода из класса Manager.

---

**Вопрос 4**

Что будет выведено на консоль в результате выполнения следующей программы и почему:

```csharp
class Auto
{
    protected internal virtual void Move()
    {
        Console.WriteLine("Auto is moving");
    }
}
class Track : Auto
{
    public override void Move()
    {
        Console.WriteLine("Track is moving");
    }
}
class Program
{
    static void Main(string[] args)
    {
        Track track = new Track();
        Auto auto = track;
        auto.Move();
            
        Console.ReadKey();
    }
}
```

**Ответ**

Программа не скомпилируется, потому что переопределяемый метод в производном классе должен иметь тот же модификатор доступа, что и виртуальный метод в базовом классе.

---

**Вопрос 5**

Для чего нужен модификатор sealed?

**Ответ**

Модификатор sealed позволяет запретить наследование от класса и переопределение метода.

---

**Вопрос 6**

Что будет выведено на консоль в результате выполнения следующей программы и почему:

```csharp
class Auto
{
    public sealed void Display()
    {
        Console.WriteLine("Auto");
    }
}
class Truck : Auto
{
    public void DisplayInfo()
    {
        base.Display();
    }
}
class Program
{
    static void Main(string[] args)
    {
        Truck truck = new Truck();
        truck.DisplayInfo();
        Console.ReadKey();
    }
}
```

**Ответ**

Данная программа не скомпилируется, так как модификатор sealed применяется только в связке с оператором override.

**Источник:** [https://metanit.com/sharp/questions/2.11.php](https://metanit.com/sharp/questions/2.11.php)