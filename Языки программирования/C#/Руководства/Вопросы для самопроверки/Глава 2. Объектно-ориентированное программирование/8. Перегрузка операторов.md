# Перегрузка операторов

[[Языки программирования/C#|C#]] / [[Языки программирования/C#/Руководства|Руководства]] / [[Языки программирования/C#/Руководства/Вопросы для самопроверки|Вопросы для самопроверки]] / [[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 2. Объектно-ориентированное программирование|Глава 2. Объектно-ориентированное программирование]] / Перегрузка операторов

[[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 2. Объектно-ориентированное программирование/7. Константы и поля для чтения|Назад]] | [[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 2. Объектно-ориентированное программирование|Содержание]] | [[Языки программирования/C#/Руководства/Вопросы для самопроверки/Глава 2. Объектно-ориентированное программирование/9. Наследование|Вперёд]]

**Дата написания:** 05.09.2026

**Вопрос 1**

Оператор + (сложение) при применении к двум объектам типа int возвращает их сумму.

```csharp
int x = 9;
int y = 10;
int z = x + y;	// 19
```

Как можно переопределить оператор + для типа int, чтобы этот оператор возвращал не сумму, а произведение двух объектов int?

**Ответ**

Никак. Мы не можем переопределить или перегрузить операторы для типов, которые есть в .NET по умолчанию.

---

**Вопрос 2**

Есть класс Counter:

```csharp
class Counter
{
    public int Number { get; set; }
    
    // определение оператора сложения
}
```

В этом классе определен оператор сложения, который позволяет складывать объект Counter с объектом типа int следующим образом:

```csharp
Counter counter = new Counter { Number = 45 };
int x = counter + 6;
Console.WriteLine(x);  // 51
```

Какой именно оператор сложения определен в классе Counter? (правильных вариантов может быть несколько)

1. `public int operator + (Counter counter, int val) { return counter.Number + val; }`
2. `public static int operator + (Counter counter, int val) { return counter.Number + val; }`
3. `public static int operator + (int val, Counter counter) { return counter.Number + val; }`
4. `public static Counter operator + (Counter counter, int val) { return new Counter {Number = counter.Number + val}; }`
5. `public static Counter operator + (int val, Counter counter) { return new Counter {Number = counter.Number + val}; }`

**Ответ**

Правильный ответ:

```csharp
public static int operator + (Counter counter, int val)
{
    return counter.Number + val;
}
```

---

**Вопрос 3**

Почему не компилируется следующая программа:

```csharp
class Counter
{
    public int Number { get; set; }

    public static int operator + (int val, Counter counter)
    {
        return counter.Number + val;
    }
}
class Program
{
    static void Main(string[] args)
    {
        Counter counter = new Counter { Number = 45 };
        int x = counter + 6;
        Console.WriteLine(x);  // 51

        Console.ReadKey();
    }
}
```

**Ответ**

Первый параметр оператора сложения в классе Counter представляет тип int, а второй - тип Counter. Поэтому при применении операции сложения левый операнд должен представлять тип int, а правый - объект Counter:

```csharp
int x = 6 + counter;
// вместо
// int x = counter + 6;
```

**Источник:** [https://metanit.com/sharp/questions/2.15.php](https://metanit.com/sharp/questions/2.15.php)