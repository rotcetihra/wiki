# Указатели и массивы как параметры функции

[[Языки программирования/C/Глава 4. Функции|Глава 4. Функции]] / Указатели и массивы как параметры функции

[[Языки программирования/C/Глава 4. Функции/7. Классы хранения (Storage class)|Назад]] | [[Языки программирования/C/Глава 4. Функции|Содержание]] | [[Языки программирования/C/Глава 4. Функции/9. Указатели на функцию|Вперёд]]

**Дата написания:** 02.08.2026

## 1. Введение

При рассмотрении передачи параметров в функцию указывалось, что параметры передаются в функцию по значению: функция не изменяет значения передаваемых аргументов. Однако, используя в качестве параметров указатели, можно получить доступ к значению аргумента и изменить его.

## 2. Указатели как параметры

Пусть будет простейшая функция, которая увеличивает число на единицу:

```c
#include <stdio.h>

void increment(int x)
{
    x = x + 1;
    printf("increment function: %d \n", x);
}

int main(void)
{
    int n = 10;
    increment(n);
    printf("main function: %d \n", n);
    return 0;
}
```

Здесь переменная `n` передается в качестве аргумента для параметра `x`. Передача происходит по значению, поэтому любое изменение параметра `x` в функции `increment` никак не скажется на значении переменной `n`. Что мы можем увидеть, запустив программу:

```
increment function: 11
main function: 10
```

Теперь изменим функцию `increment`, использовав в качестве параметра указатель:

```c
#include <stdio.h>

void increment(int *x)
{
    *x = *x + 1;
    printf("increment function: %d \n", *x);
}

int main(void)
{
    int n = 10;
    increment(&n);
    printf("main function: %d \n", n);
    return 0;
}
```

Теперь в функции `increment` разыменовываем указатель, получаем значение по адресу и увеличиваем его на единицу:

```c
*x = *x + 1;
```

Это изменяет значение, которое находится по адресу, хранимому в указателе `x`.

Поскольку теперь функция в качестве параметра принимает указатель, при ее вызове необходимо передать адрес переменной: `increment(&n);`.

В итоге изменение через параметр `x` также повлияет на переменную `n`:

```
increment function: 11
main function: 11
```

## 3. Функция обмена значений

Еще один показательный пример применения указателей в параметрах — функция обмена значений:

```c
#include <stdio.h>

void swap(int *a, int *b)
{
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main(void)
{
    int x = 100;
    int y = 200;
    swap(&x, &y);
    printf("x=%d \t y=%d \n", x, y);
    return 0;
}
```

Функция `swap()` в качестве параметров принимает два указателя. Посредством переменной `temp` происходит обмен значениями.

При вызове функции `swap` в нее передаются адреса переменных `x` и `y`, и в итоге их значения будут изменены.

## 4. Константные параметры-указатели

Если необходимо запретить изменять значение параметра-указателя внутри функции, можно определить такой параметр как константный:

```c
#include <stdio.h>

// константный параметр
int twice(const int *x)
{
    // *x = *x * *x;     // так нельзя, так как x - константный параметр
    int y = *x + *x;
    return y;
}

int main(void)
{
    int n = 10;
    int m = twice(&n);
    printf("n = %d \n", n);     // n = 10
    printf("m = %d \n", m);     // m = 20
    return 0;
}
```

Фактически такой константный параметр работает как указатель на константу — мы не сможем изменить значение по нему внутри функции.

## 5. Массивы в параметрах

Если функция принимает в качестве параметра массив, то фактически в эту функцию передается только адрес начала массива. То есть, как и в случае с указателями, нам доступен адрес, по которому мы можем менять значения. Это отличается от параметров примитивных типов, которые передаются по значению.

Например, определим функцию для увеличения элементов массива в два раза:

```c
#include <stdio.h>

void twice(size_t n, int p[])
{
    for (size_t i = 0; i < n; i++)
    {
        p[i] = p[i] * 2;
    }
}

int main(void)
{
    int nums[] = {1, 2, 3, 4, 5};
    // получаем количество элементов массива
    size_t length = sizeof(nums) / sizeof(nums[0]);

    twice(length, nums);

    for (size_t i = 0; i < length; i++)
    {
        printf("%d \t", nums[i]);
    }

    return 0;
}
```

Функция `twice` в качестве параметров принимает массив и число его элементов и в цикле увеличивает их в два раза.

В функции `main` массив передается в функцию `twice`, и затем его элементы выводятся на консоль. В результате мы увидим, что массив `nums` был изменен:

```
2	4	6	8	10
```

Так как передача массива в функцию фактически представляет передачу адреса первого элемента, массивы в параметрах можно заменить указателями:

```c
#include <stdio.h>

void twice(size_t n, int *p)
{
    for (size_t i = 0; i < n; i++)
    {
        *p = *p * 2; // увеличиваем значение по указателю
        p = p + 1;   // перемещаем указатель на следующий элемент
    }
}

int main(void)
{
    int nums[] = {1, 2, 3, 4, 5};
    size_t length = sizeof(nums) / sizeof(nums[0]);

    twice(length, nums);

    for (size_t i = 0; i < length; i++)
    {
        printf("%d \t", nums[i]);
    }

    return 0;
}
```

В данном случае не будет большой разницы, какой тип имеет параметр — массив или указатель. Компилятор в любом случае будет рассматривать параметр типа `int array[]` как указатель `int *array`.

## 6. Минимальная длина массива-параметра

При определении параметра в принципе можно даже указать длину массива, которая будет представлять минимально допустимое количество элементов:

```c
#include <stdio.h>

// функция ожидает получить массив как минимум из 4 элементов
void twice(int numbers[4])
{
    for (size_t i = 0; i < 4; i++)
    {
        numbers[i] = numbers[i] * 2;
        printf("%d \t", numbers[i] * 2);
    }
}

int main(void)
{
    int nums[5] = {1, 2, 3, 4, 5};
    twice(nums);
    return 0;
}
```

Тем не менее если в функцию будет передан массив меньшей длины, то конкретное поведение зависит от компилятора. Например, GCC успешно скомпилирует приложение, хотя при компиляции выдаст предупреждение.

> [!WARNING]
> Размер массива в параметре — не проверка границ: если передать массив меньшей длины, функция обратится за пределы массива (неопределенное поведение). Компилятор лишь предупреждает, но не защищает.

Начиная со стандарта C99 Си позволяет установить минимальную длину массива с помощью оператора `static`:

```c
// ожидаем массив как минимум с 4 элементами
void twice(int numbers[static 4])
```

## 7. Константные массивы-параметры

Массив также можно передавать как константный — в этом случае значения его элементов нельзя изменить:

```c
#include <stdio.h>

// массив p - константный
void twice(size_t n, const int p[])
{
    for (size_t i = 0; i < n; i++)
    {
        // p[i] = p[i] * 2;  // так нельзя - массив константный
        printf("%d \t", p[i] * 2);
    }
}

int main(void)
{
    int nums[] = {1, 2, 3, 4, 5};
    // получаем количество элементов массива
    size_t length = sizeof(nums) / sizeof(nums[0]);
    twice(length, nums);
    return 0;
}
```

## 8. Резюме

1. Указатели в параметрах позволяют изменять значения аргументов в вызывающем коде: `void increment(int *x)` + вызов `increment(&n)`.
2. Классический пример — функция обмена значений `swap(int *a, int *b)`.
3. Константный параметр-указатель (`const int *x`) запрещает изменение значения по нему.
4. Массив в параметре — на самом деле указатель на первый элемент (`int p[]` эквивалентно `int *p`), поэтому элементы массива можно изменять.
5. `int numbers[4]` задает лишь минимально допустимую длину (с `static 4` с C99); `const int p[]` запрещает изменение элементов.

[[Языки программирования/C/Глава 4. Функции/7. Классы хранения (Storage class)|Назад]] | [[Языки программирования/C/Глава 4. Функции|Содержание]] | [[Языки программирования/C/Глава 4. Функции/9. Указатели на функцию|Вперёд]]
