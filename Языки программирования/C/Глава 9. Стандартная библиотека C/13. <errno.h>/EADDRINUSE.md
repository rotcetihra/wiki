# EADDRINUSE

[[Языки программирования/C/Глава 9. Стандартная библиотека C|Глава 9. Стандартная библиотека C]] / [[Языки программирования/C/Глава 9. Стандартная библиотека C/13. <errno.h>|13. <errno.h>]] / EADDRINUSE

[[Языки программирования/C/Глава 9. Стандартная библиотека C/13. <errno.h>/EACCES|Назад]] | [[Языки программирования/C/Глава 9. Стандартная библиотека C/13. <errno.h>|Содержание]] | [[Языки программирования/C/Глава 9. Стандартная библиотека C/13. <errno.h>/EADDRNOTAVAIL|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <errno.h>

#define EADDRINUSE 98 // "Address already in use"
```

## Описание

«Адрес уже занят». Возвращается сокетными вызовами, когда локальный адрес (или порт) уже используется другим сокетом. Типична для серверов: попытка `bind()` повторно занять порт без флага `SO_REUSEADDR`. Не входит в стандарт C; определён в POSIX (XSI).

## Когда возникает

- `bind()`: порт уже слушается другим процессом;
- `connect()`: невозможно назначить локальный адрес;
- `bind()` на UNIX-сокет: файл сокета уже существует.

## Похожие коды

- `EADDRNOTAVAIL` — адрес не назначен интерфейсу
- `EAFNOSUPPORT` — семейство адресов не поддерживается

## Источники

- POSIX.1-2024, раздел `errno.h`
- Linux: `asm-generic/errno.h`, man-страница `bind(2)`
