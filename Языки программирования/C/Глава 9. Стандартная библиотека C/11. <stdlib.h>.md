# <stdlib.h>

[[Языки программирования/C/Глава 9. Стандартная библиотека C|Глава 9. Стандартная библиотека C]] / <stdlib.h>

[[Языки программирования/C/Глава 9. Стандартная библиотека C/10. Проверка символов и ctype.h|Назад]] | [[Языки программирования/C/Глава 9. Стандартная библиотека C|Содержание]] | [[Языки программирования/C/Глава 10. Многопоточность/1. Создание потоков|Вперёд]]

**Дата написания:** 05.08.2026

## 1. Введение

Заголовочный файл `<stdlib.h>` является одним из основных заголовочных файлов стандартной библиотеки C. В нем определены функции для преобразования строк в числа, генерации случайных чисел, завершения программы, взаимодействия с окружением, поиска и сортировки, целочисленной математики, управления памятью и работы с многобайтовыми символами.

## 2. Преобразование строк в числа

### 2.1. Функции atof, atoi, atol, atoll

Функции `atof()`, `atoi()`, `atol()`, `atoll()` преобразуют строку в число с плавающей точкой или целое число:

```c
double atof(const char *str);
int atoi(const char *str);
long atol(const char *str);
long long atoll(const char *str);
```

Пример использования:

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    char* str1 = "234";
    char* str2 = "3.14";

    int num = atoi(str1);
    double d = atof(str2);

    printf("num: %d\n", num);   // num: 234
    printf("d: %.2f\n", d);     // d: 3.14

    return 0;
}
```

>[!WARNING]
>Главная проблема этих функций — невозможность отличить успешное преобразование строки `"0"` от неудачного, так как в обоих случаях возвращается 0.

### 2.2. Функции strtol, strtoll, strtoul, strtoull

Функции `strtol()`, `strtoll()`, `strtoul()`, `strtoull()` преобразуют строку в целое число (соответственно `long`, `long long`, `unsigned long`, `unsigned long long`) с указанием основания системы счисления и возможностью определить, на каком символе остановилось преобразование:

```c
long strtol(const char *str, char **endptr, int base);
long long strtoll(const char *str, char **endptr, int base);
unsigned long strtoul(const char *str, char **endptr, int base);
unsigned long long strtoull(const char *str, char **endptr, int base);
```

Пример с проверкой ошибок:

```c
#include <stdio.h>
#include <stdlib.h>
#include <errno.h>

int main(void)
{
    char* str = "123abc";
    char* endptr;
    long num;

    errno = 0;
    num = strtol(str, &endptr, 10);
    if (errno == ERANGE) {
        printf("Переполнение!\n");
    } else if (endptr == str) {
        printf("Цифры не найдены\n");
    } else if (*endptr != '\0') {
        printf("Преобразование остановилось на: %s\n", endptr);
    } else {
        printf("Преобразование полностью: %ld\n", num);
    }

    return 0;
}
```

Подробнее эти функции рассматриваются в [[Языки программирования/C/Глава 14. Дополнительные статьи/6. Преобразование строки в целое число|статье о преобразовании строки в целое число]] и в уроке [[Языки программирования/C/Глава 9. Стандартная библиотека C/6. Преобразование строк в числа и чисел в строки|«Преобразование строк в числа и чисел в строки»]].

### 2.3. Функции strtod, strtof, strtold

Функции `strtod()`, `strtof()`, `strtold()` преобразуют строку в число с плавающей точкой (соответственно `double`, `float`, `long double`):

```c
double strtod(const char *str, char **endptr);
float strtof(const char *str, char **endptr);
long double strtold(const char *str, char **endptr);
```

Пример:

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    char* str = "3.1415abc";
    char* endptr;

    double num = strtod(str, &endptr);
    printf("num: %.4f\n", num);       // num: 3.1415
    printf("endptr: %s\n", endptr);   // endptr: abc

    return 0;
}
```

## 3. Генерация случайных чисел. rand и srand

Функция `rand()` возвращает псевдослучайное целое число в диапазоне от 0 до `RAND_MAX`. Функция `srand()` инициализирует генератор псевдослучайных чисел начальным значением (seed):

```c
int rand(void);
void srand(unsigned int seed);
```

Пример:

```c
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
    srand(time(NULL)); // Инициализация генератора текущим временем

    for (int i = 0; i < 5; i++) {
        printf("%d ", rand() % 100); // Случайное число от 0 до 99
    }
    printf("\n");

    return 0;
}
```

>[!NOTE]
>Если не вызвать `srand()`, генератор каждый раз будет выдавать одну и ту же последовательность чисел, так как начальное значение по умолчанию равно 1. Константа `RAND_MAX` определяется в `<stdlib.h>` и обычно равна 32767.

## 4. Завершение программы. exit, _Exit, abort, atexit

### 4.1. exit

Функция `exit()` завершает выполнение программы, закрывает все открытые файлы и вызывает функции, зарегистрированные через `atexit()`:

```c
void exit(int status);
```

Значения статуса — константы `EXIT_SUCCESS` (обычно 0) и `EXIT_FAILURE` (обычно 1).

### 4.2. _Exit

Функция `_Exit()` завершает программу немедленно, не вызывая функции `atexit()` и не сбрасывая буферы файлов:

```c
void _Exit(int status);
```

### 4.3. abort

Функция `abort()` аварийно завершает программу без вызова функций `atexit()`. Программа завершается с ненулевым статусом:

```c
void abort(void);
```

### 4.4. atexit

Функция `atexit()` регистрирует функцию, которая будет вызвана при завершении программы через `exit()` или при возврате из `main()`:

```c
int atexit(void (*func)(void));
```

Функции вызываются в порядке, обратном порядку регистрации. Пример:

```c
#include <stdio.h>
#include <stdlib.h>

void cleanup() {
    printf("Очистка ресурсов\n");
}

int main(void)
{
    atexit(cleanup);
    printf("Программа работает\n");

    return 0;
}
```

Консольный вывод:

```
Программа работает
Очистка ресурсов
```

## 5. Окружение. getenv и system

### 5.1. getenv

Функция `getenv()` возвращает значение переменной окружения по ее имени или `NULL`, если переменная не найдена:

```c
char *getenv(const char *name);
```

Пример:

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    char* path = getenv("PATH");
    if (path != NULL) {
        printf("PATH: %s\n", path);
    }

    return 0;
}
```

### 5.2. system

Функция `system()` выполняет команду операционной системы:

```c
int system(const char *command);
```

Если в качестве аргумента передан `NULL`, функция просто проверяет доступность командного интерпретатора. Пример:

```c
#include <stdlib.h>

int main(void)
{
    system("ls -l"); // Вывод списка файлов в Linux

    return 0;
}
```

## 6. Поиск и сортировка. qsort и bsearch

### 6.1. qsort

Функция `qsort()` сортирует массив произвольного типа, используя функцию сравнения:

```c
void qsort(void *base, size_t num, size_t size,
           int (*compare)(const void *, const void *));
```

Параметры:

- `base` — указатель на начало массива;
- `num` — количество элементов;
- `size` — размер одного элемента в байтах;
- `compare` — функция сравнения, возвращающая отрицательное число, если первый элемент меньше второго, 0 при равенстве и положительное число, если первый элемент больше второго.

Пример:

```c
#include <stdio.h>
#include <stdlib.h>

int compare(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

int main(void)
{
    int numbers[] = {5, 2, 8, 1, 9, 3};
    size_t count = sizeof(numbers) / sizeof(numbers[0]);

    qsort(numbers, count, sizeof(int), compare);

    for (size_t i = 0; i < count; i++) {
        printf("%d ", numbers[i]); // 1 2 3 5 8 9
    }
    printf("\n");

    return 0;
}
```

### 6.2. bsearch

Функция `bsearch()` ищет элемент в отсортированном массиве с помощью бинарного поиска и возвращает указатель на найденный элемент или `NULL`:

```c
void *bsearch(const void *key, const void *base, size_t num, size_t size,
              int (*compare)(const void *, const void *));
```

Пример:

```c
#include <stdio.h>
#include <stdlib.h>

int compare(const void* a, const void* b) {
    return (*(int*)a - *(int*)b);
}

int main(void)
{
    int numbers[] = {1, 2, 3, 5, 8, 9};
    int key = 5;

    int* result = bsearch(&key, numbers, 6, sizeof(int), compare);
    if (result != NULL) {
        printf("Найдено: %d\n", *result); // Найдено: 5
    } else {
        printf("Не найдено\n");
    }

    return 0;
}
```

## 7. Целочисленная математика. abs, labs, llabs, div, ldiv, lldiv

### 7.1. Функции abs, labs, llabs

Функции `abs()`, `labs()`, `llabs()` возвращают абсолютное значение числа (соответственно `int`, `long`, `long long`):

```c
int abs(int n);
long labs(long n);
long long llabs(long long n);
```

Пример:

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    printf("%d\n", abs(-5));     // 5
    printf("%ld\n", labs(-123)); // 123

    return 0;
}
```

### 7.2. Функции div, ldiv, lldiv

Функции `div()`, `ldiv()`, `lldiv()` выполняют целочисленное деление и возвращают структуру с частным и остатком:

```c
div_t div(int numerator, int denominator);
ldiv_t ldiv(long numerator, long denominator);
lldiv_t lldiv(long long numerator, long long denominator);
```

Структуры `div_t`, `ldiv_t`, `lldiv_t` содержат поля `quot` (частное) и `rem` (остаток). Пример:

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    div_t result = div(17, 5);

    printf("Частное: %d\n", result.quot); // Частное: 3
    printf("Остаток: %d\n", result.rem);  // Остаток: 2

    return 0;
}
```

## 8. Управление памятью. malloc, calloc, realloc, free

В `<stdlib.h>` определены функции для динамического выделения и освобождения памяти: `malloc()`, `calloc()`, `realloc()`, `free()`, а также `aligned_alloc()` (C11). Функции подробно рассматриваются в [[Языки программирования/C/Глава 7. Динамическая память|главе 7]] и в уроке [[Языки программирования/C/Глава 9. Стандартная библиотека C/3. Работа с памятью|«Работа с памятью»]]. Краткий пример:

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int* arr = malloc(5 * sizeof(int)); // Выделение памяти под 5 целых
    if (arr == NULL) {
        return 1;
    }

    for (int i = 0; i < 5; i++) {
        arr[i] = i + 1;
    }
    printf("arr[0]: %d\n", arr[0]); // arr[0]: 1

    free(arr); // Освобождение памяти

    return 0;
}
```

## 9. Многобайтовые символы

В `<stdlib.h>` определены функции для работы с многобайтовыми символами:

- `mblen()` — возвращает длину многобайтового символа;
- `mbtowc()` — преобразует многобайтовый символ в широкий;
- `wctomb()` — преобразует широкий символ в многобайтовый;
- `mbstowcs()` — преобразует многобайтовую строку в широкую;
- `wcstombs()` — преобразует широкую строку в многобайтовую.

Прототипы:

```c
int mblen(const char *s, size_t n);
int mbtowc(wchar_t *restrict pwc, const char *restrict s, size_t n);
int wctomb(char *s, wchar_t wchar);
size_t mbstowcs(wchar_t *restrict pwcs, const char *restrict s, size_t n);
size_t wcstombs(char *restrict s, const wchar_t *restrict pwcs, size_t n);
```

Пример с `mbstowcs()`:

```c
#include <stdio.h>
#include <stdlib.h>
#include <wchar.h>

int main(void)
{
    char* str = "Привет";
    wchar_t wstr[10];

    size_t count = mbstowcs(wstr, str, 10);
    printf("Количество символов: %zu\n", count);
    wprintf(L"%ls\n", wstr);

    return 0;
}
```

## 10. Константы и типы

В `<stdlib.h>` определены:

- `NULL` — нулевой указатель;
- `EXIT_SUCCESS` и `EXIT_FAILURE` — статусы завершения программы для `exit()`;
- `RAND_MAX` — максимальное значение, возвращаемое `rand()`;
- `MB_CUR_MAX` — максимальная длина многобайтового символа в текущей локали;
- `size_t` — беззнаковый целочисленный тип для размеров (обычно также доступен через другие заголовочные файлы);
- `div_t`, `ldiv_t`, `lldiv_t` — структуры, возвращаемые функциями `div()`, `ldiv()`, `lldiv()`.

## 11. Резюме

1. Преобразование строк: `atof`, `atoi`, `atol`, `atoll` (без проверки ошибок), `strtol`, `strtoll`, `strtoul`, `strtoull`, `strtod`, `strtof`, `strtold` (с проверкой ошибок через `endptr` и `errno`).
2. Случайные числа: `rand()`, `srand()`.
3. Завершение программы: `exit()`, `_Exit()`, `abort()`, регистрация функций через `atexit()`.
4. Окружение: `getenv()`, `system()`.
5. Поиск и сортировка: `qsort()`, `bsearch()`.
6. Целочисленная математика: `abs`, `labs`, `llabs`, `div`, `ldiv`, `lldiv`.
7. Память: `malloc`, `calloc`, `realloc`, `free`, `aligned_alloc`.
8. Многобайтовые символы: `mblen`, `mbtowc`, `wctomb`, `mbstowcs`, `wcstombs`.

[[Языки программирования/C/Глава 9. Стандартная библиотека C/10. Проверка символов и ctype.h|Назад]] | [[Языки программирования/C/Глава 9. Стандартная библиотека C|Содержание]] | [[Языки программирования/C/Глава 10. Многопоточность/1. Создание потоков|Вперёд]]