# cacosh

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<complex.h>|<complex.h>]] / cacosh

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<complex.h>/cacos|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<complex.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<complex.h>/carg|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <complex.h>

double cacosh(double complex z);
float cacoshf(float complex z);
long double cacoshl(long double complex z);
```

## Описание

Функция `cacosh` вычисляет арккосинус гиперболический комплексного числа. Арккосинус гиперболический является обратной функцией к косинусу гиперболическому: если `w = cacosh(z)`, то `z = ccosh(w)`.

Результат представляет собой комплексное число, действительная часть которого неотрицательна. Мнимая часть может быть положительной или отрицательной.

Существуют три варианта функции: `cacoshf` для `float complex`, `cacosh` для `double complex` и `cacoshl` для `long double complex`.

## Примеры

```c
#include <stdio.h>
#include <complex.h>

int main(void)
{
    double complex z = 2.0 + 1.0 * I;
    double complex result = cacosh(z);
    printf("cacosh(%.1f + %.1fi) = %.4f + %.4fi\n",
           creal(z), cimag(z), creal(result), cimag(result));

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартная функция для арккосинуса гиперболического | Результат может иметь особые значения |
| Три варианта для разных точностей | Мнимая часть не ограничена диапазоном |
| Связь с `ccosh` и `csinh` | |

## Похожие определения

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<complex.h>/ccosh|ccosh]] — косинус гиперболический комплексного числа
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<complex.h>/casinh|casinh]] — арксинус гиперболический комплексного числа
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<complex.h>/catanh|catanh]] — арктангенс гиперболический комплексного числа

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.3.5.2
- GNU C Library, заголовочный файл `complex.h`