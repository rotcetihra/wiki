# towlower

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wctype.h>|<wctype.h>]] / towlower

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<wctype.h>/towctrans|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wctype.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wctype.h>/towupper|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <wctype.h>

wint_t towlower(wint_t wc);
```

## Описание

Преобразует широкий символ `wc` в нижний регистр. Широкая версия функции `tolower()` из `<ctype.h>`.

Для букв с двунаправленным регистром возвращает соответствующий символ другого регистра. Для символов, не имеющих регистра (цифры, пунктуация и т. д.), возвращает `wc` без изменений.

Аргумент `wc` не должен равняться `WEOF`.

## Примеры

```c
#include <stdio.h>
#include <wctype.h>

int main(void)
{
    wint_t chars[] = {L'A', L'Я', L'5', L' '};
    size_t n = sizeof(chars) / sizeof(chars[0]);

    for (size_t i = 0; i < n; i++)
        printf("towlower(L'%lc') = '%lc'\n", (wint_t)chars[i], towlower(chars[i]));

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартная и портируемая | Преобразует только один символ |
| Поддерживает символы за пределами ASCII | Не преобразует строки целиком |
| Эквивалентна `towctrans(wc, wctrans("tolower"))` | Результат зависит от локали |

## Похожие определения

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/tolower|tolower]] — то же для однобайтовых символов (`int`)
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wctype.h>/towupper|towupper]] — преобразование в верхний регистр
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wctype.h>/iswlower|iswlower]] — проверка на строчную букву

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.30.2.3.2
- GNU C Library, заголовочный файл `wctype.h`
