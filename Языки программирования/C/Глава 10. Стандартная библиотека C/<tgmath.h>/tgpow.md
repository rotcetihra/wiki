# tgpow

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<tgmath.h>|<tgmath.h>]] / tgpow

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<tgmath.h>/tgsqrt|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<tgmath.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<tgmath.h>/tghypot|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <tgmath.h>

#define tgpow(x, y) /* обобщённый макрос */
```

## Описание

Макрос `tgpow` — обобщённая версия возведения в степень. Автоматически выбирает функцию `powf`, `pow`, `powl`, `cpowf`, `cpow` или `cpowl` в зависимости от типа аргументов.

Вычисляет `x^y`. Для вещественных типов: `x` должен быть `≥ 0` (если `y` не целое); `0^0` даёт 1.

## Пример

```c
#include <stdio.h>
#include <tgmath.h>

int main(void)
{
    printf("2^10 = %.0f\n", tgpow(2.0, 10.0));

    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `x` | Основание. Для вещественных типов: `≥ 0` (если `y` дробное). |
| `y` | Показатель степени. |

## Возвращаемое значение

| Тип | Результат |
|---|---|
| Вещественный | `x^y` |
| Комплексный | `z^w` |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Автоматический выбор версии по типу аргументов | Отрицательное основание с дробным показателем — ошибка |
| Работает с вещественными и комплексными типами | Переполнение при больших значениях |

## Похожие макросы

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<tgmath.h>/tgsqrt|tgsqrt]] — квадратный корень (обобщённый)
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<tgmath.h>/tgcbrt|tgcbrt]] — кубический корень (обобщённый)
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<tgmath.h>/tghypot|tghypot]] — гипотенуза (обобщённая)
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/pow|pow]] — версия для `double`

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.22
- GNU C Library, заголовочный файл `tgmath.h`
