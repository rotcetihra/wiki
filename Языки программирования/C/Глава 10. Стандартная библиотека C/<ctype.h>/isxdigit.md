# isxdigit

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>|<ctype.h>]] / isxdigit

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/isupper_l|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/isxdigit_l|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <ctype.h>

int isxdigit(int c);
```

## Параметры

| Параметр | Описание |
|---|---|
| `c` | Код символа для проверки (приводится к `unsigned char`) |

## Возвращаемое значение

Возвращает ненулевое значение, если символ `c` является шестнадцатеричной цифрой; 0 — в противном случае.

## Что делает

Проверяет, является ли символ `c` шестнадцатеричной цифрой. В локали `C` верно следующее: `0`–`9`, `a`–`f`, `A`–`F`; не зависит от регистра.

Аргумент приводится к `unsigned char`: передача отрицательного значения, отличного от `EOF`, — неопределённое поведение.

## Примеры

```c
#include <stdio.h>
#include <ctype.h>

int main(void)
{
    unsigned char c1 = 'a';
    unsigned char c2 = 'F';

    printf("isxdigit(c1) = %d\n", isxdigit(c1));
    printf("isxdigit(c2) = %d\n", isxdigit(c2));

    return 0;
}
```

## Ошибки и errno

Функция не устанавливает `errno`. Если `c` не представим как `unsigned char` и не равен `EOF`, поведение не определено.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартная и переносимая | Работает с одним символом, а не со строкой |
| Зависит от локали, а не от языка программы | Проверяет только один байт, не многобайтовые символы |
| Может быть вызвана для любого символа таблицы локали | Результат зависит от текущей локали |

## Альтернативы

- **`isxdigit_l()`** — та же проверка в явно заданной локали (POSIX)

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/isdigit|isdigit]] — десятичная цифра
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/isxdigit_l|isxdigit_l]] — шестнадцатеричная цифра с явной локалью

## Источники

- ISO C89 (§4.3.1)
- [cppreference: isxdigit](https://en.cppreference.com/w/c/string/byte/isxdigit)
- Linux man-pages: `isxdigit(3)`
