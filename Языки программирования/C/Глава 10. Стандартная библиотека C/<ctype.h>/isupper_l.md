# isupper_l

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>|<ctype.h>]] / isupper_l

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/isupper|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/isxdigit|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <ctype.h> // POSIX.1-2008

int isupper_l(int c, locale_t locale);
```

## Параметры

| Параметр | Описание |
|---|---|
| `c` | Код символа для проверки (приводится к `unsigned char`) |
| `locale` | Локаль (тип `locale_t` из `<locale.h>`), по правилам которой выполняется классификация |

## Возвращаемое значение

Возвращает ненулевое значение, если символ `c` является заглавной буквой по правилам локали `locale`; 0 — в противном случае.

Проверка выполняется по таблице символов явно переданной локали, а не глобальной, что позволяет классифицировать символы без изменения состояния процесса — например, распознавать буквы кириллицы в `ru_RU.UTF-8`.

## Что делает

Проверяет, является ли символ `c` заглавной буквой. В локали `C` верно следующее: только `A`–`Z`; для символов с двунаправленным регистром выполняется `toupper(c) == c`.

Аргумент приводится к `unsigned char`: передача отрицательного значения, отличного от `EOF`, — неопределённое поведение.

## Примеры

```c
#define _XOPEN_SOURCE 700
#include <locale.h>
#include <stdio.h>
#include <ctype.h>

int main(void)
{
    locale_t ru = newlocale(LC_ALL_MASK, "ru_RU.UTF-8", (locale_t)0);

    printf("isupper_l('А', ru) = %d\n", isupper_l((unsigned char)'А', ru));
    printf("isupper_l('A', ru) = %d\n", isupper_l('A', ru));

    freelocale(ru);
    return 0;
}
```

## Ошибки и errno

Функция не устанавливает `errno`. Если `c` не представим как `unsigned char` и не равен `EOF`, поведение не определено.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Не зависит от глобальной локали процесса | Доступна только в POSIX-системах |
| Потокобезопасна при отдельных локалях | Требует создания и освобождения `locale_t` |

## Альтернативы

- **`isupper()`** — та же проверка в текущей глобальной локали (ISO C)

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/isupper|isupper]] — заглавная буква
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/islower_l|islower_l]] — строчная буква с явной локалью

## Источники

- POSIX.1-2008 (раздел 7.4)
- Linux man-pages: `isupper(3)` (раздел `isupper_l`)
