# lround

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>|<math.h>]] / lround

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/lrint|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/nearbyint|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <math.h>

long lround(double x);
long lroundf(float x);         // C99
long lroundl(long double x); // C99
```

## Описание

Функция округляет аргумент x до ближайшего целого числа типа long. Если значение находится ровно посередине между двумя целыми, округление происходит в сторону положительной бесконечности (away from zero).

## Примеры

```c
#include <math.h>
#include <stdio.h>

int main(void) {
    printf("lround(2.3) = %ld\n", lround(2.3)); // 2
    printf("lround(2.5) = %ld\n", lround(2.5)); // 3
    printf("lround(2.7) = %ld\n", lround(2.7)); // 3
    printf("lround(-2.5) = %ld\n", lround(-2.5)); // -3
    printf("lround(-2.3) = %ld\n", lround(-2.3)); // -2
    
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Округление до целого без учета направления | Результат всегда целое (теряется дробная часть) |
| Поддерживает все типы с плавающей точкой |  |

## Похожие определения

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/round|round]] — округление до double
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/llround|llround]] — округление до long long
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/lrint|lrint]] — округление с текущим направлением округления

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.12.9.6
- GNU C Library, заголовочный файл `math.h`