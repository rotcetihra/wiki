# trunc

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>|<math.h>]] / trunc

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/round|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/fdim|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <math.h>

double trunc(double x);
float truncf(float x);         // C99
long double truncl(long double x); // C99
```

## Описание

Функция отбрасывает дробную часть аргумента x, возвращая ближайшее целое число по модулю не большее |x| (округление к нулю). trunc(2.7) = 2.0, trunc(-2.7) = -2.0.

## Примеры

```c
#include <math.h>
#include <stdio.h>

int main(void) {
    printf("trunc(2.3) = %f\n", trunc(2.3)); // 2.0
    printf("trunc(2.9) = %f\n", trunc(2.9)); // 2.0
    printf("trunc(-2.3) = %f\n", trunc(-2.3)); // -2.0
    printf("trunc(-2.9) = %f\n", trunc(-2.9)); // -2.0
    printf("trunc(2.0) = %f\n", trunc(2.0)); // 2.0
    
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Быстрое отбрасывание дробной части | Результат всегда целое число (может терять дробную часть) |
| Поддерживает все типы с плавающей точкой |  |

## Похожие определения

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/ceil|ceil]] — округление вверх
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/floor|floor]] — округление вниз
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/round|round]] — округление до ближайшего целого

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.12.9.3
- GNU C Library, заголовочный файл `math.h`