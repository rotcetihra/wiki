# log2

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>|<math.h>]] / log2

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/log1p|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/logb|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <math.h>

double log2(double x);
float log2f(float x);            // C99
long double log2l(long double x); // C99
```

## Описание

Функция вычисляет логарифм по основанию 2 аргумента x. Определена для x > 0. Результат — это показатель степени, в которую нужно возвести 2, чтобы получить x. Полезна при работе с бинарными данными и вычислении количества бит.

## Примеры

```c
#include <math.h>
#include <stdio.h>

int main(void) {
    printf("log2(1) = %f\n", log2(1.0));      // 0.0
    printf("log2(2) = %f\n", log2(2.0));      // 1.0
    printf("log2(8) = %f\n", log2(8.0));      // 3.0
    printf("log2(1024) = %f\n", log2(1024.0)); // 10.0
    printf("log2(0.5) = %f\n", log2(0.5));    // -1.0

    // Определение количества бит для числа
    unsigned int n = 255;
    int bits = (int)ceil(log2(n + 1));
    printf("Для числа %u нужно %d бит\n", n, bits);

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Точное вычисление для степеней двойки | Не определён для x ≤ 0 (результат -NaN или доменная ошибка) |
| Поддерживает все типы с плавающей точкой | Для дробных аргументов возможны погрешности округления |

## Похожие определения

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/log|log]] — натуральный логарифм (по основанию e)
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/log10|log10]] — десятичный логарифм
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/exp2|exp2]] — экспонента 2^x (обратная функция)

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.12.6.9
- GNU C Library, заголовочный файл `math.h`
