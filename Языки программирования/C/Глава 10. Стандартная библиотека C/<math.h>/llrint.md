# llrint

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>|<math.h>]] / llrint

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/llround|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/lrint|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <math.h>

long long llrint(double x);
long long llrintf(float x);         // C99
long long llrintl(long double x); // C99
```

## Описание

Функция округляет аргумент x до ближайшего целого числа типа long long с использованием текущего направления округления ( установленного через fesetround ). Если результат не может быть представлен как long long, поведение не определено.

## Примеры

```c
#include <math.h>
#include <stdio.h>

int main(void) {
    printf("llrint(2.3) = %lld\n", llrint(2.3)); // 2
    printf("llrint(2.5) = %lld\n", llrint(2.5)); // 2 (приRoundTowardZero)
    printf("llrint(2.7) = %lld\n", llrint(2.7)); // 3
    printf("llrint(-2.3) = %lld\n", llrint(-2.3)); // -2
    
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Учитывает текущее направление округления | Поведение не определено при переполнении |
| Поддерживает все типы с плавающей точкой |  |

## Похожие определения

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/rint|rint]] — округление до double
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/lrint|lrint]] — округление до long
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<math.h>/llround|llround]] — округление без учета направления

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.12.9.5
- GNU C Library, заголовочный файл `math.h`