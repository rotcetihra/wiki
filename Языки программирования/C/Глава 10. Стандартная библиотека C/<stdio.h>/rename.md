# rename

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>|<stdio.h>]] / rename

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/remove|remove]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/tmpfile|tmpfile]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdio.h>

int rename(const char *old_name, const char *new_name);
```

## Описание

Функция `rename` переименовывает файл с именем `old_name` в `new_name`. Если файл с именем `new_name` уже существует, он заменяется (поведение не определено, если `new_name` открыт). Возвращает `0` при успехе.

## Пример

```c
#include <stdio.h>

int main(void)
{
    FILE *fp = fopen("old.txt", "w");
    fprintf(fp, "Данные\n");
    fclose(fp);

    if (rename("old.txt", "new.txt") == 0) {
        printf("Файл переименован\n");
    } else {
        perror("Ошибка переименования");
    }
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `old_name` | Текущее имя файла |
| `new_name` | Новое имя файла |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| `0` | Файл успешно переименован |
| Ненулевое | Произошла ошибка |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартная функция ISO C | Поведение не определено для открытых файлов `new_name` |
| Может перемещать файлы между каталогами | Платформо-зависимые ограничения |
| — | Не работает для каталогов на некоторых платформах |

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/remove|remove]] — удаление файла
- POSIX `renameat` — переименование с указанием каталога

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.21.4.2
