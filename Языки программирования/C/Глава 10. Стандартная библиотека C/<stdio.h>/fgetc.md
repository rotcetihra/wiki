# fgetc

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>|<stdio.h>]] / fgetc

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/vsscanf|vsscanf]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/fgets|fgets]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdio.h>

int fgetc(FILE *stream);
```

## Описание

Функция `fgetc` считывает один символ из потока `stream` и возвращает его как `unsigned char`, преобразованный в `int`, или `EOF` при ошибке/достижении конца файла.

## Пример

```c
#include <stdio.h>

int main(void)
{
    FILE *fp = fopen("test.txt", "r");
    if (fp == NULL) return 1;

    int ch;
    while ((ch = fgetc(fp)) != EOF) {
        putchar(ch);
    }

    fclose(fp);
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `stream` | Входной поток |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| `unsigned char` → `int` | Считанный символ |
| `EOF` | Ошибка или конец файла |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Простое чтение посимвольно | Медленно для больших объёмов |
| Стандартная функция ISO C | Каждый вызов — системный вызов (без буферизации) |

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/getc|getc]] — макрос, аналогичен `fgetc`
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/getchar|getchar]] — чтение из `stdin`
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/fgets|fgets]] — чтение строки

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.21.7.1
