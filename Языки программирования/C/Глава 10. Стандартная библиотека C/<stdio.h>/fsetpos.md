# fsetpos

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>|<stdio.h>]] / fsetpos

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/fseek|fseek]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/ftell|ftell]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdio.h>

int fsetpos(FILE *stream, const fpos_t *pos);
```

## Описание

Функция `fsetpos` устанавливает позицию в потоке `stream` в значение, сохранённое ранее через `fgetpos`. Все буферы сбрасываются.

## Пример

```c
#include <stdio.h>

int main(void)
{
    FILE *fp = fopen("test.txt", "w+");
    fprintf(fp, "АБВГД");

    fpos_t pos;
    fgetpos(fp, &pos);

    fprintf(fp, "ЕЖЗИЙ");

    fsetpos(fp, &pos);
    char buf[64];
    fgets(buf, sizeof(buf), fp);
    printf("С позиции: %s\n", buf);

    fclose(fp);
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `stream` | Поток |
| `pos` | Указатель на позицию, полученную через `fgetpos` |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| `0` | Позиция успешно установлена |
| Ненулевое | Произошла ошибка |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Точное восстановление позиции | Значение `fpos_t` неизвестно программисту |
| Портативно | Нельзя вычислить позицию самостоятельно |
| Стандартная функция ISO C | — |

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/fgetpos|fgetpos]] — сохранение позиции
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/fseek|fseek]] — установка позиции по смещению

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.21.9.3
