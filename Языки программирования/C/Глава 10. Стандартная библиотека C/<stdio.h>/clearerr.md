# clearerr

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>|<stdio.h>]] / clearerr

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/rewind|rewind]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/feof|feof]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdio.h>

void clearerr(FILE *stream);
```

## Описание

Функция `clearerr` сбрасывает индикаторы ошибки и конца файла для потока `stream`.

## Пример

```c
#include <stdio.h>

int main(void)
{
    FILE *fp = fopen("test.txt", "r");
    int ch;
    while ((ch = fgetc(fp)) != EOF)
        ;

    if (feof(fp)) {
        printf("Конец файла достигнут\n");
        clearerr(fp);
        printf("Флаги сброшены,feof: %d\n", feof(fp));
    }

    fclose(fp);
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `stream` | Поток |

## Возвращаемое значение

Функция ничего не возвращает.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Позволяет продолжить чтение после ошибки | Не устраняет причину ошибки |
| Стандартная функция ISO C | Для полного восстановления может потребоваться `fseek` |

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/feof|feof]] — проверка конца файла
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/ferror|ferror]] — проверка ошибки
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdio.h>/rewind|rewind]] — перемотка с сбросом ошибок

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.21.10.1
