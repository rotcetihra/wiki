# mtx_init

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<threads.h>|<threads.h>]] / mtx_init

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<threads.h>/thrd_yield|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<threads.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<threads.h>/mtx_lock|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <threads.h>

int mtx_init(mtx_t *mtx, int type);
```

## Описание

Функция `mtx_init` инициализирует мьютекс `mtx` с указанием типа:

| Тип | Описание |
|---|---|
| `mtx_plain` | Простой нерекурсивный мьютекс без тайм-аута |
| `mtx_recursive` | Рекурсивный мьютекс без тайм-аута |
| `mtx_timed` | Мьютекс с тайм-аутом, без рекурсии |

Мьютекс должен быть уничтожен через `mtx_destroy` после использования.

## Возвращаемое значение

| Значение | Описание |
|---|---|
| `thrd_success` | Мьютекс инициализирован |
| `thrd_error` | Ошибка (неверный тип) |

## Пример

```c
#include <threads.h>

int main(void)
{
    mtx_t m;
    mtx_init(&m, mtx_plain);
    /* использование мьютекса */
    mtx_destroy(&m);
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `mtx` | Указатель на мьютекс для инициализации |
| `type` | Тип мьютекса (`mtx_plain`, `mtx_recursive`, `mtx_timed`) |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Гибкий выбор типа мьютекса | Тип нельзя изменить после инициализации |

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<threads.h>/mtx_lock|mtx_lock]] — блокировка мьютекса
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<threads.h>/mtx_destroy|mtx_destroy]] — уничтожение мьютекса
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<threads.h>/MTX_INIT|MTX_INIT]] — статический инициализатор

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.26.5.1
