# PRIx64

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>|<inttypes.h>]] / PRIx64

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>/PRIx32|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>/PRIxMAX|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <inttypes.h>

#define PRIx64 /* строковый литерал, раскрываемый в спецификатор формата */
```

## Описание

Макрос `PRIx64` -- строковый литерал, используемый в строке формата функции `printf` (и родственных: `fprintf`, `snprintf`, `sprintf`). Заменяет собой спецификатор формата для типа `uint64_t` в формате **шестнадцатеричный**.

Макрос предназначен для переносимого форматирования целочисленных типов фиксированной ширины. На разных платформах один и тот же тип может объявляться как `long`, `long long` или другой тип, поэтому `PRIx64` подставляет корректный спецификатор автоматически.

Использование в строке формата:

```c
printf("%" PRIx64 "\n", value);
```

> [!NOTE]
> Макрос `PRIx64` не является самостоятельным строковым литералом и не должен использоваться отдельно. Он всегда подставляется внутрь строки формата.

## Пример

```c
#include <stdio.h>
#include <inttypes.h>

int main(void)
{
    uint64_t value = 42;
    printf("Значение: %" PRIx64 "\n", value);
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Гарантированно корректный спецификатор для `uint64_t` | Требует конкатенации строк в строке формата |
| Переносимость между платформами | Некоторые компиляторы могут определять макрос как пустую строку |
| Стандартный способ (ISO C99/C23) | -- |

## Похожие макросы

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>/SCNx64|SCNx64]] -- аналогичный макрос для `scanf`

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.8.1
- GNU C Library, заголовочный файл `inttypes.h`
