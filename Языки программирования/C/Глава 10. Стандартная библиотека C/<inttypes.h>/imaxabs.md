# imaxabs

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>|<inttypes.h>]] / imaxabs

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>/SCNxMAX|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>/imaxdiv|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <inttypes.h>

intmax_t imaxabs(intmax_t j);
```

## Описание

Функция `imaxabs` -- абсолютное значение `intmax_t`.

Функция `imaxabs` вычисляет модуль значения `j` типа `intmax_t`. Если результат не может быть представлен (например, `imaxabs(INTMAX_MIN)`), поведение не определено.

## Пример

```c
#include <stdio.h>
#include <inttypes.h>

int main(void)
{
    intmax_t value = imaxabs(-42);
    printf("Значение: %jd\n", value);
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `j` | Значение типа `intmax_t`, модуль которого вычисляется |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| `j` | Абсолютное значение `j` |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Работает с типом максимальной ширины | Результат при переполнении не определён |
| Стандартная функция ISO C99/C23 | -- |

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<inttypes.h>/imaxdiv|imaxdiv]] -- деление `intmax_t`
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdlib.h>/abs|abs]] -- модуль `int`

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.8.1
- GNU C Library, заголовочный файл `inttypes.h`
