# strtoll

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdlib.h>|<stdlib.h>]] / strtoll

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdlib.h>/strtold|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdlib.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdlib.h>/strtoul|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <stdlib.h>

long long strtoll(const char *restrict str, char **restrict endptr, int base);
```

## Параметры

| Параметр | Описание |
|---|---|
| `str` | Строка для преобразования |
| `endptr` | Указатель на место для записи указателя на первый нераспознанный символ (или `NULL`) |
| `base` | Основание системы счисления (2..36) или 0 для автоопределения |

## Возвращаемое значение

Преобразованное значение или 0, если ничего не распознано.

## Что делает

Аналог `strtol()` для типа `long long`. Правила разбора те же: пробелы, знак, префиксы `0x`/`0` при `base == 0`. При переполнении возвращает `LLONG_MAX`/`LLONG_MIN` и устанавливает `errno = ERANGE`. Доступна с C99.

## Примеры

```c
#include <errno.h>
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    const char *s = "-9223372036854775808";
    char *end;

    errno = 0;
    long long v = strtoll(s, &end, 10);
    if (errno == ERANGE) {
        printf("Переполнение\n");
    } else {
        printf("%lld; хвост: %s\n", v, end); // -9223372036854775808; хвост: ""
    }

    return 0;
}
```

## Ошибки и errno

Устанавливает `errno = ERANGE` при переполнении. Для полной проверки сбрасывайте `errno` перед вызовом.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| 64-битный диапазон | Нужно проверять `endptr` и `errno` |
| Обработка переполнения через `ERANGE` | Пустая строка и 0 неотличимы без `endptr` |

## Альтернативы

- **`strtol()`** — для `long`
- `strtoull()` — для `unsigned long long`

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdlib.h>/strtol|strtol]] — для `long`
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdlib.h>/strtoull|strtoull]] — для `unsigned long long`

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.22.1.4
- GNU C Library (man-страница `strtoll(3)`)