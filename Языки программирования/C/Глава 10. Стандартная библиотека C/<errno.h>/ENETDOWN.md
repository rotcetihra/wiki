# ENETDOWN

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<errno.h>|<errno.h>]] / ENETDOWN

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<errno.h>/ENAVAIL|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<errno.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<errno.h>/ENETRESET|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <errno.h>

#define ENETDOWN 100 // "Network is down"
```

## Описание

«Сеть не работает»: локальная сеть недоступна. POSIX (XSI). Возвращается сокетными операциями, когда сетевой интерфейс/подсистема не активна.

## Когда возникает

- `connect()`/`send()`/`sendto()` при выключенном интерфейсе или отсутствии сетевой подсистемы;
- интерфейс опущен (`ip link set down`).

## Похожие коды

- `ENETUNREACH` — сеть недостижима (нет маршрута)
- `EHOSTDOWN` — конкретный хост не работает
- `ENETRESET` — соединение сброшено сетью

## Источники

- POSIX.1-2024, раздел `errno.h`
- Linux: `asm-generic/errno.h`, man-страница `connect(2)`
