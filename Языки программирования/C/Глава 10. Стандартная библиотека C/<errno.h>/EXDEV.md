# EXDEV

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<errno.h>|<errno.h>]] / EXDEV

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<errno.h>/EWOULDBLOCK|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<errno.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<errno.h>/EXFULL|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <errno.h>

#define EXDEV 18 // "Cross-device link"
```

## Описание

«Связь между устройствами»: операция `rename()`/`link()`, требующая, чтобы источник и назначение находились на одной ФС, выполнена между разными устройствами. POSIX-код. Классическая реакция — копирование с последующим удалением.

## Когда возникает

- `rename()` между разными точками монтирования;
- `link()` жёсткой ссылки между ФС (для `symlink()` не применимо — она просто создаёт файл).

## Похожие коды

- `ENOENT` — источник не существует
- `EEXIST` — назначение существует (в части случаев)
- `EPERM` — нет прав

## Источники

- POSIX.1-2024, раздел `errno.h`
- Linux: `asm-generic/errno-base.h`, man-страница `rename(2)`
