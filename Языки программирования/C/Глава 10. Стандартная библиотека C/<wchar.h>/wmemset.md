# wmemset

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>|<wchar.h>]] / wmemset

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wmemmove|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wmemcmp|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <wchar.h>

wchar_t *wmemset(wchar_t *s, wchar_t c, size_t n);
```

## Описание

Функция `wmemset()` заполняет первые `n` элементов массива `s`宽оким символом `c`.

Возвращаемое значение — указатель на `s`.

Функция является широкосимвольным аналогом `memset()` из `<string.h>`.

## Примеры

```c
#include <stdio.h>
#include <wchar.h>
#include <locale.h>

int main(void)
{
    setlocale(LC_ALL, "");

    wchar_t buf[16];

    wmemset(buf, L'-', 10);
    buf[10] = L'\0';
    printf("Заполнение: %ls\n", buf);

    wmemset(buf, L'★', 5);
    buf[5] = L'\0';
    printf("Звёзды: %ls\n", buf);

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Быстрое заполнение массива одним символом | Работает с фиксированными массивами |
| Стандартная функция ISO C | Не добавляет нуль-терминатор |
| Простой интерфейс | Не оптимизирована для нулевого символа (в отличие от `memset`) |

## Похожие определения

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<string.h>/memset|memset]] — заполнение байтовым значением
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wmemcpy|wmemcpy]] — копирование массива
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wcsncpy|wcsncpy]] — заполнение нулями при копировании строк

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.29.4.2
- GNU C Library, заголовочный файл `wchar.h`
