# wcscpy

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>|<wchar.h>]] / wcscpy

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wcscoll|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wcscspn|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <wchar.h>

wchar_t *wcscpy(wchar_t *dest, const wchar_t *src);
```

## Описание

Функция `wcscpy()` копирует широкую строку `src` в буфер `dest`, включая завершающий нуль-терминатор. Буфер `dest` должен быть достаточно большим для размещения всей строки `src`.

Возвращаемое значение — указатель на `dest`.

Функция является широкосимвольным аналогом `strcpy()` из `<string.h>`. Поведение определено, только если `dest` и `src` не перекрываются.

> [!WARNING]
> Функция не проверяет размер буфера `dest`. Для безопасного копирования используйте `wcsncpy()`.

## Примеры

```c
#include <stdio.h>
#include <wchar.h>
#include <locale.h>

int main(void)
{
    setlocale(LC_ALL, "");

    const wchar_t *source = L"Привет, мир!";
    wchar_t dest[64];

    wcscpy(dest, source);
    printf("Скопировано: %ls\n", dest);

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Простое копирование всей строки | Нет проверки размера буфера |
| Стандартная функция ISO C | Не потокобезопасна |
| Возвращает указатель на начало результата | Возможное переполнение при длинной строке |

## Похожие определения

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wcsncpy|wcsncpy]] — копирование с ограничением длины
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<string.h>/strcpy|strcpy]] — копирование байтовых строк

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.29.4.2
- GNU C Library, заголовочный файл `wchar.h`
