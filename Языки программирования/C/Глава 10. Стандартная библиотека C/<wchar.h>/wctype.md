# wctype

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>|<wchar.h>]] / wctype

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wctrans|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wmemcpy|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <wchar.h>

wctype_t wctype(const char *property);
```

## Описание

Функция `wctype()` создаёт дескриптор категории классификации宽оких символов по строке `property`. Возвращает дескриптор типа `wctype_t` или `0` (некорректный дескриптор), если категория не распознана.

Предопределённые категории (строки `property`):

| Строка | Эквивалент |
|---|---|
| `"alnum"` | `iswalnum` |
| `"alpha"` | `iswalpha` |
| `"blank"` | `iswblank` |
| `"cntrl"` | `iswcntrl` |
| `"digit"` | `iswdigit` |
| `"graph"` | `iswgraph` |
| `"lower"` | `iswlower` |
| `"print"` | `iswprint` |
| `"punct"` | `iswpunct` |
| `"space"` | `iswspace` |
| `"upper"` | `iswupper` |
| `"xdigit"` | `iswxdigit` |

Дополнительные категории могут быть определены текущей локалью.

> [!NOTE]
> `wctype_t` — неполный тип. Дескриптор используется в функции `iswctype()` (не объявлена в `<wchar.h>`, но доступна в POSIX).

## Примеры

```c
#include <stdio.h>
#include <wchar.h>
#include <locale.h>

int main(void)
{
    setlocale(LC_ALL, "");

    wctype_t alpha = wctype("alpha");
    wctype_t digit = wctype("digit");

    if (alpha && digit) {
        printf("'A' — alpha: %d\n", iswctype(L'A', alpha));
        printf("'5' — digit: %d\n", iswctype(L'5', digit));
    }

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Позволяет выбирать категорию классификации через дескриптор | Требует предварительного вызова `wctype()` |
| Стандартная функция ISO C | 12 предопределённых категорий |
| Расширяема через локали | Неинформативна при ошибке (возвращает 0) |

## Похожие определения

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wchar.h>/wctrans|wctrans]] — создание дескриптора преобразования
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<wctype.h>/iswalnum|iswalnum]] и др. — прямые функции классификации
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<ctype.h>/ctype|ctype]] — аналогичный механизм для `int`

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.30.2
- GNU C Library, заголовочный файл `wchar.h`
