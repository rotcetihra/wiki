# strcasecmp_l

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<strings.h>|<strings.h>]] / strcasecmp_l

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<strings.h>/strcasecmp|Назад]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<strings.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<strings.h>/strncasecmp|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#define _POSIX_C_SOURCE 200809L
#include <strings.h>

int strcasecmp_l(const char *s1, const char *s2, locale_t loc);
```

## Параметры

| Параметр | Описание |
|---|---|
| `s1`, `s2` | Сравниваемые строки |
| `loc` | Локаль, правила которой используются для сравнения (тип `locale_t` из `<locale.h>`) |

## Возвращаемое значение

Отрицательное число, 0 или положительное число — результат сравнения строк без учёта регистра по правилам локали `loc`.

## Что делает

Аналог `strcasecmp()`, но локаль задаётся явно через `locale_t` и не зависит от глобальной локали процесса: не нужно вызывать `setlocale()`, функция пригодна для многопоточных программ. Требует POSIX.1-2008 (в glibc — определение `_POSIX_C_SOURCE` или `_GNU_SOURCE`).

## Примеры

```c
#include <locale.h>
#include <stdio.h>
#include <strings.h>

int main(void)
{
    locale_t ru = newlocale(LC_ALL_MASK, "ru_RU.UTF-8", NULL);

    printf("%d\n", strcasecmp_l("Ёлка", "ёлка", ru)); // 0 — равны

    freelocale(ru);
    return 0;
}
```

## Ошибки и errno

Функция не устанавливает `errno`. Некорректный `loc` — неопределённое поведение.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Не зависит от глобальной локали (потокобезопасна) | Менее известна, чем `strcasecmp()` |
| Работает с локалями, созданными через `newlocale()` | Требует POSIX.1-2008 |

## Альтернативы

- **[[Языки программирования/C/Глава 10. Стандартная библиотека C/<strings.h>/strcasecmp|strcasecmp()]]** — если достаточно текущей локали
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<strings.h>/strncasecmp_l|strncasecmp_l()]] — с ограничением длины

## Похожие функции

- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<strings.h>/strcasecmp|strcasecmp]] — сравнение в текущей локали
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<strings.h>/strncasecmp_l|strncasecmp_l]] — с ограничением длины
- [[Языки программирования/C/Глава 10. Стандартная библиотека C/<string.h>/strcoll_l|strcoll_l]] — сравнение с учётом регистра и явной локалью

## Источники

- GNU C Library (man-страница `strcasecmp(3)`)
- POSIX.1-2008 (расширенный интерфейс локали)
