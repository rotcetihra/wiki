# WCHAR_MAX

[[Языки программирования/C/Глава 10. Стандартная библиотека C|Глава 10. Стандартная библиотека C]] / [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdint.h>|<stdint.h>]] / WCHAR_MAX

[[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdint.h>/WCHAR_MIN|WCHAR_MIN]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdint.h>|Содержание]] | [[Языки программирования/C/Глава 10. Стандартная библиотека C/<stdint.h>/WINT_MIN|WINT_MIN]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdint.h>

#define WCHAR_MAX /* целочисленная константа */
```

## Описание

Макрос `WCHAR_MAX` — максимальное значение типа `wchar_t`. Представляет собой целочисленную константу, вычисляемую на этапе компиляции.

Значение определяется реализацией в соответствии со стандартом C23 (ISO/IEC 9899:2024).

## Пример

```c
#include <stdio.h>
#include <stdint.h>

int main(void)
{
    printf("WCHAR_MAX = %jd\n", (long long)WCHAR_MAX);
    printf("Тип: wchar_t\n");
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Вычисляется на этапе компиляции | Значение зависит от платформы |
| Гарантирует корректность для типа | Нельзя использовать в `#if` |
| Стандартная часть C99/C23 | — |

## Похожие макросы

- Все макросы `*_MIN` и `*_MAX` из `<stdint.h>`
- Макросы `<limits.h>` для стандартных целочисленных типов

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.20
- GNU C Library, заголовочный файл `stdint.h`
