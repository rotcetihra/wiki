# Отложенное освобождение памяти

[[Языки программирования/C/Организация программ/Глава 2. Управление памятью|Глава 2. Управление памятью]] / Отложенное освобождение памяти

[[Языки программирования/C/Организация программ/Глава 2. Управление памятью/3. Allocation Wrapper|Назад]] | [[Языки программирования/C/Организация программ/Глава 2. Управление памятью|Содержание]]

**Дата написания:** 30.08.2026

## Описание

Освобождение памяти, даже будучи выполненным, может быть чревато ошибками. Например, если освободить память раньше, чем нужно, можно получить **висячий указатель** (dangling pointer). Либо можно по ошибке освободить одну и ту же память дважды, что приведёт к неопределённому поведению.

Поэтому если программа использует динамическую память для хранения данных, но при этом память выделяется не часто, а сама программа выполняется довольно быстро, можно **переложить на операционную систему** обязанность по освобождению памяти. После завершения программы ОС сама освободит всю связанную с программой память.

> [!WARNING]
> Данное обстоятельство не стоит использовать в качестве оправдания вообще не освобождать память. Диапазон возможных ситуаций, где подобное можно применять, довольно ограничен. Если программа часто выделяет память или работает продолжительное время, очевидно, что данный подход не подойдёт. Кроме того, программа может эволюционировать, и ситуация может измениться.

## Пример: чтение файла

Рассмотрим программу, которая считывает текстовый файл и выводит его на консоль:

```c
#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

int getFileLength(FILE *fp)
{
    fseek(fp, 0, SEEK_END);
    int file_length = ftell(fp);
    return file_length;
}

void readFile(FILE *fp, char *buffer, int file_length)
{
    fseek(fp, 0, SEEK_SET);
    int read_elements = fread(buffer, 1, file_length, fp);
    buffer[read_elements] = '\0';
}

int main(void)
{
    char *filename = "test.txt";
    FILE *fp = fopen(filename, "r");
    assert(fp && "Unable to open file!\n");

    int file_length = getFileLength(fp);
    char *text = malloc(file_length);
    if (text)
    {
        readFile(fp, text, file_length);
        printf(text);
    }
    fclose(fp);
}
```

Функция `getFileLength` получает длину файла, а функция `readFile` считывает файл в строку. В функции `main` сначала считываем размер файла и затем используем этот размер для выделения буфера. Если выделение памяти прошло успешно, считываем в буфер текстовый файл.

После считывания файла и вывода считанного текста на консоль буфер **явным образом не освобождается**. По большому счёту в этом нет большого смысла, поскольку после вывода строки фактически идёт закрытие файла и конец программы. Соответственно, после этого операционная система сама очистит память приложения.

## Когда применим подход

| Условие | Применимость |
|---|---|
| Программа выделяет память один раз | Да |
| Программа работает короткое время | Да |
| Программа часто выделяет/освобождает память | Нет |
| Программа работает продолжительно | Нет |
| Программа может эволюционировать | Рискованно |

> [!NOTE]
> Отложенное освобождение памяти — это **оптимизация**, а не замена ручному управлению памятью. Используйте её осознанно, когда стоимость освобождения памяти превышает выгоду, и вы уверены, что память не будет использоваться повторно.
