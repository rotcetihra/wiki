# <stdlib.h>

[[Языки программирования/C/Глава 10. POSIX|Глава 10. POSIX]] / <stdlib.h>

[[Языки программирования/C/Глава 9. Стандартная библиотека C/14. <stddef.h>|Назад]] | [[Языки программирования/C/Глава 10. POSIX|Содержание]] | [[Языки программирования/C/Глава 10. POSIX/<string.h>|Вперёд]]

**Дата написания:** 18.08.2026

## Оглавление

### Управление памятью

- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/posix_memalign|posix_memalign]] — выделение с выравниванием (POSIX)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/reallocarray|reallocarray]] — изменение размера с проверкой переполнения (BSD/GNU)

### Окружение

- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/setenv|setenv]] — установка и удаление переменной с копированием (POSIX)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/putenv|putenv]] — установка строки без копирования (XSI)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/clearenv|clearenv]] — полная очистка окружения (GNU)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/secure_getenv|secure_getenv]] — чтение с блокировкой для setuid-программ (GNU)

### Случайные числа

- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/random|random]] — генератор с управляемым состоянием (XSI)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/drand48|drand48]] — семейство 48-битных генераторов (XSI)

### Сортировка

- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/qsort_r|qsort_r]] — сортировка с контекстом (GNU)

### Временные файлы и каталоги

- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/mkstemp|mkstemp]] — безопасное создание временного файла (POSIX/GNU)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/mkdtemp|mkdtemp]] — создание временного каталога (POSIX/BSD)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/mktemp|mktemp]] — устаревшее имя без создания (LEGACY)

### Псевдо-терминалы

- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/posix_openpt|posix_openpt]] — открытие master-устройства (XSI)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/getpt|getpt]] — то же без флагов (GNU)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/grantpt|grantpt]] — права slave-устройства (XSI)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/unlockpt|unlockpt]] — разблокировка slave (XSI)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/ptsname|ptsname]] — имя slave-устройства (XSI; `ptsname_r` — GNU)

### Преобразование чисел с явной локалью

- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/strtod_l|strtod_l]] — вещественные числа по правилам локали (GNU)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/strtol_l|strtol_l]] — целые числа по правилам локали (GNU)

### Прочие утилиты

- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/getsubopt|getsubopt]] — разбор подопций с значениями (XSI)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/a64l|a64l]] — компактное кодирование чисел (XSI, устаревшая)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/ecvt|ecvt]] — форматирование чисел в цифры (SVID, устаревшая)
- [[Языки программирования/C/Глава 10. POSIX/<stdlib.h>/getloadavg|getloadavg]] — средняя загрузка системы (BSD/GNU)

## Описание библиотеки

Заголовочный файл `<stdlib.h>` в POSIX-системах расширяет стандартную библиотеку C дополнительными функциями: управление памятью (`posix_memalign()`, `reallocarray()`), окружением (`setenv()`, `putenv()`, `unsetenv()`), генераторы случайных чисел (`random()`, семейство `drand48()`), создание временных файлов и каталогов (`mkstemp()`, `mkdtemp()`), работа с псевдо-терминалами (`posix_openpt()`, `grantpt()`, `unlockpt()`, `ptsname()`) и варианты функций преобразования строк с явной локалью (`strtod_l()`, `strtol_l()` и т. п.).

Все они объявлены в том же файле `<stdlib.h>` и доступны в glibc при определении соответствующих макросов тестирования возможностей: `_POSIX_C_SOURCE`, `_XOPEN_SOURCE`, `_DEFAULT_SOURCE`, `_GNU_SOURCE`. Часть функций отмечена как устаревшие (LEGACY): `mktemp()`, `ecvt()`, `a64l()` — для новых программ они не рекомендуются.

Стандартные функции ISO C из `<stdlib.h>` описаны в статье [[Языки программирования/C/Глава 9. Стандартная библиотека C/11. <stdlib.h>|11. <stdlib.h>]] главы 9.

### Управление памятью

| Функция | Описание |
|---|---|
| `posix_memalign()` | Выделяет блок с выравниванием (степень двойки); код ошибки в возвращаемом значении |
| `reallocarray()` | `realloc()` с проверкой переполнения `nmemb × size` (BSD, glibc 2.26+) |

### Окружение

| Функция | Описание |
|---|---|
| `setenv()` / `unsetenv()` | Установка/замена/удаление переменной; строки копируются |
| `putenv()` | Установка строки без копирования — следите за сроком жизни |
| `clearenv()` | Очистка всего окружения (GNU) |
| `secure_getenv()` | `getenv()`, возвращающий `NULL` в setuid-режиме (GNU) |

### Случайные числа

| Функция | Описание |
|---|---|
| `random()` / `srandom()` | Генератор с 31-битным выходом; состояние управляемо |
| `initstate()` / `setstate()` | Установка состояния генератора |
| `drand48()` и семейство | 48-битные генераторы: `drand48`, `erand48`, `lrand48`, `nrand48`, `mrand48`, `jrand48`, `srand48`, `seed48`, `lcong48` |

### Сортировка

| Функция | Описание |
|---|---|
| `qsort_r()` | `qsort()` с передачей контекста в функцию сравнения (GNU; сигнатура отличается от BSD) |

### Временные файлы и каталоги

| Функция | Описание |
|---|---|
| `mkstemp()` | Создаёт файл атомарно (`O_EXCL`), права 0600; варианты `mkstemps()`, `mkostemp()`, `mkostemps()` |
| `mkdtemp()` | Создаёт каталог с правами 0700 |
| `mktemp()` | Только имя — устаревшая, небезопасная (LEGACY) |

### Псевдо-терминалы

| Функция | Описание |
|---|---|
| `posix_openpt()` | Открывает master; `getpt()` — GNU-сокращение |
| `grantpt()` | Права доступа на slave |
| `unlockpt()` | Разблокирует slave для открытия |
| `ptsname()` / `ptsname_r()` | Имя slave-устройства; `_r` — потокобезопасная (GNU) |

### Преобразование чисел с явной локалью

| Функция | Описание |
|---|---|
| `strtod_l()` / `strtof_l()` / `strtold_l()` | Вещественные числа по правилам заданной локали |
| `strtol_l()` / `strtoll_l()` / `strtoul_l()` / `strtoull_l()` | Целые числа по правилам заданной локали |

### Прочие утилиты

| Функция | Описание |
|---|---|
| `getsubopt()` | Разбор подопций вида `имя=значение` через запятую (XSI) |
| `a64l()` / `l64a()` | Кодирование 36 бит в 6 символов (XSI, устаревшая) |
| `ecvt()` / `fcvt()` / `gcvt()` | Цифры числа без точки и знака (SVID, устаревшие) |
| `getloadavg()` | Средняя загрузка за 1/5/15 минут (BSD/GNU) |

[[Языки программирования/C/Глава 9. Стандартная библиотека C/14. <stddef.h>|Назад]] | [[Языки программирования/C/Глава 10. POSIX|Содержание]] | [[Языки программирования/C/Глава 10. POSIX/<string.h>|Вперёд]]