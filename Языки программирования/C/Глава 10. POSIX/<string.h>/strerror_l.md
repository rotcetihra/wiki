# strerror_l

[[Языки программирования/C/Глава 10. POSIX|Глава 10. POSIX]] / [[Языки программирования/C/Глава 10. POSIX/<string.h>|<string.h>]] / strerror_l

[[Языки программирования/C/Глава 10. POSIX/<string.h>/strerrordesc_np|Назад]] | [[Языки программирования/C/Глава 10. POSIX/<string.h>|Содержание]] | [[Языки программирования/C/Глава 10. POSIX/<string.h>/strerror_r|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <string.h> // POSIX.1-2008

char *strerror_l(int errnum, locale_t locale);
```

## Параметры

| Параметр | Описание |
|---|---|
| `errnum` | Код ошибки (обычно `errno`) |
| `locale` | Локаль, в которой переводить описание (тип `locale_t`) |

## Возвращаемое значение

Возвращает указатель на строку с описанием ошибки в нужной локали. Строка статическая — не изменяйте её.

## Что делает

Вариант [[Языки программирования/C/Глава 9. Стандартная библиотека C/12. <string.h>/strerror|strerror()]], который переводит описание в заданную локаль явно, а не в текущую глобальную. Удобно, когда программа выводит сообщения на языке пользователя, отличном от языка системы.

## Примеры

### Перевод описания в нужную локаль

```c
#include <stdio.h>
#include <string.h>
#include <locale.h>

int main(void)
{
    locale_t ru = newlocale(LC_MESSAGES_MASK, "ru_RU.UTF-8", NULL);
    if (ru != NULL) {
        printf("%s\n", strerror_l(ENOENT, ru)); // «Нет такого файла...» (если локализация доступна)
        freelocale(ru);
    }

    return 0;
}
```

## Ошибки и errno

Если `locale` невалидна — поведение не определено. Значение `errno` не изменяется (функция лишь переводит код в текст).

>[!NOTE]
>Наличие перевода зависит от установленных языковых пакетов системы: даже при запросе `ru_RU.UTF-8` описание может остаться английским, если локализация не установлена.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Явное управление языком | Статический буфер — не для хранения |
| Независима от глобальной локали | Только POSIX.1-2008 |
| | Нужно управлять локалью |

## Альтернативы

- **[[Языки программирования/C/Глава 9. Стандартная библиотека C/12. <string.h>/strerror|strerror()]]** — использует текущую (глобальную) локаль
- **[[Языки программирования/C/Глава 10. POSIX/<string.h>/strerror_r|strerror_r()]]** — потокобезопасный вариант
- **[[Языки программирования/C/Глава 10. POSIX/<string.h>/strerror_s|strerror_s()]]** — безопасный вариант (C11 Annex K)

## Похожие функции

- [[Языки программирования/C/Глава 9. Стандартная библиотека C/12. <string.h>/strerror|strerror]] — описание в глобальной локали
- [[Языки программирования/C/Глава 10. POSIX/<string.h>/strerror_r|strerror_r]] — потокобезопасный вариант
- [[Языки программирования/C/Глава 10. POSIX/<string.h>/strcoll_l|strcoll_l]] — сравнение строк с явной локалью

## Источники

- POSIX.1-2008 (§13)
- Linux man-pages: `strerror_l(3)`