# thrd_detach

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<threads.h>|<threads.h>]] / thrd_detach

[[Языки программирования/C/Библиотеки/<threads.h>/thrd_current|Назад]] | [[Языки программирования/C/Библиотеки/<threads.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<threads.h>/thrd_equal|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <threads.h>

int thrd_detach(thrd_t thr);
```

## Описание

Функция `thrd_detach` отсоединяет поток `thr`. Ресурсы потока автоматически освобождаются при его завершении. После отсоединения нельзя вызывать `thrd_join` для этого потока.

## Возвращаемое значение

| Значение | Описание |
|---|---|
| `thrd_success` | Поток отсоединён |
| `thrd_error` | Ошибка |

## Пример

```c
#include <stdio.h>
#include <threads.h>

int func(void *arg)
{
    printf("Фоновый поток\n");
    return 0;
}

int main(void)
{
    thrd_t t;
    thrd_create(&t, func, NULL);
    thrd_detach(t);
    /* Нельзя вызвать thrd_join(t, ...) */
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `thr` | Идентификатор потока для отсоединения |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Освобождает ресурсы автоматически | Нельзя получить результат потока |
| Поток работает в фоновом режиме | Нельзя отменить отсоединение |

## Обработка ошибок

- **Возвращаемое значение при ошибке:** `thrd_error` — ошибка отсоединения.
- **Установление errno:** функция не устанавливает `errno` (стандарт C23 не определяет использование `errno` для `<threads.h>`).
- **Поведение при передаче NULL:** идентификатор `thr` передаётся по значению (не указатель), поэтому NULL не применимо. Однако передача недействительного идентификатора — UB.
- **Многопоточность:** после отсоединения ресурсы потока автоматически освобождаются при его завершении. Нельзя вызывать `thrd_join` для отсоединённого потока.
- **Связанные функции:** `thrd_create` — создание потока; `thrd_join` — ожидание завершения потока.

## Похожие функции

- [[Языки программирования/C/Библиотеки/<threads.h>/thrd_create|thrd_create]] — создание потока
- [[Языки программирования/C/Библиотеки/<threads.h>/thrd_join|thrd_join]] — ожидание завершения потока

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.26.4.3
