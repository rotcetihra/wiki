# tgfmax

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<tgmath.h>|<tgmath.h>]] / tgfmax

[[Языки программирования/C/Библиотеки/<tgmath.h>/tgfdim|Назад]] | [[Языки программирования/C/Библиотеки/<tgmath.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<tgmath.h>/tgfmin|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <tgmath.h>

#define tgfmax(x, y) /* обобщённый макрос */
```

## Описание

Макрос `tgfmax` — обобщённая версия максимума из двух значений. Автоматически выбирает `fmaxf`, `fmax` или `fmaxl`.

Возвращает большее из двух значений. Если одно из значений `NaN`, возвращается другое значение. Только для вещественных типов.

## Пример

```c
#include <stdio.h>
#include <tgmath.h>

int main(void)
{
    printf("fmax(3, 7) = %.1f\n", tgfmax(3.0, 7.0));

    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `x` | Первый аргумент. Конечное значение. |
| `y` | Второй аргумент. Конечное значение. |

## Возвращаемое значение

| Тип | Результат |
|---|---|
| `float` | `max(x, y)` |
| `double` | `max(x, y)` |
| `long double` | `max(x, y)` |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Автоматический выбор версии по типу аргументов | Только для вещественных типов |
| Корректно обрабатывает `NaN` | — |

## Похожие макросы

- [[Языки программирования/C/Библиотеки/<tgmath.h>/tgfmin|tgfmin]] — минимум из двух (обобщённый)
- [[Языки программирования/C/Библиотеки/<tgmath.h>/tgfdim|tgfdim]] — положительная разность (обобщённая)
- [[Языки программирования/C/Библиотеки/<math.h>/fmax|fmax]] — версия для `double`

## Обработка ошибок

- Если оба аргумента — `NaN`, возвращается `NaN`.
- Если один из аргументов — `NaN`, возвращается другой аргумент.
- Если аргументы — `+0` и `-0`, результат может зависеть от реализации.
- Поведение не определено для нечисловых аргументов (кроме `NaN`).
- Потокобезопасность: безопасен (нет разделяемых данных).

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.22
- GNU C Library, заголовочный файл `tgmath.h`
