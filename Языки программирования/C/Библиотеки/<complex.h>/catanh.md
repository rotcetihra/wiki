# catanh

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<complex.h>|<complex.h>]] / catanh

[[Языки программирования/C/Библиотеки/<complex.h>/catan|Назад]] | [[Языки программирования/C/Библиотеки/<complex.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<complex.h>/ccos|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <complex.h>

double catanh(double complex z);
float catanhf(float complex z);
long double catanhl(long double complex z);
```

## Описание

Функция `catanh` вычисляет арктангенс гиперболический комплексного числа. Арктангенс гиперболический является обратной функцией к тангенсу гиперболическому: если `w = catanh(z)`, то `z = ctanh(w)`.

Результат представляет собой комплексное число, действительная часть которого лежит в диапазоне `(-π/2, π/2)`. Мнимая часть может быть положительной или отрицательной.

Существуют три варианта функции: `catanhf` для `float complex`, `catanh` для `double complex` и `catanhl` для `long double complex`.

## Примеры

```c
#include <stdio.h>
#include <complex.h>

int main(void)
{
    double complex z = 2.0 + 1.0 * I;
    double complex result = catanh(z);
    printf("catanh(%.1f + %.1fi) = %.4f + %.4fi\n",
           creal(z), cimag(z), creal(result), cimag(result));

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартная функция для арктангенса гиперболического | Результат может иметь особые значения |
| Три варианта для разных точностей | Действительная часть ограничена диапазоном |
| Связь с `ctanh` и `csinh` | |

## Похожие определения

- [[Языки программирования/C/Библиотеки/<complex.h>/ctanh|ctanh]] — тангенс гиперболический комплексного числа
- [[Языки программирования/C/Библиотеки/<complex.h>/casinh|casinh]] — арксинус гиперболический комплексного числа
- [[Языки программирования/C/Библиотеки/<complex.h>/cacosh|cacosh]] — арккосинус гиперболический комплексного числа

## Обработка ошибок

- **Поведение при передаче NULL:** Аргумент не является указателем, поэтому передача NULL не применима.
- **Установление errno:** Функции в подмножестве комплексных математических функций не устанавливают `errno` (согласно C11 7.3.8).
- **Возвращаемое значение при ошибке:** В точках ±i результат не определён (может содержать ±∞). Для остальных конечных аргументов результат конечен.
- **Многопоточность:** Функция является потокобезопасной.
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<complex.h>/ctanh|ctanh]] — тангенс гиперболический комплексного числа, [[Языки программирования/C/Библиотеки/<complex.h>/casinh|casinh]] — арксинус гиперболический.

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.3.5.2
- GNU C Library, заголовочный файл `complex.h`