# strtoumax

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<inttypes.h>|<inttypes.h>]] / strtoumax

[[Языки программирования/C/Библиотеки/<inttypes.h>/strtoimax|Назад]] | [[Языки программирования/C/Библиотеки/<inttypes.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<inttypes.h>/wcstoimax|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <inttypes.h>

uintmax_t strtoumax(const char * restrict nptr, char ** restrict endptr, int base);
```

## Описание

Функция `strtoumax` -- преобразование строки в `uintmax_t`.

Функция `strtoumax` преобразует строку `nptr` в значение типа `uintmax_t`. Параметр `base` определяет основание системы счисления (от 2 до 36, или 0 для автоопределения по префиксу `0x`/`0`). Параметр `endptr`, если не равен `NULL`, получает указатель на первый необработанный символ. При переполнении возвращается `UINTMAX_MAX`, а `errno` устанавливается в `ERANGE`.

## Пример

```c
#include <stdio.h>
#include <inttypes.h>

int main(void)
{
    uintmax_t value = strtoumax("0xFF", NULL, 16);
    printf("Значение: %ju\n", value);
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `nptr` | Указатель на преобразуемую строку |
| `endptr` | Указатель на указатель, получающий первый необработанный символ (`NULL` если не используется) |
| `base` | Основание системы счисления (2--36 или 0 для автоопределения) |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| Успешное преобразование | Значение типа `uintmax_t` |
| Переполнение | `UINTMAX_MAX`, `errno = ERANGE` |
| Неверный формат | `0`, `errno` не изменяется |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Работает с типом максимальной ширины | Некорректный `base` -- неопределённое поведение |
| Стандартная функция ISO C99/C23 | -- |

## Похожие функции

- [[Языки программирования/C/Библиотеки/<inttypes.h>/strtoimax|strtoimax]] -- преобразование строки в `intmax_t`
- [[Языки программирования/C/Библиотеки/<stdlib.h>/strtoul|strtoul]] -- преобразование строки в `unsigned long`

## Обработка ошибок

- **Поведение при передаче NULL:** поведение не определено. Параметр `nptr` не должен быть `NULL`; передача `NULL` приводит к неопределённому поведению. Параметр `endptr` может быть `NULL`.
- **Установление errno:** при переполнении (результат не представим в `uintmax_t`) `errno` устанавливается в `ERANGE`. При некорректном формате `errno` не изменяется.
- **Возвращаемое значение при ошибке:** `UINTMAX_MAX` при переполнении; `0` при некорректном формате.
- **Многопоточность:** функция безопасна в многопоточной среде при условии, что аргументы не разделяются между потоками.
- **Связанные функции:** пары с `strtoimax` (для знакового типа); аналог `strtoul` из `<stdlib.h>`, но для типа `uintmax_t`.

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.8.1
- GNU C Library, заголовочный файл `inttypes.h`
