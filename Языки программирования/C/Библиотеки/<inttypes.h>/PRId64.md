# PRId64

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<inttypes.h>|<inttypes.h>]] / PRId64

[[Языки программирования/C/Библиотеки/<inttypes.h>/PRId32|Назад]] | [[Языки программирования/C/Библиотеки/<inttypes.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<inttypes.h>/PRIdMAX|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <inttypes.h>

#define PRId64 /* строковый литерал, раскрываемый в спецификатор формата */
```

## Описание

Макрос `PRId64` -- строковый литерал, используемый в строке формата функции `printf` (и родственных: `fprintf`, `snprintf`, `sprintf`). Заменяет собой спецификатор формата для типа `int64_t` в формате **десятичный**.

Макрос предназначен для переносимого форматирования целочисленных типов фиксированной ширины. На разных платформах один и тот же тип может объявляться как `long`, `long long` или другой тип, поэтому `PRId64` подставляет корректный спецификатор автоматически.

Использование в строке формата:

```c
printf("%" PRId64 "\n", value);
```

> [!NOTE]
> Макрос `PRId64` не является самостоятельным строковым литералом и не должен использоваться отдельно. Он всегда подставляется внутрь строки формата.

## Пример

```c
#include <stdio.h>
#include <inttypes.h>

int main(void)
{
    int64_t value = 42;
    printf("Значение: %" PRId64 "\n", value);
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Гарантированно корректный спецификатор для `int64_t` | Требует конкатенации строк в строке формата |
| Переносимость между платформами | Некоторые компиляторы могут определять макрос как пустую строку |
| Стандартный способ (ISO C99/C23) | -- |

## Похожие макросы

- [[Языки программирования/C/Библиотеки/<inttypes.h>/SCNd64|SCNd64]] -- аналогичный макрос для `scanf`

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.8.1
- GNU C Library, заголовочный файл `inttypes.h`
