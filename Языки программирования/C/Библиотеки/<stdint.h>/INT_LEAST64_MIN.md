# INT_LEAST64_MIN

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<stdint.h>|<stdint.h>]] / INT_LEAST64_MIN

[[Языки программирования/C/Библиотеки/<stdint.h>/UINT_LEAST32_MAX|UINT_LEAST32_MAX]] | [[Языки программирования/C/Библиотеки/<stdint.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<stdint.h>/INT_LEAST64_MAX|INT_LEAST64_MAX]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdint.h>

#define INT_LEAST64_MIN /* целочисленная константа */
```

## Описание

Макрос `INT_LEAST64_MIN` — минимальное значение типа `int_least64_t`. Представляет собой целочисленную константу, вычисляемую на этапе компиляции.

Значение определяется реализацией в соответствии со стандартом C23 (ISO/IEC 9899:2024).

## Пример

```c
#include <stdio.h>
#include <stdint.h>

int main(void)
{
    printf("INT_LEAST64_MIN = %jd\n", (long long)INT_LEAST64_MIN);
    printf("Тип: int_least64_t\n");
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Вычисляется на этапе компиляции | Значение зависит от платформы |
| Гарантирует корректность для типа | Нельзя использовать в `#if` |
| Стандартная часть C99/C23 | — |

## Похожие макросы

- Все макросы `*_MIN` и `*_MAX` из `<stdint.h>`
- Макросы `<limits.h>` для стандартных целочисленных типов

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.20
- GNU C Library, заголовочный файл `stdint.h`
