# stdout

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<stdio.h>|<stdio.h>]] / stdout

[[Языки программирования/C/Библиотеки/<stdio.h>/stdin|stdin]] | [[Языки программирования/C/Библиотеки/<stdio.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<stdio.h>/remove|remove]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdio.h>

extern FILE *stdout;
```

## Описание

`stdout` — стандартный поток вывода. По умолчанию связывает программу с экраном (терминалом). По умолчанию используется построчная буферизация (`_IOLBF`), что означает сброс буфера при записи символа новой строки.

## Пример

```c
#include <stdio.h>

int main(void)
{
    printf("Обычный вывод\n");
    fprintf(stdout, "Вывод в stdout напрямую\n");
    puts("Вывод через puts");
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартный канал вывода | Построчная буферизация может задерживать вывод |
| Может быть перенаправлен в файл | Не подходит для вывода ошибок |
| Стандартная часть ISO C | — |

## Похожие потоки

- [[Языки программирования/C/Библиотеки/<stdio.h>/stdin|stdin]] — стандартный ввод
- [[Языки программирования/C/Библиотеки/<stdio.h>/stderr|stderr]] — вывод ошибок

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.21.1
