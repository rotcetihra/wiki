# fprintf

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<stdio.h>|<stdio.h>]] / fprintf

[[Языки программирования/C/Библиотеки/<stdio.h>/setvbuf|setvbuf]] | [[Языки программирования/C/Библиотеки/<stdio.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<stdio.h>/fscanf|fscanf]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdio.h>

int fprintf(FILE *stream, const char *format, ...);
```

## Описание

Функция `fprintf` записывает форматированный текст в поток `stream`. Параметр `format` определяет формат вывода, за которым следуют аргументы. Возвращает количество записанных символов или отрицательное значение при ошибке.

## Пример

```c
#include <stdio.h>

int main(void)
{
    FILE *fp = fopen("log.txt", "w");
    int age = 25;
    fprintf(fp, "Имя: %s, Возраст: %d\n", "Иван", age);
    fclose(fp);
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `stream` | Выходной поток |
| `format` | Строка формата |
| `...` | Аргументы для подстановки |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| Положительное | Количество записанных символов |
| Отрицательное | Произошла ошибка |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Гибкий формат вывода | Нет ограничения длины результата |
| Работает с любым потоком | Сложный синтаксис формата |
| Стандартная функция ISO C | — |

## Похожие функции

- [[Языки программирования/C/Библиотеки/<stdio.h>/printf|printf]] — вывод в `stdout`
- [[Языки программирования/C/Библиотеки/<stdio.h>/sprintf|sprintf]] — вывод в строку
- [[Языки программирования/C/Библиотеки/<stdio.h>/snprintf|snprintf]] — безопасный вывод в строку

## Обработка ошибок

- **Поведение при передаче NULL:** Поведение не определено, если `stream` или `format` равны `NULL`.
- **Установление errno:** При ошибке может установить `EILSEQ` (ошибка кодирования) или `EBADF` (поток не открыт для записи).
- **Многопоточность:** Не потокобезопасна — запись в общий поток из нескольких потоков может привести к перемешиванию вывода. Используйте блокировку или отдельные потоки.

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.21.6.1
