# fgets

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<stdio.h>|<stdio.h>]] / fgets

[[Языки программирования/C/Библиотеки/<stdio.h>/fgetc|fgetc]] | [[Языки программирования/C/Библиотеки/<stdio.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<stdio.h>/fputc|fputc]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdio.h>

char *fgets(char *s, int size, FILE *stream);
```

## Описание

Функция `fgets` считывает не более `size - 1` символов из потока `stream` и сохраняет их в строку `s` с завершающим `\0`. Чтение прекращается при достижении символа новой строки, конца файла или ограничения `size`. Символ `\n` включается в строку.

## Пример

```c
#include <stdio.h>

int main(void)
{
    char buf[64];
    printf("Введите строку: ");
    if (fgets(buf, sizeof(buf), stdin) != NULL) {
        printf("Прочитано: %s", buf);
    }
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `s` | Буфер для строки |
| `size` | Максимальный размер буфера |
| `stream` | Входной поток |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| `s` | Строка успешно прочитана |
| `NULL` | Ошибка или конец файла |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Безопасна — ограничивает длину | Включает `\n` в результат |
| Стандартная функция ISO C | Нужно удалять `\n` вручную |
| Работает с бинарными данными | — |

## Похожие функции

- [[Языки программирования/C/Библиотеки/<stdio.h>/gets|gets]] — **нежелательна**, небезопасна
- [[Языки программирования/C/Библиотеки/<stdio.h>/fgetc|fgetc]] — посимвольное чтение
- POSIX `getline` — автоматическое расширение буфера

## Обработка ошибок

- **Поведение при передаче NULL:** Поведение не определено, если `stream` равен `NULL`.
- **Установление errno:** При ошибке чтения `errno` может быть установлено в `EIO` (ошибка ввода-вывода) или `EBADF` (поток не открыт для чтения).
- **Многопоточность:** Не потокобезопасна — чтение из общего потока. В многопоточных программах используйте отдельные потоки или блокировку.

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.21.7.2
