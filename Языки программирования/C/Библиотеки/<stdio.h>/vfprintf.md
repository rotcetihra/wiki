# vfprintf

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<stdio.h>|<stdio.h>]] / vfprintf

[[Языки программирования/C/Библиотеки/<stdio.h>/sscanf|sscanf]] | [[Языки программирования/C/Библиотеки/<stdio.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<stdio.h>/vfscanf|vfscanf]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdio.h>
#include <stdarg.h>

int vfprintf(FILE *stream, const char *format, va_list arg);
```

## Описание

Функция `vfprintf` — аналог `fprintf`, принимающий `va_list` вместо переменного числа аргументов. Используется внутри пользовательских обёрток над `fprintf`.

## Пример

```c
#include <stdio.h>
#include <stdarg.h>

void log_message(FILE *fp, const char *fmt, ...)
{
    va_list args;
    va_start(args, fmt);
    vfprintf(fp, fmt, args);
    va_end(args);
}

int main(void)
{
    log_message(stdout, "Число: %d, Строка: %s\n", 42, "текст");
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `stream` | Выходной поток |
| `format` | Строка формата |
| `arg` | Список аргументов `va_list` |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| Положительное | Количество записанных символов |
| Отрицательное | Произошла ошибка |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Позволяет создавать обёртки с форматированием | Требует `<stdarg.h>` |
| Стандартная функция ISO C | Нет ограничения длины |

## Похожие функции

- [[Языки программирования/C/Библиотеки/<stdio.h>/fprintf|fprintf]] — прямой вызов с `...`
- [[Языки программирования/C/Библиотеки/<stdio.h>/vprintf|vprintf]] — вывод в `stdout` с `va_list`
- [[Языки программирования/C/Библиотеки/<stdio.h>/vsnprintf|vsnprintf]] — безопасный вывод в строку с `va_list`

## Обработка ошибок

- **Поведение при передаче NULL:** Поведение не определено, если `stream` или `format` равны `NULL`.
- **Установление errno:** При ошибке кодирования может установить `EILSEQ`.
- **Многопоточность:** Не потокобезопасна — запись в общий поток.

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.21.6.8
