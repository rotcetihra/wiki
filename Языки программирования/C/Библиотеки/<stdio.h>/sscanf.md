# sscanf

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<stdio.h>|<stdio.h>]] / sscanf

[[Языки программирования/C/Библиотеки/<stdio.h>/sprintf|sprintf]] | [[Языки программирования/C/Библиотеки/<stdio.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<stdio.h>/vfprintf|vfprintf]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <stdio.h>

int sscanf(const char *s, const char *format, ...);
```

## Описание

Функция `sscanf` считывает данные из строки `s` в соответствии с форматом `format`. Возвращает количество успешно считанных элементов.

## Пример

```c
#include <stdio.h>

int main(void)
{
    const char *data = "25 Иван 3.14";
    int age;
    char name[32];
    double pi;

    sscanf(data, "%d %s %lf", &age, name, &pi);
    printf("Возраст: %d, Имя: %s, Pi: %.2f\n", age, name, pi);
    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `s` | Входная строка |
| `format` | Строка формата |
| `...` | Указатели на переменные для чтения |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| Количество элементов | Успешно считанные элементы |
| `EOF` | Ошибка до первого преобразования |
| `0` | Ни один элемент не был сопоставлен |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Разбор строк без обращения к потоку | Переполнение буферов при `%s` |
| Гибкий формат | Ошибки формата трудно обнаружить |
| Стандартная функция ISO C | — |

## Похожие функции

- [[Языки программирования/C/Библиотеки/<stdio.h>/scanf|scanf]] — чтение из `stdin`
- [[Языки программирования/C/Библиотеки/<stdio.h>/fscanf|fscanf]] — чтение из потока

## Обработка ошибок

- **Поведение при передаче NULL:** Поведение не определено, если `s` или `format` равны `NULL`.
- **Установление errno:** Не устанавливает `errno` напрямую. При ошибке кодирования (невалидная последовательность байтов) может быть установлено `EILSEQ`.
- **Многопоточность:** Функция потокобезопасна (не использует общее состояние). Не является альтернативой `fscanf` для потокобезопасного чтения — `sscanf` работает со строкой, поэтому потокобезопасна по определению.

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.21.6.7
