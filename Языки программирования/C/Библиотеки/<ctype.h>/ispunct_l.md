# ispunct_l

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<ctype.h>|<ctype.h>]] / ispunct_l

[[Языки программирования/C/Библиотеки/<ctype.h>/ispunct|Назад]] | [[Языки программирования/C/Библиотеки/<ctype.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<ctype.h>/isspace|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <ctype.h> // POSIX.1-2008

int ispunct_l(int c, locale_t locale);
```

## Параметры

| Параметр | Описание |
|---|---|
| `c` | Код символа для проверки (приводится к `unsigned char`) |
| `locale` | Локаль (тип `locale_t` из `<locale.h>`), по правилам которой выполняется классификация |

## Возвращаемое значение

Возвращает ненулевое значение, если символ `c` является знаком пунктуации по правилам локали `locale`; 0 — в противном случае.

Проверка выполняется по таблице символов явно переданной локали, а не глобальной, что позволяет классифицировать символы без изменения состояния процесса — например, распознавать буквы кириллицы в `ru_RU.UTF-8`.

## Что делает

Проверяет, является ли символ `c` знаком пунктуации. В локали `C` верно следующее: `!`, `"`, `#`, `$`, `%`, `&`, `'`, `(`, `)`, `*`, `+`, `,`, `-`, `.`, `/`, `:`, `;`, `<`, `=`, `>`, `?`, `@`, `[`, `\`, `]`, `^`, `_`, `` ` ``, `{`, `|`, `}`, `~`.

Аргумент приводится к `unsigned char`: передача отрицательного значения, отличного от `EOF`, — неопределённое поведение.

## Примеры

```c
#define _XOPEN_SOURCE 700
#include <locale.h>
#include <stdio.h>
#include <ctype.h>

int main(void)
{
    locale_t ru = newlocale(LC_ALL_MASK, "ru_RU.UTF-8", (locale_t)0);

    printf("ispunct_l('А', ru) = %d\n", ispunct_l((unsigned char)'А', ru));
    printf("ispunct_l('A', ru) = %d\n", ispunct_l('A', ru));

    freelocale(ru);
    return 0;
}
```

## Ошибки и errno

Функция не устанавливает `errno`. Если `c` не представим как `unsigned char` и не равен `EOF`, поведение не определено.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Не зависит от глобальной локали процесса | Доступна только в POSIX-системах |
| Потокобезопасна при отдельных локалях | Требует создания и освобождения `locale_t` |

## Альтернативы

- **`ispunct()`** — та же проверка в текущей глобальной локали (ISO C)

## Похожие функции

- [[Языки программирования/C/Библиотеки/<ctype.h>/ispunct|ispunct]] — знак пунктуации
- [[Языки программирования/C/Библиотеки/<ctype.h>/isgraph_l|isgraph_l]] — печатный символ, отличный от пробела с явной локалью

## Источники

- POSIX.1-2008 (раздел 7.4)
- Linux man-pages: `ispunct(3)` (раздел `ispunct_l`)
