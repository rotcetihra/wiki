# isblank

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<ctype.h>|<ctype.h>]] / isblank

[[Языки программирования/C/Библиотеки/<ctype.h>/isascii|Назад]] | [[Языки программирования/C/Библиотеки/<ctype.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<ctype.h>/isblank_l|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <ctype.h>

int isblank(int c);
```

## Параметры

| Параметр | Описание |
|---|---|
| `c` | Код символа для проверки (приводится к `unsigned char`) |

## Возвращаемое значение

Возвращает ненулевое значение, если символ `c` является пробелом или горизонтальной табуляцией; 0 — в противном случае.

## Что делает

Проверяет, является ли символ `c` пробелом или горизонтальной табуляцией. В локали `C` верно следующее: только `' '` и `'\t'`; добавлена в C99.

Аргумент приводится к `unsigned char`: передача отрицательного значения, отличного от `EOF`, — неопределённое поведение.

## Примеры

```c
#include <stdio.h>
#include <ctype.h>

int main(void)
{
    unsigned char c1 = ' ';
    unsigned char c2 = '\t';

    printf("isblank(c1) = %d\n", isblank(c1));
    printf("isblank(c2) = %d\n", isblank(c2));

    return 0;
}
```

## Ошибки и errno

Функция не устанавливает `errno`. Если `c` не представим как `unsigned char` и не равен `EOF`, поведение не определено.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартная и переносимая | Работает с одним символом, а не со строкой |
| Зависит от локали, а не от языка программы | Проверяет только один байт, не многобайтовые символы |
| Может быть вызвана для любого символа таблицы локали | Результат зависит от текущей локали |

## Альтернативы

- **`isblank_l()`** — та же проверка в явно заданной локали (POSIX)

## Похожие функции

- [[Языки программирования/C/Библиотеки/<ctype.h>/isspace|isspace]] — пробельный символ
- [[Языки программирования/C/Библиотеки/<ctype.h>/isblank_l|isblank_l]] — пробел или горизонтальная табуляция с явной локалью

## Источники

- ISO C99 (§7.4.1.3)
- [cppreference: isblank](https://en.cppreference.com/w/c/string/byte/isblank)
- Linux man-pages: `isblank(3)`
