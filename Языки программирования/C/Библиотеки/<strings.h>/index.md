# index

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<strings.h>|<strings.h>]] / index

[[Языки программирования/C/Библиотеки/<strings.h>/ffsll|Назад]] | [[Языки программирования/C/Библиотеки/<strings.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<strings.h>/rindex|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <strings.h>

char *index(const char *s, int c);
```

## Параметры

| Параметр | Описание |
|---|---|
| `s` | Строка для поиска |
| `c` | Искомый символ (преобразуется в `unsigned char`) |

## Возвращаемое значение

Указатель на первое вхождение символа `c` в строке `s` или `NULL`, если символ не найден.

## Что делает

Ищет первое вхождение символа в строке — аналог `strchr()`. Как и `strchr()`, находит завершающий нуль: `index(s, '\0')` возвращает указатель на конец строки. Устаревшая (LEGACY) функция BSD: вместо неё следует использовать `strchr()` из `<string.h>`.

## Примеры

```c
#include <stdio.h>
#include <strings.h>

int main(void)
{
    const char *text = "Hello, world!";

    printf("%s\n", index(text, 'o'));   // o, world!
    printf("%p\n", (void *)index(text, 'z')); // NULL

    return 0;
}
```

## Ошибки и errno

Функция не устанавливает `errno`.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Короткое имя | Устаревшая (LEGACY) |

## Альтернативы

- **`strchr()`** — стандартная замена из `<string.h>`

## Похожие функции

- [[Языки программирования/C/Библиотеки/<strings.h>/rindex|rindex]] — поиск последнего вхождения
- [[Языки программирования/C/Библиотеки/<string.h>/strchr|strchr]] — стандартный поиск символа

## Обработка ошибок

- **Возвращаемое значение при ошибке:** `NULL`, если символ не найден.
- **Установление errno:** не устанавливает `errno`.
- **Поведение при передаче NULL:** поведение не определено (UB) при передаче `NULL` в качестве `s`.
- **Многопоточность:** потокобезопасна (только чтение).
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<strings.h>/rindex|rindex]], [[Языки программирования/C/Библиотеки/<string.h>/strchr|strchr]].

## Источники

- GNU C Library (man-страница `index(3)`)
- POSIX.1-2008 (помечена как LEGACY)
