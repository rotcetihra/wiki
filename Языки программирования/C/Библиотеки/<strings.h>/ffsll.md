# ffsll

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<strings.h>|<strings.h>]] / ffsll

[[Языки программирования/C/Библиотеки/<strings.h>/ffsl|Назад]] | [[Языки программирования/C/Библиотеки/<strings.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<strings.h>/index|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <strings.h>

int ffsll(long long int ll);
```

## Параметры

| Параметр | Описание |
|---|---|
| `ll` | Проверяемое целое |

## Возвращаемое значение

Позицию первого (наименее значимого) установленного бита с нумерацией от 1 или 0, если установленных битов нет.

## Что делает

Аналог `ffs()` для типа `long long`. В glibc доступна как расширение GNU (не входит в POSIX).

## Примеры

```c
#include <stdio.h>
#include <strings.h>

int main(void)
{
    printf("%d\n", ffsll(1LL << 62)); // 63 — 63-й бит
    printf("%d\n", ffsll(0LL));       // 0

    return 0;
}
```

## Ошибки и errno

Функция не устанавливает `errno`.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Работает с `long long` (всегда 64 бита) | Расширение GNU: не переносится на другие POSIX-системы |

## Альтернативы

- [[Языки программирования/C/Библиотеки/<strings.h>/ffs|ffs()]] — если достаточно `int`
- `__builtin_ffsll()` — встроенная функция GCC

## Похожие функции

- [[Языки программирования/C/Библиотеки/<strings.h>/ffs|ffs]] — для `int`
- [[Языки программирования/C/Библиотеки/<strings.h>/ffsl|ffsl]] — для `long`

## Обработка ошибок

- **Возвращаемое значение при ошибке:** не применимо (возвращает позицию бита или 0).
- **Установление errno:** не устанавливает `errno`.
- **Поведение при передаче NULL:** не применимо (аргумент — целое число, не указатель).
- **Многопоточность:** потокобезопасна (не использует разделяемое состояние).
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<strings.h>/ffs|ffs]], [[Языки программирования/C/Библиотеки/<strings.h>/ffsl|ffsl]].

## Источники

- GNU C Library (man-страница `ffsll(3)`)
- Расширение GNU (glibc), не входит в POSIX
