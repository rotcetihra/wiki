# remainder

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<math.h>|<math.h>]] / remainder

[[Языки программирования/C/Библиотеки/<math.h>/nearbyint|Назад]] | [[Языки программирования/C/Библиотеки/<math.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<math.h>/remquo|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <math.h>

double remainder(double x, double y);
double remainderf(float x, float y);         // C99
long double remainderl(long double x, long double y); // C99
```

## Описание

Функция вычисляет остаток от деления x на y по формуле x - n * y, где n — ближайшее целое к отношению x/y. Если результат находится ровно посередине между двумя целыми, выбирается четное n. Результат имеет ту же знак, что и x.

## Примеры

```c
#include <math.h>
#include <stdio.h>

int main(void) {
    printf("remainder(5, 2) = %f\n", remainder(5.0, 2.0)); // 1.0
    printf("remainder(5.5, 2) = %f\n", remainder(5.5, 2.0)); // -0.5
    printf("remainder(-5, 2) = %f\n", remainder(-5.0, 2.0)); // -1.0
    printf("remainder(10, 3) = %f\n", remainder(10.0, 3.0)); // 1.0
    
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Остаток с округлением частного | Не определена при y = 0 |
| Поддерживает все типы с плавающей точкой |  |

## Похожие определения

- [[Языки программирования/C/Библиотеки/<math.h>/fmod|fmod]] — вещественный остаток от деления
- [[Языки программирования/C/Библиотеки/<math.h>/remquo|remquo]] — остаток и часть частного
- [[Языки программирования/C/Библиотеки/<math.h>/round|round]] — округление до ближайшего целого

## Обработка ошибок

- **Поведение при передаче NULL:** Аргументы не являются указателями, поэтому передача NULL не применима.
- **Установление errno:** При y = 0 возникает доменная ошибка; реализация может установить `errno` = `EDOM`.
- **Возвращаемое значение при ошибке:** При y = 0 результат не определён (может быть NaN или вызвать исключение).
- **Многопоточность:** Функция является потокобезопасной.
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<math.h>/fmod|fmod]] — вещественный остаток от деления, [[Языки программирования/C/Библиотеки/<math.h>/remquo|remquo]] — остаток и часть частного.

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.12.10.2
- GNU C Library, заголовочный файл `math.h`