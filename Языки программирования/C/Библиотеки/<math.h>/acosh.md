# acosh

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<math.h>|<math.h>]] / acosh

[[Языки программирования/C/Библиотеки/<math.h>/acos|Назад]] | [[Языки программирования/C/Библиотеки/<math.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<math.h>/asin|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <math.h>

double acosh(double x);
float acoshf(float x);         // C99
long double acoshl(long double x); // C99
```

## Описание

Функция вычисляет обратный гиперболический косинус. Defined for x ≥ 1. Может быть выражена как acosh(x) = ln(x + √(x² - 1)).

## Примеры

```c
#include <math.h>
#include <stdio.h>

int main(void) {
    printf("acosh(1) = %f\n", acosh(1.0)); // 0.0
    printf("acosh(2) = %f\n", acosh(2.0)); // ~1.31696
    
    // Проверка свойства cosh(acosh(x)) = x для x ≥ 1
    double x = 3.0;
    printf("cosh(acosh(%f)) = %f\n", x, cosh(acosh(x)));
    
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Точное вычисление для полного диапазона определения | Не определен для x < 1 (результат NaN) |
| Поддерживает все типы с плавающей точкой |  |

## Похожие определения

- [[Языки программирования/C/Библиотеки/<math.h>/cosh|cosh]] — гиперболический косинус (обратная функция)
- [[Языки программирования/C/Библиотеки/<math.h>/asinh|asinh]] — обратный гиперболический синус
- [[Языки программирования/C/Библиотеки/<math.h>/atanh|atanh]] — обратный гиперболический тангенс

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.12.5.2
- GNU C Library, заголовочный файл `math.h`