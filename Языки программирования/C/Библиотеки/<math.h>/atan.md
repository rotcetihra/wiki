# atan

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<math.h>|<math.h>]] / atan

[[Языки программирования/C/Библиотеки/<math.h>/asinh|Назад]] | [[Языки программирования/C/Библиотеки/<math.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<math.h>/atan2|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <math.h>

double atan(double x);
float atanf(float x);         // C99
long double atanl(long double x); // C99
```

## Описание

Функция вычисляет арктангенс (обратный тангенс) аргумента x. Defined for все действительные числа x. Результат находится в диапазоне (-π/2, π/2).

## Примеры

```c
#include <math.h>
#include <stdio.h>

int main(void) {
    printf("atan(0) = %f\n", atan(0.0)); // 0.0
    printf("atan(1) = %f\n", atan(1.0)); // π/4 ≈ 0.7854
    printf("atan(-1) = %f\n", atan(-1.0)); // -π/4 ≈ -0.7854
    
    // Проверка свойства tan(atan(x)) = x для всех x
    double x = 0.5;
    printf("tan(atan(%f)) = %f\n", x, tan(atan(x)));
    
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Точное вычисление для всего диапазона определения |  |
| Поддерживает все типы с плавающей точкой |  |

## Похожие определения

- [[Языки программирования/C/Библиотеки/<math.h>/tan|tan]] — тангенс (обратная функция)
- [[Языки программирования/C/Библиотеки/<math.h>/atan2|atan2]] — арктангенс с двумя аргументами
- [[Языки программирования/C/Библиотеки/<math.h>/asin|asin]] — арксинус

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.12.4.3
- GNU C Library, заголовочный файл `math.h`