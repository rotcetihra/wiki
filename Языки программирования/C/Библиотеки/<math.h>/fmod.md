# fmod

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<math.h>|<math.h>]] / fmod

[[Языки программирования/C/Библиотеки/<math.h>/floor|Назад]] | [[Языки программирования/C/Библиотеки/<math.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<math.h>/llround|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <math.h>

double fmod(double x, double y);
float fmodf(float x, float y);         // C99
long double fmodl(long double x, long double y); // C99
```

## Описание

Функция возвращает вещественный остаток от деления x на y: x - n * y, где n — целая часть отношения x/y. Результат имеет ту же знак, что и x. Если y = 0, результат не определен.

## Примеры

```c
#include <math.h>
#include <stdio.h>

int main(void) {
    printf("fmod(5, 2) = %f\n", fmod(5.0, 2.0)); // 1.0
    printf("fmod(5.5, 2) = %f\n", fmod(5.5, 2.0)); // 1.5
    printf("fmod(-5, 2) = %f\n", fmod(-5.0, 2.0)); // -1.0
    printf("fmod(5, -2) = %f\n", fmod(5.0, -2.0)); // 1.0
    printf("fmod(10, 3) = %f\n", fmod(10.0, 3.0)); // 1.0
    
    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Вещественный остаток от деления | Не определена при y = 0 |
| Поддерживает все типы с плавающей точкой |  |

## Похожие определения

- [[Языки программирования/C/Библиотеки/<math.h>/remainder|remainder]] — остаток от деления с округлением частного
- [[Языки программирования/C/Библиотеки/<math.h>/remquo|remquo]] — остаток и часть частного
- [[Языки программирования/C/Библиотеки/<math.h>/ceil|ceil]] — округление вверх

## Обработка ошибок

- **Поведение при передаче NULL:** Аргументы не являются указателями, поэтому передача NULL не применима.
- **Установление errno:** При y = 0 возникает доменная ошибка; реализация может установить `errno` = `EDOM`.
- **Возвращаемое значение при ошибке:** При y = 0 результат не определён (может быть NaN или вызвать исключение).
- **Многопоточность:** Функция является потокобезопасной.
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<math.h>/remainder|remainder]] — остаток от деления с округлением частного, [[Языки программирования/C/Библиотеки/<math.h>/remquo|remquo]] — остаток и часть частного.

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.12.10.1
- GNU C Library, заголовочный файл `math.h`