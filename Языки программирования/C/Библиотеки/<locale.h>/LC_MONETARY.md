# LC_MONETARY

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<locale.h>|<locale.h>]] / LC_MONETARY

[[Языки программирования/C/Библиотеки/<locale.h>/LC_CTYPE|Назад]] | [[Языки программирования/C/Библиотеки/<locale.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<locale.h>/LC_NUMERIC|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <locale.h>

#define LC_MONETARY /* константа категории локали */
```

## Описание

Константа `LC_MONETARY` определяет форматирование денежных значений.

Используется как параметр функции `setlocale` для установки или запроса текущей локали, а также как параметр `localeconv` (косвенно, через `setlocale`).

`LC_MONETARY` можно комбинировать с другими категориями через побитовое ИЛИ, но стандарт не гарантирует, что значение `LC_MONETARY` является побитовой маской. Безопасный способ установить все категории -- передать `LC_MONETARY` как отдельный параметр.

## Пример

```c
#include <stdio.h>
#include <locale.h>

int main(void)
{
    if (setlocale(LC_MONETARY, "ru_RU.UTF-8") == NULL) {
        printf("Не удалось установить локаль\n");
        return 1;
    }

    printf("Категория LC_MONETARY установлена\n");
    printf("Текущая локаль: %s\n", setlocale(LC_MONETARY, NULL));

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартный способ управления локалью | Доступные локали зависят от операционной системы |
| Позволяет устанавливать категории по отдельности | Значение константы определяется реализацией |
| Совместим с `localeconv` и `setlocale` | -- |

## Похожие константы

- [[Языки программирования/C/Библиотеки/<locale.h>/LC_ALL|LC_ALL]] -- все категории локали
- [[Языки программирования/C/Библиотеки/<locale.h>/LC_COLLATE|LC_COLLATE]] -- правила колляции
- [[Языки программирования/C/Библиотеки/<locale.h>/LC_CTYPE|LC_CTYPE]] -- классификация символов
- [[Языки программирования/C/Библиотеки/<locale.h>/LC_MONETARY|LC_MONETARY]] -- денежные форматы
- [[Языки программирования/C/Библиотеки/<locale.h>/LC_NUMERIC|LC_NUMERIC]] -- числовые форматы
- [[Языки программирования/C/Библиотеки/<locale.h>/LC_TIME|LC_TIME]] -- формат даты и времени

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.11.1
- GNU C Library, заголовочный файл `locale.h`
