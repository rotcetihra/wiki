# wcsrchr

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<wchar.h>|<wchar.h>]] / wcsrchr

[[Языки программирования/C/Библиотеки/<wchar.h>/wcspbrk|Назад]] | [[Языки программирования/C/Библиотеки/<wchar.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<wchar.h>/wcsspn|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <wchar.h>

wchar_t *wcsrchr(const wchar_t *str, wchar_t c);
```

## Описание

Функция `wcsrchr()` ищет **последнее** вхождение宽окого символа `c` в строке `str` (включая завершающий нуль-терминатор). Возвращает указатель на найденный символ или `NULL`, если символ не найден.

Функция является широкосимвольным аналогом `strrchr()` из `<string.h>`.

> [!NOTE]
> Если `c` равно `L'\0'`, функция возвращает указатель на завершающий нуль-терминатор строки.

## Примеры

```c
#include <stdio.h>
#include <wchar.h>
#include <locale.h>

int main(void)
{
    setlocale(LC_ALL, "");

    const wchar_t *path = L"/home/user/file.txt";
    wchar_t *slash = wcsrchr(path, L'/');

    if (slash)
        printf("Имя файла: %ls\n", slash + 1);

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Быстрый поиск символа с конца строки | Линейная сложность |
| Стандартная функция ISO C | Не potokobezopasna |
| Удобна для извлечения компонентов пути | Возвращает указатель — не индекс |

## Похожие определения

- [[Языки программирования/C/Библиотеки/<wchar.h>/wcschr|wcschr]] — поиск символа с начала строки
- [[Языки программирования/C/Библиотеки/<wchar.h>/wcsstr|wcsstr]] — поиск подстроки
- [[Языки программирования/C/Библиотеки/<string.h>/strrchr|strrchr]] — аналог для байтовых строк

## Обработка ошибок

- **Возвращаемое значение при ошибке:** `NULL`, если символ не найден.
- **Установление errno:** не устанавливает `errno`.
- **Поведение при передаче NULL:** поведение не определено (UB) при передаче `NULL` в качестве `str`.
- **Многопоточность:** потокобезопасна (только чтение).
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<wchar.h>/wcschr|wcschr]], [[Языки программирования/C/Библиотеки/<wchar.h>/wcsstr|wcsstr]].

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.29.4.2
- GNU C Library, заголовочный файл `wchar.h`
