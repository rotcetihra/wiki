# towupper

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<wctype.h>|<wctype.h>]] / towupper

[[Языки программирования/C/Библиотеки/<wctype.h>/towlower|Назад]] | [[Языки программирования/C/Библиотеки/<wctype.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<uchar.h>/char16_t|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <wctype.h>

wint_t towupper(wint_t wc);
```

## Описание

Преобразует широкий символ `wc` в верхний регистр. Широкая версия функции `toupper()` из `<ctype.h>`.

Для букв с двунаправленным регистром возвращает соответствующий символ другого регистра. Для символов, не имеющих регистра (цифры, пунктуация и т. д.), возвращает `wc` без изменений.

Аргумент `wc` не должен равняться `WEOF`.

## Примеры

```c
#include <stdio.h>
#include <wctype.h>

int main(void)
{
    wint_t chars[] = {L'a', L'я', L'5', L' '};
    size_t n = sizeof(chars) / sizeof(chars[0]);

    for (size_t i = 0; i < n; i++)
        printf("towupper(L'%lc') = '%lc'\n", (wint_t)chars[i], towupper(chars[i]));

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартная и портируемая | Преобразует только один символ |
| Поддерживает символы за пределами ASCII | Не преобразует строки целиком |
| Эквивалентна `towctrans(wc, wctrans("toupper"))` | Результат зависит от локали |

## Похожие определения

- [[Языки программирования/C/Библиотеки/<ctype.h>/toupper|toupper]] — то же для однобайтовых символов (`int`)
- [[Языки программирования/C/Библиотеки/<wctype.h>/towlower|towlower]] — преобразование в нижний регистр
- [[Языки программирования/C/Библиотеки/<wctype.h>/iswupper|iswupper]] — проверка на заглавную букву

## Обработка ошибок

- **Возвращаемое значение при ошибке:** не применимо (возвращает `wc` если преобразование не применимо).
- **Установление errno:** не устанавливает `errno`.
- **Поведение при передаче NULL:** не применимо (аргумент — целое число, не указатель).
- **Многопоточность:** потокобезопасна (не использует разделяемое состояние).
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<wctype.h>/towlower|towlower]], [[Языки программирования/C/Библиотеки/<wctype.h>/iswupper|iswupper]].

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.30.2.3.3
- GNU C Library, заголовочный файл `wctype.h`
