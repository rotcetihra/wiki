# iswgraph

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<wctype.h>|<wctype.h>]] / iswgraph

[[Языки программирования/C/Библиотеки/<wctype.h>/iswdigit|Назад]] | [[Языки программирования/C/Библиотеки/<wctype.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<wctype.h>/iswlower|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <wctype.h>

int iswgraph(wint_t wc);
```

## Описание

Проверяет, является ли широкий символ `wc` печатным символом, отличным от пробела. Широкая версия функции `isgraph()` из `<ctype.h>`.

Множество печатных символов без пробела — это все символы, для которых `iswprint()` возвращает ненулевое значение, за вычетом символов, для которых `iswspace()` возвращает ненулевое значение.

Аргумент `wc` должен быть представим как `wint_t` и не должен равняться `WEOF`.

## Примеры

```c
#include <stdio.h>
#include <wctype.h>

int main(void)
{
    wint_t chars[] = {L'A', L'5', L' ', L'\t', L'Я'};
    size_t n = sizeof(chars) / sizeof(chars[0]);

    for (size_t i = 0; i < n; i++)
        printf("iswgraph(L'%lc') = %d\n", (wint_t)chars[i], iswgraph(chars[i]));

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Стандартная и портируемая | Работает с одним символом |
| Чёткое разделение: печатный без пробела | Не различает категории печатных символов |

## Похожие определения

- [[Языки программирования/C/Библиотеки/<ctype.h>/isgraph|isgraph]] — то же для однобайтовых символов (`int`)
- [[Языки программирования/C/Библиотеки/<wctype.h>/iswprint|iswprint]] — печатный символ (включая пробел)
- [[Языки программирования/C/Библиотеки/<wctype.h>/iswspace|iswspace]] — пробельный символ (противоположная категория)

## Обработка ошибок

- **Возвращаемое значение при ошибке:** не применимо (возвращает ненулевое значение, если символ является печатным без пробела, иначе 0).
- **Установление errno:** не устанавливает `errno`.
- **Поведение при передаче NULL:** не применимо (аргумент — целое число, не указатель).
- **Многопоточность:** потокобезопасна (не использует разделяемое состояние).
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<wctype.h>/iswprint|iswprint]], [[Языки программирования/C/Библиотеки/<wctype.h>/iswspace|iswspace]].

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.30.2.1.5
- GNU C Library, заголовочный файл `wctype.h`
