# strcoll

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<string.h>|<string.h>]] / strcoll

[[Языки программирования/C/Библиотеки/<string.h>/strcmp|Назад]] | [[Языки программирования/C/Библиотеки/<string.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<string.h>/strcoll_l|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <string.h>

int strcoll(const char *s1, const char *s2);
```

## Параметры

| Параметр | Описание |
|---|---|
| `s1` | Первая строка для сравнения |
| `s2` | Вторая строка для сравнения |

## Возвращаемое значение

Возвращает значение < 0, если `s1` сортируется раньше `s2`; 0 при равенстве; > 0, если `s1` сортируется позже. Значение того же смысла, что и у `strcmp()`.

## Что делает

Сравнивает строки по правилам сортировки (колляции) текущей локали — категории `LC_COLLATE`. В отличие от `strcmp()`, учитывает языковые правила:

- **Регистр**: «а» и «А» часто располагаются рядом
- **Диакритика**: «ä», «á», «à» могут группироваться с «a»
- **Составные буквы**: в некоторых локалях «ch» и «ll» считаются отдельными буквами алфавита
- **Многобайтовые символы**: строки UTF-8 разбираются по правилам локали, а не по отдельным байтам

Если локаль не установлена (по умолчанию `"C"`), ведёт себя как `strcmp()`.

## Примеры

### Разница между побайтовым и локализованным сравнением

```c
#include <stdio.h>
#include <string.h>
#include <locale.h>

int main(void)
{
    setlocale(LC_ALL, "");

    printf("strcmp:  %d\n", strcmp("ä", "z"));  // по кодам байтов
    printf("strcoll: %d\n", strcoll("ä", "z")); // по правилам локали

    return 0;
}
```

### Сортировка массива строк

```c
#include <stdio.h>
#include <string.h>
#include <locale.h>
#include <stdlib.h>

int compare(const void *a, const void *b)
{
    return strcoll(*(const char **)a, *(const char **)b);
}

int main(void)
{
    setlocale(LC_ALL, "");

    const char *words[] = {"Äpfel", "zebra", "ananas", "árbol"};
    size_t n = sizeof(words) / sizeof(words[0]);

    qsort(words, n, sizeof(char *), compare);

    for (size_t i = 0; i < n; i++) {
        printf("%s\n", words[i]);
    }

    return 0;
}
```

## Ошибки и errno

- **Возвращаемое значение при ошибке:** не применимо (возвращает знаковый результат сравнения)
- **Установление errno:** функция не устанавливает `errno`
- **Поведение при передаче NULL:** неопределённое поведение (аварийный завершение или некорректный доступ к памяти)
- **Многопоточность:** потокобезопасна, но зависит от глобальной локали (используйте `strcoll_l()` для явной локали)
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<string.h>/strcmp|strcmp]] — побайтовое сравнение без локали; [[Языки программирования/C/Библиотеки/<string.h>/strxfrm|strxfrm]] — преобразование строки для последующего сравнения через `strcmp()`

Неопределённое поведение также возникает при обращении по невалидным указателям.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Учитывает правила сортировки локали | Медленнее `strcmp()` (разбор по правилам локали) |
| Результат можно использовать в `qsort()` | Результат зависит от установленной локали |
| Стандартная и переносимая | |

## Альтернативы

- **`strcmp()`** — быстрое побайтовое сравнение без локали
- **`strxfrm()`** + `strcmp()` — ключ строится один раз, потом сравнение быстрое
- **`strcoll_l()`** (POSIX) — сравнение с указанной локалью

## Похожие функции

- [[Языки программирования/C/Библиотеки/<string.h>/strxfrm|strxfrm]] — преобразование строки для последующего сравнения через `strcmp()`
- [[Языки программирования/C/Библиотеки/<string.h>/strcmp|strcmp]] — побайтовое сравнение строк

## Источники

- Стандарт ISO C89 (§4.11.4.3)
- [cppreference: strcoll](https://en.cppreference.com/w/c/string/byte/strcoll)
- Linux man-pages: `strcoll(3)`
