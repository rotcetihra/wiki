# stpcpy

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<string.h>|<string.h>]] / stpcpy

[[Языки программирования/C/Библиотеки/<string.h>/rawmemchr|Назад]] | [[Языки программирования/C/Библиотеки/<string.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<string.h>/stpncpy|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <string.h> // POSIX.1-2008

char *stpcpy(char *dest, const char *src);
```

## Параметры

| Параметр | Описание |
|---|---|
| `dest` | Буфер, куда копируется строка |
| `src` | Копируемая строка |

## Возвращаемое значение

Возвращает указатель на завершающий нуль, записанный в `dest` (конец скопированной строки). В отличие от `strcpy()`, которая возвращает начало `dest`.

## Что делает

Копирует строку `src` в `dest` как `strcpy()`, но возвращает указатель на конец результата. Это позволяет последовательно строить строку «склейкой» без повторного прохода `strlen()` — типичный паттерн для буферов и сообщений.

## Примеры

### Последовательная сборка строки

```c
#include <stdio.h>
#include <string.h>

int main(void)
{
    char buf[64];
    char *p = buf;

    p = stpcpy(p, "Hello");
    p = stpcpy(p, ", ");
    p = stpcpy(p, "world");

    printf("%s\n", buf); // Hello, world

    return 0;
}
```

### Подсчёт длины и копирование за один проход

```c
#include <stdio.h>
#include <string.h>

int main(void)
{
    const char *src = "example";
    char dest[32];

    char *end = stpcpy(dest, src);
    printf("%s (длина %td)\n", dest, end - dest); // example (длина 7)

    return 0;
}
```

## Ошибки и errno

- **Возвращаемое значение при ошибке:** не применимо (всегда возвращает указатель на завершающий нуль)
- **Установление errno:** функция не устанавливает `errno`
- **Поведение при передаче NULL:** неопределённое поведение (аварийный завершение или некорректный доступ к памяти)
- **Многопоточность:** потокобезопасна (не использует разделяемое состояние)
- **Связанные функции:** [[Языки программирования/C/Библиотеки/<string.h>/strcpy|strcpy]] — возвращает начало буфера; [[Языки программирования/C/Библиотеки/<string.h>/stpncpy|stpncpy]] — версия с ограничением длины

Как и `strcpy()`, не проверяет размер буфера — переполнение при слишком длинной `src` даёт неопределённое поведение.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Возврат конца строки упрощает сборку | Не проверяет размер буфера |
| Заменяет связки `strcpy + strlen` | Только POSIX (нет в C) |
| | Области не должны перекрываться |

## Альтернативы

- **[[Языки программирования/C/Библиотеки/<string.h>/strcpy|strcpy()]]** — возвращает начало буфера
- **[[Языки программирования/C/Библиотеки/<string.h>/stpncpy|stpncpy()]]** — версия с ограничением длины
- **[[Языки программирования/C/Библиотеки/<string.h>/mempcpy|mempcpy()]]** — то же для блоков памяти

## Похожие функции

- [[Языки программирования/C/Библиотеки/<string.h>/strcpy|strcpy]] — копирование строки (возврат начала)
- [[Языки программирования/C/Библиотеки/<string.h>/stpncpy|stpncpy]] — копирование с ограничением длины
- [[Языки программирования/C/Библиотеки/<string.h>/strlcat|strlcat]] — безопасная конкатенация

## Источники

- POSIX.1-2008 (§13)
- Linux man-pages: `stpcpy(3)`