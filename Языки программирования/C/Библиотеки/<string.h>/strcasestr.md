# strcasestr

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<string.h>|<string.h>]] / strcasestr

[[Языки программирования/C/Библиотеки/<string.h>/stpncpy|Назад]] | [[Языки программирования/C/Библиотеки/<string.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<string.h>/strcat|Вперёд]]

**Дата написания:** 18.08.2026

## Прототип

```c
#include <string.h> // GNU

char *strcasestr(const char *haystack, const char *needle);
```

## Параметры

| Параметр | Описание |
|---|---|
| `haystack` | Строка, в которой ищется подстрока |
| `needle` | Искомая подстрока (регистр не учитывается) |

## Возвращаемое значение

Возвращает указатель на первое вхождение `needle` в `haystack` без учёта регистра. Если вхождение не найдено — `NULL`. Пустой `needle` возвращает `haystack`.

## Что делает

Аналог [[Языки программирования/C/Библиотеки/<string.h>/strstr|strstr()]], который игнорирует регистр символов: `strcasestr("Hello", "HELLO")` находит вхождение. Применима для поиска ключевых слов, расширений файлов и имён в пользовательском вводе.

## Примеры

### Поиск без учёта регистра

```c
#include <stdio.h>
#include <string.h>

int main(void)
{
    const char *text = "The Quick Brown Fox";

    char *found = strcasestr(text, "brown");
    if (found != NULL) {
        printf("Найдено: %s\n", found); // brown Fox
    }

    return 0;
}
```

### Проверка расширения файла

```c
#include <stdio.h>
#include <string.h>

int main(void)
{
    const char *file = "PHOTO.PNG";

    if (strcasestr(file, ".png") != NULL) {
        printf("Это изображение PNG\n");
    }

    return 0;
}
```

## Ошибки и errno

Функция не устанавливает `errno`. Передача невалидных указателей — неопределённое поведение.

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Игнорирует регистр | Только GNU-расширение |
| Простая в использовании | Не учитывает многобайтовые кодировки корректно |
| | Обычно медленнее `strstr()` |

## Альтернативы

- **[[Языки программирования/C/Библиотеки/<string.h>/strstr|strstr()]]** — с учётом регистра
- **[[Языки программирования/C/Библиотеки/<string.h>/memmem|memmem()]]** — для бинарных данных
- Ручной цикл: привести обе строки к нижнему регистру и сравнить

## Похожие функции

- [[Языки программирования/C/Библиотеки/<string.h>/strstr|strstr]] — поиск подстроки с учётом регистра
- [[Языки программирования/C/Библиотеки/<string.h>/strchrnul|strchrnul]] — поиск символа
- [[Языки программирования/C/Библиотеки/<string.h>/memmem|memmem]] — поиск в бинарных данных

## Источники

- GNU C Library (man-страница `strcasestr(3)`)
- glibc `<string.h>` (секция `__USE_GNU`)