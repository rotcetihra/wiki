# feholdexcept

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<fenv.h>|<fenv.h>]] / feholdexcept

[[Языки программирования/C/Библиотеки/<fenv.h>/fegetenv|Назад]] | [[Языки программирования/C/Библиотеки/<fenv.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<fenv.h>/fesetenv|Вперёд]]

**Дата написания:** 20.08.2026

## Определение

```c
#include <fenv.h>

int feholdexcept(fenv_t *envp);
```

## Описание

Функция `feholdexcept` сохраняет текущую среду вычислений в объект `envp` типа `fenv_t`, затем устанавливает среду по умолчанию (округление — `FE_TONEAREST`, флаги исключений сброшены).

Это комбинация `fegetenv` + `feclearexcept(FE_ALL_EXCEPT)` + `fesetenv(FE_DFL_ENV)` в одной атомарной операции. Функция возвращает предыдущее значение флагов исключений (как `int`), что позволяет определить, были ли активны исключения до сброса.

Функция полезна для временного выполнения кода, который может генерировать исключения, с последующим восстановлением исходной среды и проверкой того, что произошло.

## Пример

```c
#include <stdio.h>
#include <fenv.h>

int main(void)
{
    fenv_t saved;
    int prev_exceptions;

    feraiseexcept(FE_INVALID | FE_DIVBYZERO);
    printf("До feholdexcept: 0x%x\n", fetestexcept(FE_ALL_EXCEPT));

    prev_exceptions = feholdexcept(&saved);
    printf("Предыдущие исключения: 0x%x\n", prev_exceptions);
    printf("После feholdexcept: 0x%x\n", fetestexcept(FE_ALL_EXCEPT));
    printf("Направление округления: %d (ожидается FE_TONEAREST)\n", fegetround());

    volatile double a = 0.0;
    volatile double b = 0.0;
    volatile double c = a / b;

    printf("Новые исключения: 0x%x\n", fetestexcept(FE_ALL_EXCEPT));

    fesetenv(&saved);
    printf("Среда восстановлена\n");

    return 0;
}
```

## Параметры

| Параметр | Описание |
|---|---|
| `envp` | Указатель на объект `fenv_t` для сохранения среды |

## Возвращаемое значение

| Значение | Описание |
|---|---|
| Предыдущее значение флагов исключений | Побитовая маска активных исключений до сброса |

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Сохраняет среду и сбрасывает исключения за один вызов | Возвращает флаги, а не код ошибки — нестандартный паттерн |
| Идеально подходит для изоляции кода | Сбрасывает среду по умолчанию — не произвольную |
| Атомарность операции | — |

## Обработка ошибок

- **Возвращаемое значение при ошибке:** возвращает предыдущее значение флагов исключений (не код ошибки). Ошибки не возникают.
- **Установление errno:** функция не устанавливает `errno`.
- **Поведение при передаче NULL:** если `envp` равен `NULL`, поведение не определено (UB).
- **Многопоточность:** среда вычислений с плавающей точкой является потоковой (каждый поток имеет свою копию). Функция безопасна в многопоточной среде при условии, что каждый поток работает со своей средой.
- **Связанные функции:** `fegetenv` — сохранение среды без сброса исключений; `feupdateenv` — восстановление среды с повторной генерацией исключений.

## Похожие функции

- [[Языки программирования/C/Библиотеки/<fenv.h>/fegetenv|fegetenv]] — сохранение среды без сброса исключений
- [[Языки программирования/C/Библиотеки/<fenv.h>/feupdateenv|feupdateenv]] — восстановление среды с повторной генерацией исключений
- [[Языки программирования/C/Библиотеки/<fenv.h>/feclearexcept|feclearexcept]] — сброс исключений

## Источники

- ISO/IEC 9899:2024 (C23), раздел 7.6.7.2
- GNU C Library, заголовочный файл `fenv.h`
