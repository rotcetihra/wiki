# INT_WIDTH

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<limits.h>|<limits.h>]] / INT_WIDTH

[[Языки программирования/C/Библиотеки/<limits.h>/INT_MIN|Назад]] | [[Языки программирования/C/Библиотеки/<limits.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<limits.h>/LLONG_MAX|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <limits.h>

#define INT_WIDTH 32 // в glibc
```

## Описание

Константа (C23) — ширина типа `int`: количество битов в значении типа без учёта бита знака. По стандарту не меньше 16 (при гарантированном минимуме `INT_MAX` 32767); на всех распространённых платформах равна 32. От `sizeof(int) * CHAR_BIT` отличается тем, что не включает знаковый бит.

## Примеры

```c
#include <limits.h>
#include <stdio.h>

int main(void)
{
    printf("Ширина int: %d\n", INT_WIDTH);

    return 0;
}
```

## Похожие определения

- [[Языки программирования/C/Библиотеки/<limits.h>/INT_MAX|INT_MAX]] — максимум `int`
- [[Языки программирования/C/Библиотеки/<limits.h>/UINT_WIDTH|UINT_WIDTH]] — ширина `unsigned int`

## Источники

- ISO/IEC 9899:2024 (C23), раздел 5.2.4.2.1
- GNU C Library, заголовочный файл `limits.h`