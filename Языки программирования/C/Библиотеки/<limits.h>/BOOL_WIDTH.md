# BOOL_WIDTH

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<limits.h>|<limits.h>]] / BOOL_WIDTH

[[Языки программирования/C/Библиотеки/<limits.h>/BITINT_MAXWIDTH|Назад]] | [[Языки программирования/C/Библиотеки/<limits.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<limits.h>/CHAR_BIT|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <limits.h>

#define BOOL_WIDTH 1 // в glibc
```

## Описание

Константа (C23) — ширина типа `bool`: количество битов в значении этого типа; по стандарту всегда равна 1. Значение `TRUE`/`FALSE`... нет — хранится как 0 или 1, остальные биты отсутствуют, потому что `bool` — тип с одной двоичной цифрой.

## Примеры

```c
#include <limits.h>
#include <stdbool.h>
#include <stdio.h>

int main(void)
{
    bool flag = true;

    printf("Ширина bool: %d\n", BOOL_WIDTH);
    printf("Размер bool: %zu байт\n", sizeof flag);

    return 0;
}
```

## Плюсы и минусы

| Преимущество | Недостаток |
|---|---|
| Гарантированная ширина 1 бит по стандарту | Не показывает, сколько памяти занимает тип (обычно 1 байт) |

## Похожие определения

- [[Языки программирования/C/Библиотеки/<limits.h>/BITINT_MAXWIDTH|BITINT_MAXWIDTH]] — максимальная ширина `_BitInt`
- [[Языки программирования/C/Библиотеки/<stdbool.h>/bool|bool]] — логический тип

## Источники

- ISO/IEC 9899:2024 (C23), раздел 5.2.4.2.1
- GNU C Library, заголовочный файл `limits.h`