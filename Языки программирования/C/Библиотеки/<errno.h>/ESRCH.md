# ESRCH

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<errno.h>|<errno.h>]] / ESRCH

[[Языки программирования/C/Библиотеки/<errno.h>/ESPIPE|Назад]] | [[Языки программирования/C/Библиотеки/<errno.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<errno.h>/ESRMNT|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <errno.h>

#define ESRCH 3 // "No such process"
```

## Описание

«Нет такого процесса»: ID процесса/потока, указанный в вызове, не существует. POSIX-код.

## Когда возникает

- `kill()` с несуществующим PID (классика — «kill: no such process»);
- `waitpid()`/`sched_*`/`setpgid()`/`getpriority()` с чужим или несуществующим процессом;
- операции с группами процессов.

## Похожие коды

- `ECHILD` — нет дочерних процессов
- `EINVAL` — неверный аргумент (например, отрицательный pid в waitpid)

## Источники

- POSIX.1-2024, раздел `errno.h`
- Linux: `asm-generic/errno-base.h`, man-страница `kill(2)`
