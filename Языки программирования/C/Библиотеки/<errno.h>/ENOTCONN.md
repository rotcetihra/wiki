# ENOTCONN

[[Языки программирования/C/Библиотеки|Библиотеки]] / [[Языки программирования/C/Библиотеки/<errno.h>|<errno.h>]] / ENOTCONN

[[Языки программирования/C/Библиотеки/<errno.h>/ENOTBLK|Назад]] | [[Языки программирования/C/Библиотеки/<errno.h>|Содержание]] | [[Языки программирования/C/Библиотеки/<errno.h>/ENOTDIR|Вперёд]]

**Дата написания:** 18.08.2026

## Определение

```c
#include <errno.h>

#define ENOTCONN 107 // "Transport endpoint is not connected"
```

## Описание

«Транспортная конечная точка не соединена»: сокетная операция, требующая установленного соединения, выполнена на неподключённом сокете. POSIX (XSI).

## Когда возникает

- `send()`/`recv()` на сокете без установленного соединения (для потоковых сокетов);
- `getsockopt(SO_ERROR)` при разорванном соединении;
- `shutdown()` на несоединённом сокете.

## Похожие коды

- `EISCONN` — сокет уже соединён
- `EPIPE` — запись в разорванное соединение
- `ECONNRESET` — соединение сброшено

## Источники

- POSIX.1-2024, раздел `errno.h`
- Linux: `asm-generic/errno.h`, man-страница `recv(2)`
