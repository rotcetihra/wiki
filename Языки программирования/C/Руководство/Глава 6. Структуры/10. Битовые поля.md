# Битовые поля

[[Языки программирования/C/Руководство/Глава 6. Структуры|Глава 6. Структуры]] / Битовые поля

[[Языки программирования/C/Руководство/Глава 6. Структуры/9. Объединения|Назад]] | [[Языки программирования/C/Руководство/Глава 6. Структуры|Содержание]] | [[Языки программирования/C/Руководство/Глава 7. Динамическая память/1. Выделение и освобождение памяти|Вперёд]]

**Дата написания:** 03.08.2026

## 1. Введение

Битовые поля обеспечивают удобный доступ к отдельным битам данных. Они позволяют формировать объекты с длиной, не кратной байту. Что в свою очередь позволяет экономить память, более плотно размещая данные.

Битовое поле не может существовать само по себе. Оно может быть только элементом структуры или объединения. Например, в рамках структуры битовые поля имеют следующую форму определения:

```c
struct имя_структуры
{
    тип1 имя_поля1 : ширина_поля1;
    тип2 имя_поля2 : ширина_поля2;
    // ...
    типi имя_поляi : ширина_поляi;
};
```

В качестве типа поля может использоваться только `int`, но допустимы также модификаторы `signed` и `unsigned`. Имя поля представляет произвольный идентификатор, а ширина поля — положительное целое число, которое не должно превышать длину машинного слова для конкретной платформы (машинное слово измеряется в битах или байтах и равно разрядности регистров процессора; например, для 64-разрядной архитектуры длина — 64 бита).

## 2. Определение битовых полей

Например, определим структуру с битовыми полями:

```c
struct point
{
    unsigned int x:5;   // 0-31
    unsigned int y:3;   // 0-7
};
```

Структура `point` содержит два битовых поля. Первое поле — `x` — имеет ширину в 5 бит. То есть оно может принимать значения от 0 до 31. Второе поле — `y` — имеет ширину в 3 бита и может принимать значения от 0 до 7.

Затем мы сможем работать с этой структурой и ее элементами как с любой структурой:

```c
#include <stdio.h>

struct point
{
    unsigned int x:5;   // 0-31
    unsigned int y:3;   // 0-7
};

int main(void)
{
    struct point center = {0, 5};
    center.x = 2;
    printf("x=%d    y=%d \n", center.x, center.y);  // x=2  y=5

    return 0;
}
```

## 3. Размещение полей в памяти

В зависимости от платформы расположение полей структуры в памяти может отличаться. В частности, на Windows порядок расположения следующий: первое объявленное поле занимает младшие биты, а последующие поля — более старшие. То есть если мы возьмем из примера выше поля `x` и `y` структуры `point` с финальными значениями x=2 и y=5, то получим следующее размещение битов в памяти: поле `x` (объявлено первым) занимает младшие 3 бита, поле `y` — следующие 5 бит.

Так как поля `x` и `y` вместе занимают 8 бит, то всего имеется 8 ячеек. На трех младших битах размещено поле `x` со значением 2 (в двоичной системе это `010`). А на следующих 5 битах помещается поле `y` со значением 5 (то есть `101` в двоичной системе).

В принципе мы сами можем программным образом узнать размещение полей в памяти. Для этого воспользуемся объединениями:

```c
#include <stdio.h>

struct point
{
    unsigned int x:5;   // 0-31
    unsigned int y:3;   // 0-7
};

union code
{
    struct point p;
    struct {
        unsigned a0:1;
        unsigned a1:1;
        unsigned a2:1;
        unsigned a3:1;
        unsigned a4:1;
        unsigned a5:1;
        unsigned a6:1;
        unsigned a7:1;
    } byte;
};

int main(void)
{
    struct point center = {2, 5};
    union code c;
    c.p = center;
    printf("7 \t 6 \t 5 \t 4 \t 3 \t 2 \t 1 \t 0 \n");
    printf("%d \t %d \t %d \t %d \t %d \t %d \t %d \t %d \n",
           c.byte.a7, c.byte.a6, c.byte.a5, c.byte.a4,
           c.byte.a3, c.byte.a2, c.byte.a1, c.byte.a0);
    return 0;
}
```

Объединение `code` содержит два элемента — структуру `point` и безымянную структуру `byte`. Как известно из прошлой темы, элементы объединения будут занимать одну и ту же область в памяти, точнее, будут начинаться с одного и того же места в памяти. Поэтому для выяснения, какие биты заняты, безымянная структура `byte` имеет 8 полей, каждое из которых имеет ширину 1 бит.

При запуске программы мы наглядно сможем увидеть размещение значений в памяти:

```
7	6	5	4	3	2	1	0
1	0	1	0	0	0	1	0
```

## 4. Резюме

1. Битовое поле — элемент структуры или объединения с шириной в битах: `unsigned int x:5;`.
2. Тип поля — `int` (или `signed`/`unsigned`), ширина не больше машинного слова платформы.
3. Битовые поля позволяют плотно упаковывать данные и экономить память.
4. Размещение битовых полей зависит от платформы; его можно проверить программно через объединение с полями шириной в 1 бит.

[[Языки программирования/C/Руководство/Глава 6. Структуры/9. Объединения|Назад]] | [[Языки программирования/C/Руководство/Глава 6. Структуры|Содержание]] | [[Языки программирования/C/Руководство/Глава 7. Динамическая память/1. Выделение и освобождение памяти|Вперёд]]
