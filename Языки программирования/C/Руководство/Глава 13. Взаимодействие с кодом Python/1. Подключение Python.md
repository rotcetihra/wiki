# Подключение Python

[[Языки программирования/C/Руководство/Глава 13. Взаимодействие с кодом Python|Глава 13. Взаимодействие с кодом Python]] / Подключение Python

[[Языки программирования/C/Руководство/Глава 12. Среды разработки для С/2. Первая программа в Qt Creator|Назад]] | [[Языки программирования/C/Руководство/Глава 13. Взаимодействие с кодом Python|Содержание]] | [[Языки программирования/C/Руководство/Глава 14. Дополнительные статьи/1. Разделяемые библиотеки на Linux|Вперёд]]

**Дата написания:** 04.08.2026

## 1. Введение

Язык программирования Python обладает большими возможностями, имеет богатый функционал и библиотеки, написание которых на языке Си может занять продолжительное время, особенно это касается сферы машинного обучения. И было бы неплохо просто взять готовую функциональность на Python и инкорпорировать в программу на C. Python позволяет это сделать, предоставляя такую функциональность, как **встраиваемый Python** (Embedded Python). То есть мы можем встроить в программу на C интерпретатор Python и выполнять в программе на C код на языке Python. Рассмотрим, как это сделать.

## 2. Настройка и компиляция на Windows

Для работы на ОС Windows прежде всего нужно узнать путь к установленному интерпретатору Python. Например, `C:\Users\eugen\AppData\Local\Programs\Python\Python311`. Для упрощения работы установим в переменных среды переменную `PYTHONHOME`. Она должна указывать на путь к каталогу, где установлен Python. Эта переменная будет использоваться для поиска нужных библиотек.

Определим файл `app.c` со следующей программой на языке C:

```c
#include <Python.h>

int main()
{
    Py_Initialize();
    PyRun_SimpleString("print('Hello METANIT.COM')");
    Py_Finalize();
    return 0;
}
```

Прежде всего, чтобы встроить Python в программу, подключаем заголовочный файл `Python.h`. Этот файл устанавливается вместе с другими файлами интерпретатора Python по умолчанию и находится в папке интерпретатора Python в каталоге `include`.

В программе сначала вызывается функция `Py_Initialize()`. Эта функция инициализирует интерпретатор Python, таблицу модулей и устанавливает путь к модулям.

Чтобы выполнить некоторый код Python, применяется функция `PyRun_SimpleString()`, в которую передается код Python:

```c
PyRun_SimpleString("print('Hello METANIT.COM')");
```

Здесь код Python представляет вывод строки на консоль с помощью функции `print()`.

После выполнения кода Python вызывается функция `Py_Finalize()`, которая освобождает память, выделенную для интерпретатора Python.

### 2.1. Компиляция на Windows

Для компиляции в Windows используем компилятор `gcc`. Выполним команду:

```
gcc -Wall -I %PYTHONHOME%\include -L %PYTHONHOME%\libs -o app.exe app.c -lpython311 & app
```

При компиляции с помощью опции `-I` нужно передать путь к заголовочным файлам — это папка `%PYTHONHOME%\include`. Чтобы не писать полный путь, заменяем его часть содержимым ранее установленной переменной `PYTHONHOME`.

Кроме того, с помощью опции `-L` нужно установить путь к библиотеке Python — это путь `%PYTHONHOME%\libs`. Поскольку здесь используется версия Python 3.11, указывается параметр `-lpython311`.

В итоге при выполнении программы будет выполняться код Python, который выведет на консоль строку:

```
c:\C>gcc -Wall -I %PYTHONHOME%\include -L %PYTHONHOME%\libs -o app app.c -lpython311 & app
Hello METANIT.COM

c:\C>
```

При этом в программе могут быть и другие инструкции на языке C:

```c
#include <stdio.h>
#include <Python.h>

int main()
{
    printf("Python program starts\n");
    Py_Initialize();
    PyRun_SimpleString("print('Hello METANIT.COM')");
    Py_Finalize();
    printf("Python program ends\n");
    return 0;
}
```

### 2.2. Компиляция на Linux

Компиляция на Linux несколько отличается. Для компиляции на Ubuntu применяется следующая команда:

```
gcc -Wall $(python3-config --cflags) -o app app.c $(python3-config --embed --ldflags)
```

## 3. Использование библиотек Python

Подобным образом можно использовать более сложный код Python, в том числе с использованием сторонних библиотек Python. Например, нарисуем произвольный график с помощью библиотек `numpy` и `matplotlib`. Соответственно, вначале надо установить данные библиотеки с помощью пакетного менеджера `pip`:

```
pip install numpy
pip install matplotlib
```

Для создания графика определим на C следующую программу:

```c
#include <Python.h>
int main()
{
    Py_Initialize();
    PyRun_SimpleString(
"import numpy as np\n"
"import matplotlib.pyplot as plt\n"
"x = np.array(range(0, 8))\n"
"y = eval('2 * x + 1')\n"
"plt.title('y = 2x + 1')\n"
"plt.xlabel('x')\n"
"plt.ylabel('y')\n"
"plt.plot(x,y)\n"
"plt.show()");
    Py_Finalize();
    return 0;
}
```

В данном случае сначала подключается функционал библиотек `numpy` и `matplotlib`:

```python
import numpy as np
import matplotlib.pyplot as plt
```

Далее генерируются значения, которые отображаются по оси X:

```python
x = np.array(range(0, 8))
```

Для генерации применяется функция `np.array()` библиотеки Numpy, которая создает набор чисел от 0 до 7.

Далее по значениям X получаем значения Y по определенной формуле. Здесь рисуется график функции `y = 2x + 1`. Для вычисления значений Y применяется встроенная функция `eval()`, которая есть в языке Python и которая вычисляет произвольное выражение:

```python
y = eval('2 * x + 1')
```

Затем устанавливается заголовок графика:

```python
plt.title('y = 2x + 1')
```

Устанавливаются текстовые метки для осей X и Y:

```python
plt.xlabel('x')
plt.ylabel('y')
```

И в конце отрисовывается график и отображается на экране:

```python
plt.plot(x,y)
plt.show()
```

## 4. Выполнение файла с кодом Python

Если инструкций на Python много, то их проще определить в отдельный файл. Затем в программе на языке C этот файл можно выполнить с помощью функции `PyRun_SimpleFile()`, которая имеет следующее определение:

```c
int PyRun_SimpleFile(FILE *fp, const char *filename)
```

В качестве первого параметра передается дескриптор файла, а в качестве второго — имя файла.

Например, определим в одной папке с программой файл `main.py` со следующим кодом на языке Python:

```python
print("Hello Python in C")
```

Здесь код просто выводит строку на консоль.

В главном файле программы на C (например, `app.c`) выполним этот скрипт Python:

```c
#include <Python.h>

int main()
{
    char filename[] = "main.py";
    Py_Initialize();
    FILE* fp = fopen(filename, "rb");   // открываем файл
    if(fp)
        PyRun_SimpleFile(fp, filename);     // выполняем программу python
    Py_Finalize();
    return 0;
}
```

Здесь сначала открывается файл в бинарном режиме для чтения и получается указатель на файл с помощью функции `fopen()`, затем выполняется файл с кодом Python. В итоге консоль должна вывести:

```
Hello Python in C
```

## 5. Резюме

1. Встраиваемый Python позволяет выполнять код на Python из программы на C через заголовочный файл `Python.h`.
2. Инициализация интерпретатора выполняется функцией `Py_Initialize()`, освобождение ресурсов — `Py_Finalize()`.
3. Код Python выполняется функциями `PyRun_SimpleString()` и `PyRun_SimpleFile()`.
4. При компиляции на Windows передаются пути к `include` и `libs` через `%PYTHONHOME%` и флаг `-lpython311`; на Linux используется `python3-config --embed --ldflags`.

[[Языки программирования/C/Руководство/Глава 12. Среды разработки для С/2. Первая программа в Qt Creator|Назад]] | [[Языки программирования/C/Руководство/Глава 13. Взаимодействие с кодом Python|Содержание]] | [[Языки программирования/C/Руководство/Глава 14. Дополнительные статьи/1. Разделяемые библиотеки на Linux|Вперёд]]