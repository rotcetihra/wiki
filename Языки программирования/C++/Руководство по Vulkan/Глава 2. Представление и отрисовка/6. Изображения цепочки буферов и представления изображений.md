# Изображения цепочки буферов и представления изображений

[[Языки программирования/C++|C++]] / [[Языки программирования/C++/Руководство по Vulkan|Руководство по Vulkan]] / [[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка|Глава 2. Представление и отрисовка]] / Изображения цепочки буферов и представления изображений

[[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка/5. Создание и удаление цепочки буферов|Назад]] | [[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка|Содержание]] | [[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка/7. Графический конвейер. Шейдеры|Вперёд]]

**Дата написания:** 05.09.2026

После создания цепочки буферов можно из нее получить изображения. В последствии их можно будет использовать при рендеринге. Изображения в Vulkan представляют непрозрачный указатель `VkImage`. А для получения изображений из цепочки буферов применяется функция `vkGetSwapchainImagesKHR()`:

```cpp
VkResult vkGetSwapchainImagesKHR(
    VkDevice                                    device,
    VkSwapchainKHR                              swapchain,
    uint32_t*                                   pSwapchainImageCount,
    VkImage*                                    pSwapchainImages);
```

Функция принимает следующие параметры:

- `device`: логическое устройство, связанное с цепочкой буферов.
- `swapchain`: сама цепочка буферов.
- `pSwapchainImageCount`: количество доступных изображений.
- `pSwapchainImages`: либо NULL, либо указатель на массив `VkImage`.

Изображения создаются реализацией для цепочки буферов и автоматически удаляются после уничтожения цепочки буферов.

Пример получения изображений:

```cpp
std::vector<VkImage> swapChainImages;

uint32_t imageCount = 0;
vkGetSwapchainImagesKHR(device, swapChain, &imageCount, nullptr);
swapChainImages.resize(imageCount);
vkGetSwapchainImagesKHR(device, swapChain, &imageCount, swapChainImages.data());
```

## Представления изображений

Объекты `VkImage` не имеют прямого доступа к конвейерным шейдерам для чтения или записи данных изображения. И чтобы использовать любое изображение, в том числе изображения из цепочки буферов, нам нужно создать объект `VkImageView`. Он описывает, как получить доступ к изображению и к какой части изображения получить доступ.

Для создания представления изображения применяется функция `vkCreateImageView()`:

```cpp
VkResult vkCreateImageView(
    VkDevice                                    device,
    const VkImageViewCreateInfo*                pCreateInfo,
    const VkAllocationCallbacks*                pAllocator,
    VkImageView*                                pView);
```

Для создания представления изображения требуется структура `VkImageViewCreateInfo`:

```cpp
typedef struct VkImageViewCreateInfo {
    VkStructureType            sType;
    const void*                pNext;
    VkImageViewCreateFlags     flags;
    VkImage                    image;
    VkImageViewType            viewType;
    VkFormat                   format;
    VkComponentMapping         components;
    VkImageSubresourceRange    subresourceRange;
} VkImageViewCreateInfo;
```

Пример создания представлений изображений:

```cpp
void createImageViews() {
    swapChainImageViews.resize(swapChainImages.size());
    for (size_t i{}; i < swapChainImages.size(); i++) {
        VkImageViewCreateInfo createInfo{};
        createInfo.sType = VK_STRUCTURE_TYPE_IMAGE_VIEW_CREATE_INFO;
        createInfo.image = swapChainImages[i];
        createInfo.viewType = VK_IMAGE_VIEW_TYPE_2D;
        createInfo.format = swapChainImageFormat;

        createInfo.components.r = VK_COMPONENT_SWIZZLE_IDENTITY;
        createInfo.components.g = VK_COMPONENT_SWIZZLE_IDENTITY;
        createInfo.components.b = VK_COMPONENT_SWIZZLE_IDENTITY;
        createInfo.components.a = VK_COMPONENT_SWIZZLE_IDENTITY;

        createInfo.subresourceRange.aspectMask = VK_IMAGE_ASPECT_COLOR_BIT;
        createInfo.subresourceRange.baseMipLevel = 0;
        createInfo.subresourceRange.levelCount = 1;
        createInfo.subresourceRange.baseArrayLayer = 0;
        createInfo.subresourceRange.layerCount = 1;

        if (vkCreateImageView(device, &createInfo, nullptr, &swapChainImageViews[i]) != VK_SUCCESS) {
            throw std::runtime_error("Не удалось создать представление изображения!");
        }
    }
}
```

Поле `components` позволяет переключать цветовые каналы. Значение `VK_COMPONENT_SWIZZLE_IDENTITY` указывает, что компонент получает значение по умолчанию.

Поле `subresourceRange` указывает на назначение изображения и к какой части изображения следует получить доступ.

## Удаление представлений изображений

```cpp
vkDestroyImageView(device, imageView, nullptr);
```

**Источник:** [https://metanit.com/cpp/vulkan/2.6.php](https://metanit.com/cpp/vulkan/2.6.php)
