# Создание графического конвейера

[[Языки программирования/C++|C++]] / [[Языки программирования/C++/Руководство по Vulkan|Руководство по Vulkan]] / [[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка|Глава 2. Представление и отрисовка]] / Создание графического конвейера

[[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка/10. Фиксированные функции графического конвейера|Назад]] | [[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка|Содержание]] | [[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка/12. Буфер кадров|Вперёд]]

**Дата написания:** 05.09.2026

Графический конвейер представляет непрозрачный указатель `VkPipeline`. Для создания конвейера применяется функция `vkCreateGraphicsPipelines()`:

```cpp
VkResult vkCreateGraphicsPipelines(
    VkDevice                                    device,
    VkPipelineCache                             pipelineCache,
    uint32_t                                    createInfoCount,
    const VkGraphicsPipelineCreateInfo*         pCreateInfos,
    const VkAllocationCallbacks*                pAllocator,
    VkPipeline*                                 pPipelines);
```

Для создания конвейера необходима структура `VkGraphicsPipelineCreateInfo`:

```cpp
VkGraphicsPipelineCreateInfo pipelineInfo{};
pipelineInfo.sType = VK_STRUCTURE_TYPE_GRAPHICS_PIPELINE_CREATE_INFO;
pipelineInfo.stageCount = 2;
pipelineInfo.pStages = shaderStages;
pipelineInfo.pVertexInputState = &vertexInputInfo;
pipelineInfo.pInputAssemblyState = &inputAssembly;
pipelineInfo.pViewportState = &viewportState;
pipelineInfo.pRasterizationState = &rasterizer;
pipelineInfo.pMultisampleState = &multisampling;
pipelineInfo.pDepthStencilState = nullptr;
pipelineInfo.pColorBlendState = &colorBlending;
pipelineInfo.pDynamicState = &dynamicState;
pipelineInfo.layout = pipelineLayout;
pipelineInfo.renderPass = renderPass;
pipelineInfo.subpass = 0;
pipelineInfo.basePipelineHandle = VK_NULL_HANDLE;
pipelineInfo.basePipelineIndex = -1;

if (vkCreateGraphicsPipelines(device, VK_NULL_HANDLE, 1, &pipelineInfo, nullptr, &graphicsPipeline) != VK_SUCCESS) {
    throw std::runtime_error("Не удалось создать графический конвейер!");
}
```

Vulkan позволяет создать новый графический конвейер, производный от уже существующего конвейера. Для этого используются поля `basePipelineHandle` или `basePipelineIndex`.

Удаление конвейера:

```cpp
vkDestroyPipeline(device, graphicsPipeline, nullptr);
```

**Источник:** [https://metanit.com/cpp/vulkan/2.11.php](https://metanit.com/cpp/vulkan/2.11.php)
