# Цепочка буферов и проверка ее поддержки

[[Языки программирования/C++|C++]] / [[Языки программирования/C++/Руководство по Vulkan|Руководство по Vulkan]] / [[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка|Глава 2. Представление и отрисовка]] / Цепочка буферов и проверка ее поддержки

[[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка/2. Очередь представления|Назад]] | [[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка|Содержание]] | [[Языки программирования/C++/Руководство по Vulkan/Глава 2. Представление и отрисовка/4. Настройка цепочки буферов|Вперёд]]

**Дата написания:** 05.09.2026

Для визуализации Vulkan требуется инфраструктура, которая известна как цепочка буферов или Swap chain. Цепочка буферов представляет собой очередь изображений, которые ожидают вывода на экран. По сути цепочка буферов позволяет приложению представлять изображения пользователю. Цепочки буферов используются для достижения плавного рендеринга кадров в графических приложениях. В Vulkan обычно создается цепочка буферов для каждого окна или поверхности, где нужно отображать графику.

Основные аспекты применения цепочки буферов:

- **Двойная буферизация:** одной из основных целей цепочки буферов является реализация двойной буферизации. При двойной буферизации приложение отображает графику в одно изображение (фоновый буфер), в то время как другое изображение (передний буфер) отображается на экране.
- **Синхронизация:** цепочка буферов обеспечивают синхронизацию между кодом рендеринга и частотой обновления дисплея.
- **Адаптируемость к дисплею:** цепочка буферов адаптируется к свойствам целевого дисплея или окна.

## Расширение VK_KHR_swapchain

Для работы с цепочкой буферов необходимо подключить расширение `VK_KHR_swapchain`. Заголовочный файл Vulkan предоставляет макрос `VK_KHR_SWAPCHAIN_EXTENSION_NAME`, который определяется как `VK_KHR_swapchain`.

Прежде всего необходимо проверить поддержку расширения выбранным физическим устройством:

```cpp
bool checkSwapchainExtensionSupport(VkPhysicalDevice device) {
    uint32_t extensionCount;
    vkEnumerateDeviceExtensionProperties(device, nullptr, &extensionCount, nullptr);

    std::vector<VkExtensionProperties> availableExtensions(extensionCount);
    vkEnumerateDeviceExtensionProperties(device, nullptr, &extensionCount, availableExtensions.data());

    std::string extensionName {VK_KHR_SWAPCHAIN_EXTENSION_NAME};

    for (const auto& extension : availableExtensions) {
        if(extensionName == extension.extensionName) return true;
    }

    return false;
}
```

И для подключения расширения потребуется настроить создание логического устройства:

```cpp
const std::vector<const char*> deviceExtensions = { VK_KHR_SWAPCHAIN_EXTENSION_NAME};
createInfo.enabledExtensionCount = static_cast<uint32_t>(deviceExtensions.size()); 
createInfo.ppEnabledExtensionNames = deviceExtensions.data();
```

## Совместимость с поверхностью рисования

Цепочка буферов может быть несовместима с выбранной поверхностью окна. В основном нам нужно проверить три типа свойств:

- **Базовые возможности поверхности** (минимальное/максимальное количество изображений, ширина и высота), представленные структурой `VkSurfaceCapabilitiesKHR`. Для проверки применяется функция `vkGetPhysicalDeviceSurfaceCapabilitiesKHR()`.
- **Форматы поверхности** (формат пикселей, цветовое пространство), представленные структурой `VkSurfaceFormatKHR`. Для проверки применяется функция `vkGetPhysicalDeviceSurfaceFormatsKHR()`.
- **Доступные режимы представления**, представленные структурой `VkPresentModeKHR`. Для проверки применяется функция `vkGetPhysicalDeviceSurfacePresentModesKHR()`.

Чтобы упростить проверку совместимости, определим следующую структуру:

```cpp
struct SwapChainSupportDetails {
    VkSurfaceCapabilitiesKHR capabilities;
    std::vector<VkSurfaceFormatKHR> formats;
    std::vector<VkPresentModeKHR> presentModes;
};
```

Функция проверки поддержки цепочки буферов:

```cpp
SwapChainSupportDetails querySwapChainSupport(VkPhysicalDevice device, VkSurfaceKHR surface) {
    SwapChainSupportDetails details;

    vkGetPhysicalDeviceSurfaceCapabilitiesKHR(device, surface, &details.capabilities);

    uint32_t formatCount;
    vkGetPhysicalDeviceSurfaceFormatsKHR(device, surface, &formatCount, nullptr);

    if (formatCount != 0) {
        details.formats.resize(formatCount);
        vkGetPhysicalDeviceSurfaceFormatsKHR(device, surface, &formatCount, details.formats.data());
    }

    uint32_t presentModeCount;
    vkGetPhysicalDeviceSurfacePresentModesKHR(device, surface, &presentModeCount, nullptr);

    if (presentModeCount != 0) {
        details.presentModes.resize(presentModeCount); 
        vkGetPhysicalDeviceSurfacePresentModesKHR(device, surface, &presentModeCount, details.presentModes.data());
    }

    return details;
}
```

Если есть хотя бы один поддерживаемый формат изображения и один поддерживаемый режим представления, мы можем считать, что устройство поддерживает цепочку буферов:

```cpp
bool isDeviceSuitable(VkPhysicalDevice device) {
    QueueFamilyIndices indices = findQueueFamilies(device);
    if(!indices.isComplete()) return false; 

    if(!checkSwapchainExtensionSupport(device)) return false;
    
    SwapChainSupportDetails swapChainSupport = querySwapChainSupport(device);
    return !swapChainSupport.formats.empty() && !swapChainSupport.presentModes.empty();
}
```

**Источник:** [https://metanit.com/cpp/vulkan/2.3.php](https://metanit.com/cpp/vulkan/2.3.php)
