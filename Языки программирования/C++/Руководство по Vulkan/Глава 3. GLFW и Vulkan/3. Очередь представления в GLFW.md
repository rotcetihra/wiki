# Очередь представления в GLFW

[[Языки программирования/C++|C++]] / [[Языки программирования/C++/Руководство по Vulkan|Руководство по Vulkan]] / [[Языки программирования/C++/Руководство по Vulkan/Глава 3. GLFW и Vulkan|Глава 3. GLFW и Vulkan]] / Очередь представления в GLFW

[[Языки программирования/C++/Руководство по Vulkan/Глава 3. GLFW и Vulkan/2. Поверхность рисования в GLFW|Назад]] | [[Языки программирования/C++/Руководство по Vulkan/Глава 3. GLFW и Vulkan|Содержание]] | [[Языки программирования/C++/Руководство по Vulkan/Глава 3. GLFW и Vulkan/4. Цепочка буферов и проверка ее поддержки на GLFW|Вперёд]]

**Дата написания:** 05.09.2026

Для получения очереди представления в приложении на GLFW определяем структуру для хранения обоих индексов семейств очередей:

```cpp
struct QueueFamilyIndices {
    uint32_t graphicsFamilyIndex;
    uint32_t presentFamilyIndex;
    VkBool32 graphicsSupport;
    VkBool32 presentSupport;
    bool isComplete() {
        return graphicsSupport && presentSupport;
    }
};
```

Проверка поддержки очереди представления:

```cpp
for (const auto& queueFamily : queueFamilies) {
    if (queueFamily.queueFlags & VK_QUEUE_GRAPHICS_BIT) {
        indices.graphicsFamilyIndex = i;
        indices.graphicsSupport = true;
    }
    vkGetPhysicalDeviceSurfaceSupportKHR(device, i, surface, &indices.presentSupport);
    if (indices.presentSupport) indices.presentFamilyIndex = i;
    if (indices.isComplete()) break;
    i++;
}
```

При создании логического устройства создаем обе очереди:

```cpp
std::set<uint32_t> uniqueQueueFamilies = {
    indices.graphicsFamilyIndex,
    indices.presentFamilyIndex
};

for (uint32_t queueFamily : uniqueQueueFamilies) {
    VkDeviceQueueCreateInfo queueCreateInfo{};
    queueCreateInfo.sType = VK_STRUCTURE_TYPE_DEVICE_QUEUE_CREATE_INFO;
    queueCreateInfo.queueFamilyIndex = queueFamily;
    queueCreateInfo.queueCount = 1;
    queueCreateInfo.pQueuePriorities = &queuePriority;
    queueCreateInfos.push_back(queueCreateInfo);
}

vkGetDeviceQueue(device, indices.graphicsFamilyIndex, 0, &graphicsQueue);
vkGetDeviceQueue(device, indices.presentFamilyIndex, 0, &presentQueue);
```

**Источник:** [https://metanit.com/cpp/vulkan/3.3.php](https://metanit.com/cpp/vulkan/3.3.php)
