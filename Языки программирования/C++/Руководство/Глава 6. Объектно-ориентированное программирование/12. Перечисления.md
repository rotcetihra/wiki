# Перечисления

[[Языки программирования/C++/Руководство/Глава 6. Объектно-ориентированное программирование|Глава 6. Объектно-ориентированное программирование]] / Перечисления

[[Языки программирования/C++/Руководство/Глава 6. Объектно-ориентированное программирование/11. Структуры|Назад]] | [[Языки программирования/C++/Руководство/Глава 6. Объектно-ориентированное программирование|Содержание]] | [[Языки программирования/C++/Руководство/Глава 6. Объектно-ориентированное программирование/13. Наследование|Вперёд]]

**Дата написания:** 05.09.2026

## Перечисления

Перечисления (enum) представляют еще один способ определения своих типов. Их отличительной особенностью является то, что они содержат набор числовых констант. Перечисление имеет следующую форму:

```cpp
enum class имя_перечисления { константа_1, константа_2, ... константа_N};
```

После ключевых **enum class** идет название перечисления, и затем в фигурных скобках перечисляются через запятую константы перечисления.

Определим простейшее перечисление:

```cpp
enum class Day {Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday}
```

Каждой константе сопоставляется некоторое числовое значение. По умолчанию первая константа получает в качестве значения 0, а остальные увеличиваются на единицу. Так, Monday будет иметь значение 0, Tuesday - 1 и так далее.

После создания перечисления мы можем определить его переменную и присвоить ей одну из констант:

```cpp
Day today {Day::Thursday};
```

Чтобы вывести значение переменной на консоль, можно использовать преобразование к типу int:

```cpp
#include <iostream>

enum class Day {Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday};
int main()
{
    Day today {Day::Thursday};
    std::cout << "Today: " << static_cast<int>(today) << std::endl;
}
```

Мы также можем управлять установкой значений в перечислении:

```cpp
enum class Day {Monday = 1, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday};
```

Можно даже назначать двум константам одно и то же значение:

```cpp
enum class Day {Monday = 1, Mon = 1, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday};
```

### Применение перечислений

Перечисления удобны, когда необходимо хранить ограниченный набор состояний и в зависимости от текущего состояния выполнять некоторые действия:

```cpp
#include <iostream>

enum class Operation {Add, Subtract, Multiply};

void calculate(int n1, int n2, Operation op)
{
    switch (op)
    {
        case Operation::Add:
            std::cout << n1 + n2 << std::endl;
            break;
        case Operation::Subtract:
            std::cout << n1 - n2 << std::endl;
            break;
        case Operation::Multiply:
            std::cout << n1 * n2 << std::endl;
            break;
    }
}
int main()
{
    calculate(10, 6, Operation::Add);           // 16
    calculate(10, 6, Operation::Subtract);      // 4
    calculate(10, 6, Operation::Multiply);      // 60
}
```

### Подключение констант перечисления

При обращении к константам перечисления по умолчанию необходимо указывать название перечисления. Но начиная со стандарта C++20 мы можем подключить константы перечисления в текущий контекст с помощью оператора using:

```cpp
using enum Day;
```

```cpp
#include <iostream>

enum class Day {Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday};
using enum Day;     // подключаем константы перечисления в текущую область видимости

int main()
{
    Day today {Thursday};
    std::cout << static_cast<int>(today) << std::endl;      // 3
    std::cout << static_cast<int>(Sunday) << std::endl;     // 6
}
```

## Источники

- https://metanit.com/cpp/tutorial/5.9.php
