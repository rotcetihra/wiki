# Преобразование типов

[[Языки программирования/C++/Руководство|Руководство]] / [[Языки программирования/C++/Руководство/Глава 6. Объектно-ориентированное программирование|Глава 6. Объектно-ориентированное программирование]] / Преобразование типов

[[Языки программирования/C++/Руководство/Глава 6. Объектно-ориентированное программирование/17. Виртуальные функции и их переопределение.md|Назад]] | [[Языки программирования/C++/Руководство/Глава 6. Объектно-ориентированное программирование|Содержание]] | [[Языки программирования/C++/Руководство/Глава 6. Объектно-ориентированное программирование/19. Динамическое преобразование.md|Вперёд]]

**Дата написания:** 05.09.2026

## Преобразование типов

Объект производного класса одновременно является объектом базового класса. Поэтому преобразования из производного типа в базовый выполняются автоматически.

```cpp
#include <iostream>
#include <string>

class Person
{
public:
    Person(std::string name): name{name}
    {  }
    void print() const
    {
        std::cout << "Person " << name << std::endl;
    }
private:
    std::string name;
};

class Employee: public Person
{
public:
    Employee(std::string name): Person{name} {}
};

int main()
{
    Employee employee{"Bob"};
    employee.print();

    // преобразуем в базовый тип
    Person person1{employee};
    person1.print();

    Person person2{"Tom"};
    person2 = employee;
    person2.print();
}
```

### Преобразование указателей

Указатель на объект производного класса можно преобразовать автоматически в указатель на объект базового типа:

```cpp
int main()
{
    Employee bob{"Bob", "Google"};
    Person* person{&bob};
    person->print();    // Bob (Google)
}
```

В некоторых случаях возможно приведение в обратную сторону - от базового к производному, но автоматически оно не выполняется. Для этого надо использовать функции преобразования, в частности, static_cast():

```cpp
int main()
{
    Employee sam{"Sam", "Microsoft"};
    Person* person {&sam};
    Employee* employee{static_cast<Employee*>(person)};
    employee->print();  // Sam (Microsoft)
}
```

### Преобразование smart-указателей

smart-указатели на базовый класс также могут указывать на объект производного класса:

```cpp
int main()
{
    std::unique_ptr<Person> bob{std::make_unique<Employee>("Bob", "Google")};
    bob->print();    // Bob (Google)

    std::shared_ptr<Person> tom{std::make_shared<Employee>("Tom", "Microsoft")};
    tom->print();    // Tom (Microsoft)
}
```

## Источники

- https://metanit.com/cpp/tutorial/5.25.php
