# all_of

[[Языки программирования/C++/Библиотеки|Библиотеки]] / [[Языки программирования/C++/Библиотеки/<algorithm>|<algorithm>]] / all_of

[[Языки программирования/C++/Библиотеки/<algorithm>/clamp|Назад]] | [[Языки программирования/C++/Библиотеки/<algorithm>|Содержание]] | [[Языки программирования/C++/Библиотеки/<algorithm>/any_of|Вперёд]]

**Дата написания:** 05.09.2026

## Прототип

```cpp
#include <algorithm>

template<class InputIt, class UnaryPredicate>
bool all_of(InputIt first, InputIt last, UnaryPredicate p);
```

## Параметры

| Параметр | Описание |
|---|---|
| `first`, `last` | Диапазон итераторов |
| `p` | Унарный предикат |

## Возвращаемое значение

`true` если `p` возвращает `true` для всех элементов, или диапазон пуст.

## Что делает

Проверяет, что предикат `p` возвращает `true` для каждого элемента в диапазоне.

## Примеры

```cpp
#include <algorithm>
#include <vector>

int main()
{
    std::vector<int> v = {2, 4, 6, 8};

    bool all_even = std::all_of(v.begin(), v.end(), [](int x){ return x % 2 == 0; }); // true
}
```

## Исключения

- **Исключения:** не бросает исключений (если предикат не бросает).

## Похожие функции

- [[Языки программирования/C++/Библиотеки/<algorithm>/any_of|any_of]] — хотя бы один элемент
- [[Языки программирования/C++/Библиотеки/<algorithm>/none_of|none_of]] — ни один элемент

## Источники

- https://en.cppreference.com/w/cpp/algorithm/all_of
- ISO/IEC 14882:2024

[[Языки программирования/C++/Библиотеки/<algorithm>/clamp|Назад]] | [[Языки программирования/C++/Библиотеки/<algorithm>|Содержание]] | [[Языки программирования/C++/Библиотеки/<algorithm>/any_of|Вперёд]]
