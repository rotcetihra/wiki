# Журнал изменений

Все важные изменения базы знаний фиксируются в этом файле. Записи группируются по датам (ДД.ММ.ГГГГ), новые — сверху. Подробное правило ведения — в разделе «Журнал изменений (CHANGELOG)» файла `AGENTS.md`.

## 03.08.2026

### Добавлено

- Глава 10 «Многопоточность»: урок 1 «Создание потоков» (тип `pthread_t`, `pthread_create`, `pthread_exit`, `sleep`) и индекс главы.
- Глава 10 «Многопоточность»: урок 2 «Завершение потоков» (`pthread_join`, ожидание нескольких потоков, получение результата потока).
- Глава 10 «Многопоточность»: урок 3 «Мьютексы» (`pthread_mutex_t`, `lock`/`unlock`, критические секции, взаимоблокировка).
- Глава 10 «Многопоточность»: урок 4 «Сигналы и условные переменные синхронизации» (`pthread_cond_signal`/`pthread_cond_wait`, защита от раннего пробуждения).
- Глава 10 «Многопоточность»: урок 5 «Семафоры» (`sem_t`, `sem_init`, `sem_destroy`, `sem_wait`, `sem_post`; упорядочивание действий потоков, ограничение количества потоков).
- Глава 10 «Многопоточность»: урок 6 «Введение в OpenMP» (директивы `parallel`, `for`, `simd`, `ordered`, `critical`, флаг `-fopenmp`, заголовочный файл `omp.h`, функция `omp_get_thread_num`).
- Глава 11 «Макросы»: урок 1 «Примеры распространенных макросов» (макросы `MIN`/`MAX`, побочные действия, `#` и `##`, `ARRAY_SIZE`, `SWAP`, `SAFE_FREE`, `OFFSETOF`) и индекс главы.
- Глава 11 «Макросы»: урок 2 «Макрос для вывода отладочной информации» (`DEBUG_PRINT`, `__FILE__`/`__LINE__`/`__func__`, вариативные макросы и `__VA_ARGS__`, управление через `#ifdef DEBUG`).
- Глава 11 «Макросы»: урок 3 «Макрос для создания обобщенного списка» (генерация структуры и функций через `DEFINE_LIST(T)`, `realloc`-расширение, списки через значения и указатели).
- Глава 11 «Макросы»: урок 4 «Макрос перебора списков в стиле for-each» (макрос `foreach` для односвязного списка, `for_each` для обобщенного списка, оператор запятой).
- Глава 11 «Макросы»: урок 5 «Стек на основе макросов» (`PUSH`/`POP`, `STACK_FULL`/`STACK_EMPTY`, `STACK_USED`, `CLEAR_STACK`, указатель вершины стека).
- Глава 9 «Стандартная библиотека C»: урок 1 «Заголовочные файлы стандартной библиотеки C» (состав библиотеки по стандартам) и индекс главы.
- Глава 9 «Стандартная библиотека C»: урок 2 «Работа со строками» (`strlen`, `strcmp`, `strcat`, `strcpy`/`strncpy`/`strncpy_s`, `strstr`).
- Глава 9 «Стандартная библиотека C»: урок 3 «Работа с памятью» (`memset`, `memcpy`, `memcmp`).
- Глава 9 «Стандартная библиотека C»: урок 4 «Работа с датами и временем» (`time`, `localtime`, структура `tm`, `strftime`).
- Глава 9 «Стандартная библиотека C»: урок 5 «Математические функции» (`pow`, `round`, `sqrt`, `abs`/`labs`/`llabs`, `fabs`).
- Глава 9 «Стандартная библиотека C»: урок 6 «Преобразование строк в числа и чисел в строки» (`strtol`, `snprintf`).
- Глава 9 «Стандартная библиотека C»: урок 7 «Обобщения и макрос _Generic» (имитация обобщений макросами с `##`, выбор реализации через `_Generic`).
- Глава 9 «Стандартная библиотека C»: урок 8 «Поддержка Unicode и кодировки UTF-8, UTF-16 и UTF-32» (`wchar_t`, `char8_t`/`char16_t`/`char32_t`, префиксы литералов, спецификаторы `%ls`/`%lc`).
- Глава 9 «Стандартная библиотека C»: урок 9 «Платформо-независимые целочисленные типы» (модели данных, типы из `stdint.h`, макросы `PRI`/`SCN` из `inttypes.h`).
- Глава 9 «Стандартная библиотека C»: урок 10 «Проверка символов и ctype.h» (`isspace`, `isalnum`, `isalpha`, `isdigit`, `isxdigit`, `iscntrl`, `isprint`, `isgraph`, `ispunct`, `islower`/`isupper`, `tolower`/`toupper`).
- Глава 8 «Ввод-вывод и работа с файлами»: урок 1 «Файлы и потоки ввода-вывода» (файл как поток, буферизация, `fopen`/`fclose`, режимы открытия, обработка ошибок) и индекс главы.
- Глава 8 «Ввод-вывод и работа с файлами»: урок 2 «Чтение и запись бинарных файлов» (`putc`, `getc`/`fgetc`, конец файла и значение `EOF`).
- Глава 8 «Ввод-вывод и работа с файлами»: урок 3 «Чтение и запись структур в файл» (побайтовая запись структуры, чтение через `malloc`, сохранение и чтение массива структур с метаинформацией о количестве).
- Глава 8 «Ввод-вывод и работа с файлами»: урок 4 «Чтение и запись в файл с помощью функций fwrite и fread» (запись строк и структур, чтение массива структур и по одной структуре, `feof`/`ferror`).
- Глава 8 «Ввод-вывод и работа с файлами»: урок 5 «Чтение и запись текстовых файлов» (`fputs`, `fgets`, копирование файлов).
- Глава 8 «Ввод-вывод и работа с файлами»: урок 6 «Форматируемый ввод-вывод» (`fprintf`, `fscanf`).
- Глава 8 «Ввод-вывод и работа с файлами»: урок 7 «Позиционирование в файле» (`fseek`, `ftell`, `rewind`, чтение и обновление структур по позиции).
- Глава 8 «Ввод-вывод и работа с файлами»: урок 8 «Консольный ввод-вывод» (потоки `stdin`/`stdout`/`stderr`, `getchar`/`putchar`, `puts`/`fputs`, `fgets` и конфликт с `scanf`).
- Глава 8 «Ввод-вывод и работа с файлами»: урок 9 «Форматированный ввод и вывод в строки. Функции sscanf и sprintf» (`sscanf` для разбора строк, связка с `fgets`, `sprintf`).
- Глава 7 «Динамическая память»: урок 1 «Выделение и освобождение памяти» (`malloc`, `calloc`, `realloc`, `free`, память под объекты, массивы и структуры) и индекс главы.
- Глава 7 «Динамическая память»: урок 2 «Выделение памяти для двухмерного массива произвольной длины» (зубчатый массив, `int **table` и `rows`, освобождение в обратном порядке).
- Глава 7 «Динамическая память»: урок 3 «Управление динамической памятью» (указатель как локальный, статический и глобальный объект, момент освобождения памяти).
- Глава 7 «Динамическая память»: урок 4 «Указатель как результат функции` (возврат указателя, возврат массива из функции, ошибка возврата локальной переменной).
- Глава 7 «Динамическая память»: урок 5 «Организация памяти программы и структура сегментов» (стек, куча, сегмент констант и глобальных переменных, сегмент кода).
- Глава 6 «Структуры»: урок 1 «Определение структур» (объявление и определение, неполные типы, инициализация, `typedef`, области видимости, копирование, ввод с консоли) и индекс главы.
- Глава 6 «Структуры»: урок 2 «Структуры как элементы структур» (вложенные структуры, доступ через несколько точек, инициализация вложенных структур).
- Глава 6 «Структуры»: урок 3 «Указатели на структуры» (операция `->`, самоссылающиеся структуры и списки, объявление до определения, константные структуры).
- Глава 6 «Структуры»: урок 4 «Массивы структур» (инициализация вложенными скобками, доступ по индексу, `sizeof`, указатели на массив структур).
- Глава 6 «Структуры»: урок 5 «Структуры и функции» (передача по значению, указатели на структуры в параметрах, возврат структуры из функции).
- Глава 6 «Структуры»: урок 6 «Размещение структур и их полей в памяти» (выравнивание полей и структур, отступы, выравнивание массивов, оптимизация расположения полей).
- Глава 6 «Структуры»: урок 7 «Составные литералы» (литералы структур, возврат структуры из функции, массивы через составные литералы).
- Глава 6 «Структуры»: урок 8 «Перечисления» (`enum`, числовые значения констант, `typedef`, безымянные перечисления, применение в `switch`).
- Глава 6 «Структуры»: урок 9 «Объединения» (`union`, наложение элементов, анонимные объединения и `typedef`, хранение значений разных типов, указатели).
- Глава 6 «Структуры»: урок 10 «Битовые поля» (поля с шириной в битах, размещение в памяти, проверка через объединение).
- Глава 5 «Препроцессор»: урок 1 (этапы компиляции, директивы препроцессора, `#include`, стандартные заголовочные файлы, подключение собственных файлов и `extern`) и индекс главы.
- Глава 5 «Препроцессор»: урок 2 «Директива #define» (определение идентификатора и макросов с параметрами, `#undef`, константа через `-D`).
- Глава 5 «Препроцессор»: урок 3 «Макросы» (макросы для выражений, макросы с параметрами, скобки в параметрах, операции `#` и `##`).
- Глава 5 «Препроцессор»: урок 4 «Условная компиляция» (`#if`/`#elif`/`#else`/`#endif`, `#ifdef`/`#ifndef`, оператор `defined`).
- Глава 5 «Препроцессор»: урок 5 «Встроенные макросы» (макросы компиляторов, архитектур, порядка байтов и операционных систем).
- Урок 12 «Функция как результат другой функции» в главу 4 «Функции» (возврат указателя на функцию, разбор заголовка `int (*select(int choice))(int, int)`, упрощение через `typedef`).
- Урок 13 «Функции с переменным количеством параметров» в главу 4 «Функции» (многоточие в списке параметров, макросы `va_start`/`va_arg`/`va_end` и тип `va_list` из `stdarg.h`).
- Урок 14 «Параметры командной строки» в главу 4 «Функции» (`argc`, `argv`, вывод аргументов).
- Правило ведения журнала изменений: создан `CHANGELOG.md`, в `AGENTS.md` добавлен раздел «Журнал изменений (CHANGELOG)».

### Изменено

- `AGENTS.md`: зафиксировано правило обновлять обзорную информацию о главе в заметке с её содержанием при добавлении новой статьи (урока).
- Глава 4 «Функции»: добавлена навигация «Вперёд» в урок 13 (к уроку 14), обновлены индекс и обзор главы (охват всех 14 уроков).
- Глава 5 «Препроцессор»: добавлена навигация «Вперёд» в урок 4 (к уроку 5), обновлены индекс и обзор главы (охват 5 уроков).
- Глава 6 «Структуры»: добавлена навигация «Вперёд» в урок 9 (к уроку 10), обновлены индекс и обзор главы (охват 10 уроков).
- Глава 7 «Динамическая память»: добавлена навигация «Вперёд» в урок 4 (к уроку 5), обновлены индекс и обзор главы (охват 5 уроков).
- Исправлена фактическая ошибка исходной статьи 7.1: прототип `free()` — `void free(void *bl)`, а не `void *free(void *bl)` (функция ничего не возвращает).
- Исправлена фактическая ошибка исходной статьи 8.1: режим `"w+"` описан неверно; по стандарту файл очищается сразу при открытии (создается для чтения и записи).
- Исправлена фактическая ошибка исходной статьи 8.4: `feof()` возвращает ненулевое значение при достижении конца файла (в исходнике было сказано «возвращает 0»).

## 02.08.2026

### Добавлено

- Глава 2 «Основы языка Си»: уроки 1–14 (структура программы, переменные, типы данных, консольный вывод, константы, арифметические/логические/поразрядные операции и операции присваивания, преобразование типов, условные конструкции, циклы, массивы и строки, ввод через `scanf`) и индекс главы.
- Глава 3 «Указатели»: уроки 1–7 (что такое указатели, операции с указателями, арифметика указателей, константы и указатели, указатели и массивы, указатели и строки, массивы указателей и многоуровневая адресация) и индекс главы.
- Глава 4 «Функции»: уроки 1–11 (определение и описание функций, параметры, результат, рекурсивные функции, область видимости переменных, внешние и статические объекты, классы хранения, указатели и массивы как параметры, указатели на функцию, тип функции, функции как параметры) и индекс главы.

### Изменено

- Обновлены обзоры глав 2 и 3 (охват всех уроков глав).
- Все уроки глав 2–4: исправлены фактические ошибки исходных статей (описание `size_t` и `ptrdiff_t`, поведение VLA, `int (*pa)[]` как неполный тип), опечатки, добавлены callout, таблицы и навигация по главе.

## 01.08.2026

### Изменено

- Курс C: введено правило нумерации файлов уроков (цифра в имени файла, без цифры в заголовке `#` и хлебных крошках), уроки главы 1 переименованы соответствующим образом.
- Добавлена навигация «Назад | Содержание | Вперёд» в уроки главы 1.
- `AGENTS.md`: зафиксированы правила именования файлов, навигации и мета-информации для курса C.
